From 0146000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: GCSA Aegis <aegis@gcsa.local>
Date: Sat, 19 Sep 2026 03:00:00 +0800
Subject: [PATCH] feat(aegis): gate process-backed ServiceWorker script traffic

---
diff --git a/chrome/browser/aegis/access/access_proxying_url_loader_factory.cc b/chrome/browser/aegis/access/access_proxying_url_loader_factory.cc
--- a/chrome/browser/aegis/access/access_proxying_url_loader_factory.cc
+++ b/chrome/browser/aegis/access/access_proxying_url_loader_factory.cc
@@ -366,6 +366,14 @@ void AccessProxyingURLLoaderFactory::MaybeProxyServiceWorkerSubResource(
   MaybeProxyProfileOnlyFactory(profile, render_process_id, factory_builder);
 }
 
+// static
+void AccessProxyingURLLoaderFactory::MaybeProxyServiceWorkerScript(
+    Profile* profile,
+    int render_process_id,
+    network::URLLoaderFactoryBuilder& factory_builder) {
+  MaybeProxyProfileOnlyFactory(profile, render_process_id, factory_builder);
+}
+
 // static
 void AccessProxyingURLLoaderFactory::MaybeProxyNavigation(
     Profile* profile,
diff --git a/chrome/browser/aegis/access/access_proxying_url_loader_factory.h b/chrome/browser/aegis/access/access_proxying_url_loader_factory.h
--- a/chrome/browser/aegis/access/access_proxying_url_loader_factory.h
+++ b/chrome/browser/aegis/access/access_proxying_url_loader_factory.h
@@ -35,12 +35,13 @@ class AccessProxyingURLTrackedRequest;
 // dispatch. It covers primary-page document subresources, primary-page
 // main-frame/subframe navigation, Worker main-script factories, frame-owned
 // Worker subresource factories, frame-less Worker subresource factories, and
-// process-backed ServiceWorker subresource factories with browser-owned
-// Profile-only process/partition attribution, including redirect follow
-// re-evaluation under one stable logical request identity. ServiceWorker
-// script/update, prefetch/preconnect, WebSocket, BFCache, and prerender remain
-// later slices. Each request is re-evaluated from its browser-owned attribution
-// source and the latest published Access state
+// process-backed ServiceWorker subresource plus script/importScripts factories
+// with browser-owned Profile-only process/partition attribution, including
+// redirect follow re-evaluation under one stable logical request identity.
+// Browser-process ServiceWorker install/update script fetches,
+// prefetch/preconnect, WebSocket, BFCache, and prerender remain later slices.
+// Each request is re-evaluated from its browser-owned attribution source and the
+// latest published Access state
 // before it may reach the target Network Service factory.
 //
 // DIRECT/absent policy is forwarded unchanged. A PROXY decision is forwarded
@@ -84,6 +85,10 @@ class AccessProxyingURLLoaderFactory : public network::mojom::URLLoaderFactory {
       Profile* profile,
       int render_process_id,
       network::URLLoaderFactoryBuilder& factory_builder);
+  static void MaybeProxyServiceWorkerScript(
+      Profile* profile,
+      int render_process_id,
+      network::URLLoaderFactoryBuilder& factory_builder);
   static void MaybeProxyNavigation(
       Profile* profile,
       content::RenderFrameHost* frame,
diff --git a/chrome/browser/aegis/access/access_proxying_url_loader_factory_browsertest.cc b/chrome/browser/aegis/access/access_proxying_url_loader_factory_browsertest.cc
--- a/chrome/browser/aegis/access/access_proxying_url_loader_factory_browsertest.cc
+++ b/chrome/browser/aegis/access/access_proxying_url_loader_factory_browsertest.cc
@@ -45,33 +45,53 @@ constexpr char kTargetHost[] = "target.example";
 constexpr char kUnselectedRedirectHost[] = "redirect-unselected.example";
 constexpr char kProxyGroup[] = "proxy-group-browser-test";
 
+void ConfigureServiceWorkerMainScriptResponse(
+    net::test_server::BasicHttpResponse* response) {
+  response->set_code(net::HTTP_OK);
+  response->set_content(
+      "importScripts('/aegis-service-worker-imported.js');"
+      "self.addEventListener('install', event => "
+      "  event.waitUntil(self.skipWaiting()));"
+      "self.addEventListener('activate', event => "
+      "  event.waitUntil(self.clients.claim()));"
+      "self.addEventListener('message', event => {"
+      "  const port = event.ports[0];"
+      "  if (event.data === 'aegis-script-source') {"
+      "    port.postMessage(self.aegisImportedScriptSource || 'missing');"
+      "    return;"
+      "  }"
+      "  event.waitUntil(fetch(event.data, {mode: 'no-cors'})"
+      "    .then(() => port.postMessage('resolved'))"
+      "    .catch(() => port.postMessage('error')));"
+      "});");
+  response->set_content_type("application/javascript");
+}
+
 std::unique_ptr<net::test_server::HttpResponse>
 ServiceWorkerOriginReply(std::atomic<size_t>* counter,
                          const net::test_server::HttpRequest& request) {
-  const bool is_script =
+  const bool is_main_script =
       request.relative_url.find("/aegis-service-worker.js") !=
       std::string::npos;
+  const bool is_imported_script =
+      request.relative_url.find("/aegis-service-worker-imported.js") !=
+      std::string::npos;
   const bool is_data =
       request.relative_url.find("/service-worker-data") != std::string::npos;
-  if (!is_script && !is_data) {
+  if (!is_main_script && !is_imported_script && !is_data) {
     return nullptr;
   }
 
   counter->fetch_add(1, std::memory_order_relaxed);
   auto response = std::make_unique<net::test_server::BasicHttpResponse>();
   response->set_code(net::HTTP_OK);
-  if (is_script) {
+  if (is_main_script) {
+    ConfigureServiceWorkerMainScriptResponse(response.get());
+    return response;
+  }
+  if (is_imported_script) {
     response->set_content(
-        "self.addEventListener('install', event => "
-        "  event.waitUntil(self.skipWaiting()));"
-        "self.addEventListener('activate', event => "
-        "  event.waitUntil(self.clients.claim()));"
-        "self.addEventListener('message', event => {"
-        "  const port = event.ports[0];"
-        "  event.waitUntil(fetch(event.data, {mode: 'no-cors'})"
-        "    .then(() => port.postMessage('resolved'))"
-        "    .catch(() => port.postMessage('error')));"
-        "});");
+        "self.aegisImportedScriptSource = 'origin-service-worker-script';");
     response->set_content_type("application/javascript");
     return response;
   }
@@ -84,14 +104,32 @@ ServiceWorkerOriginReply(std::atomic<size_t>* counter,
 std::unique_ptr<net::test_server::HttpResponse>
 ServiceWorkerProxyReply(std::atomic<size_t>* counter,
                         const net::test_server::HttpRequest& request) {
-  if (request.relative_url.find("/service-worker-data") ==
-      std::string::npos) {
+  const bool is_main_script =
+      request.relative_url.find("/aegis-service-worker.js") !=
+      std::string::npos;
+  const bool is_imported_script =
+      request.relative_url.find("/aegis-service-worker-imported.js") !=
+      std::string::npos;
+  const bool is_data =
+      request.relative_url.find("/service-worker-data") != std::string::npos;
+  if (!is_main_script && !is_imported_script && !is_data) {
     return nullptr;
   }
 
   counter->fetch_add(1, std::memory_order_relaxed);
   auto response = std::make_unique<net::test_server::BasicHttpResponse>();
   response->set_code(net::HTTP_OK);
+  if (is_main_script) {
+    ConfigureServiceWorkerMainScriptResponse(response.get());
+    return response;
+  }
+  if (is_imported_script) {
+    response->set_content(
+        "self.aegisImportedScriptSource = 'proxy-service-worker-script';");
+    response->set_content_type("application/javascript");
+    return response;
+  }
+
   response->set_content("proxy-service-worker-subresource");
   response->set_content_type("text/plain");
   return response;
@@ -453,25 +491,23 @@ class AccessProxyingURLLoaderFactoryBrowserTest : public InProcessBrowserTest {
         .ExtractString();
   }
 
-  void StartServiceWorkerSubresourceHarness() {
-    EXPECT_EQ(
-        content::EvalJs(
-            web_contents(),
-            content::JsReplace(
-                "new Promise(async resolve => {"
-                "  try {"
-                "    const registration = "
-                "        await navigator.serviceWorker.register($1);"
-                "    await navigator.serviceWorker.ready;"
-                "    const worker = registration.active;"
-                "    if (!worker) { resolve('error'); return; }"
-                "    window.aegisServiceWorker = worker;"
-                "    resolve('ready');"
-                "  } catch { resolve('error'); }"
-                "})",
-                service_worker_script_url().spec()))
-            .ExtractString(),
-        "ready");
+  std::string StartServiceWorkerHarness() {
+    return content::EvalJs(
+               web_contents(),
+               content::JsReplace(
+                   "new Promise(async resolve => {"
+                   "  try {"
+                   "    const registration = "
+                   "        await navigator.serviceWorker.register($1);"
+                   "    await navigator.serviceWorker.ready;"
+                   "    const worker = registration.active;"
+                   "    if (!worker) { resolve('error'); return; }"
+                   "    window.aegisServiceWorker = worker;"
+                   "    resolve('ready');"
+                   "  } catch { resolve('error'); }"
+                   "})",
+                   service_worker_script_url().spec()))
+        .ExtractString();
   }
 
   void PrepareServiceWorkerSubresourceTest(size_t* origin_before,
@@ -480,11 +516,23 @@ class AccessProxyingURLLoaderFactoryBrowserTest : public InProcessBrowserTest {
     ASSERT_NE(proxy_before, nullptr);
     ASSERT_TRUE(
         ui_test_utils::NavigateToURL(browser(), service_worker_page_url()));
-    StartServiceWorkerSubresourceHarness();
+    EXPECT_EQ(StartServiceWorkerHarness(), "ready");
     *origin_before = origin_requests_.load(std::memory_order_relaxed);
     *proxy_before = proxy_requests_.load(std::memory_order_relaxed);
   }
 
+  std::string ServiceWorkerScriptSource() {
+    return content::EvalJs(
+               web_contents(),
+               "new Promise(resolve => {"
+               "  const worker = window.aegisServiceWorker;"
+               "  const channel = new MessageChannel();"
+               "  channel.port1.onmessage = event => resolve(event.data);"
+               "  worker.postMessage('aegis-script-source', [channel.port2]);"
+               "})")
+        .ExtractString();
+  }
+
   std::string FetchServiceWorkerSubresource(const GURL& url) {
     return content::EvalJs(
                web_contents(),
@@ -809,6 +857,38 @@ IN_PROC_BROWSER_TEST_F(AccessProxyingURLLoaderFactoryBrowserTest,
   EXPECT_EQ(origin_requests_.load(std::memory_order_relaxed), origin_before);
 }
 
+IN_PROC_BROWSER_TEST_F(
+    AccessProxyingURLLoaderFactoryBrowserTest,
+    ServiceWorkerProcessScriptWithoutPolicyPreservesNativePath) {
+  ASSERT_TRUE(
+      ui_test_utils::NavigateToURL(browser(), service_worker_page_url()));
+
+  EXPECT_EQ(StartServiceWorkerHarness(), "ready");
+  EXPECT_EQ(ServiceWorkerScriptSource(), "origin-service-worker-script");
+  EXPECT_EQ(proxy_requests_.load(std::memory_order_relaxed), 0u);
+}
+
+IN_PROC_BROWSER_TEST_F(AccessProxyingURLLoaderFactoryBrowserTest,
+                       ServiceWorkerProcessScriptUsesSelectedProxy) {
+  ASSERT_TRUE(
+      ui_test_utils::NavigateToURL(browser(), service_worker_page_url()));
+  PublishProxyPolicy(/*publish_endpoint=*/true, "localhost");
+
+  EXPECT_EQ(StartServiceWorkerHarness(), "ready");
+  EXPECT_EQ(ServiceWorkerScriptSource(), "proxy-service-worker-script");
+  EXPECT_GT(proxy_requests_.load(std::memory_order_relaxed), 0u);
+}
+
+IN_PROC_BROWSER_TEST_F(AccessProxyingURLLoaderFactoryBrowserTest,
+                       ServiceWorkerProcessScriptWithoutEndpointFailsClosed) {
+  ASSERT_TRUE(
+      ui_test_utils::NavigateToURL(browser(), service_worker_page_url()));
+  PublishProxyPolicy(/*publish_endpoint=*/false, "localhost");
+
+  EXPECT_EQ(StartServiceWorkerHarness(), "error");
+  EXPECT_EQ(proxy_requests_.load(std::memory_order_relaxed), 0u);
+}
+
 IN_PROC_BROWSER_TEST_F(
     AccessProxyingURLLoaderFactoryBrowserTest,
     ServiceWorkerSubresourceWithoutPolicyPreservesNativePath) {

diff --git a/chrome/browser/chrome_content_browser_client.cc b/chrome/browser/chrome_content_browser_client.cc
--- a/chrome/browser/chrome_content_browser_client.cc
+++ b/chrome/browser/chrome_content_browser_client.cc
@@ -6758,10 +6758,18 @@ void ChromeContentBrowserClient::WillCreateURLLoaderFactory(
   if (type == URLLoaderFactoryType::kServiceWorkerSubResource && !frame) {
     aegis::access::AccessProxyingURLLoaderFactory::
         MaybeProxyServiceWorkerSubResource(
             Profile::FromBrowserContext(browser_context), render_process_id,
             factory_builder);
   }
 
+  if (type == URLLoaderFactoryType::kServiceWorkerScript && !frame &&
+      render_process_id != content::ChildProcessHost::kInvalidUniqueID) {
+    aegis::access::AccessProxyingURLLoaderFactory::
+        MaybeProxyServiceWorkerScript(
+            Profile::FromBrowserContext(browser_context), render_process_id,
+            factory_builder);
+  }
+
 #if BUILDFLAG(ENABLE_CAPTIVE_PORTAL_DETECTION)
   if (disable_secure_dns) {
     WebContents* web_contents = WebContents::FromRenderFrameHost(frame);
