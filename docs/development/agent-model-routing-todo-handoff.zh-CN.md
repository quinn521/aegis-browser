# Agent 模型路由与成本统计：TODO、Plan 与 Handoff

更新日期：2026-09-23（Asia/Shanghai）

状态：**DEFERRED — 保留已合并代码，暂停后续扩展，等待重新排期**

当前产品主线：**网络代理 / Access Service**

## 1. 已确定的决策

- 保留 [PR #166](https://github.com/quinn521/aegis-browser/pull/166)，本次不 revert。
- 将真实 API 联调、成本对照实验、配置热更新回归和后续路由优化列为 TODO，不继续争用网络代理主线的开发和构建资源。
- 当前优先完成 [PR #162](https://github.com/quinn521/aegis-browser/pull/162) 的固定 Chromium 原生及浏览器验证，再继续网络代理计划。
- 本文只记录排期和交接，不改变用户现有设置，不关闭或开启任何模型服务，不修改生产代码、CI required rules 或 Chromium 版本。
- 冻结的是后续功能工作；若已合并代码出现影响代理主线的实际缺陷，仍须修复或评估受控回退。

理由：#166 已合并，本轮审查未确认新的 P1/P2 问题。立即撤销会改变依赖它的产品基线并增加重验成本。TypeSafe 默认关闭、路由默认 fixed，但 #166 仍改动共享 Agent 执行和持久化路径，不能把“默认关闭 TypeSafe”等同于“整个 PR 不生效”。

## 2. 它解决什么，不解决什么

#166 服务于 Aegis 浏览器内的 Agent：根据任务能力需求，从配置的模型池选择主模型和一个备用模型，并冻结 reasoning effort、输出预算和价格快照；逐次记录快筛、生成、修复和回退调用的用量及成本。

TypeSafe 是可选的目标理解入口。关闭它后，自动路由仍可使用浏览器内置的保守能力要求。仅本地模式针对新路由排除云端候选并绕过 TypeSafe。

本功能尚未接入 Codex 开发任务调度，不会自动调整开发窗口的模型，也不会缩短 Chromium 编译或 CI 时间。当前确定的收益是配置能力和观测能力；省钱、提速和正确率提升均未通过真实配对实验验证。

## 3. 冻结身份与证据边界

以下是 2026-09-23 交接时的事实快照，恢复工作前必须回读实时状态。

| 项目 | 身份或状态 |
| --- | --- |
| #166 原始 base B | `2f5aeeb235eeb988a96c88a322196c8b2838d4e6` |
| #166 最终 head H | `57e9c4783907ec0763aab54b58c45474bcdb9860` |
| #166 squash 提交 S | `b81d0eff320b899538a3ccd828e714f9aa9f28f1` |
| 合并状态 | GitHub 已合并；H 与 S 的 tree 相同 |
| 文档起点 develop | `5c6fff20e51341db35c4da5f2731032861773fe8` |
| #162 当时 head | `ba593955c1e5c9cf42399f70bb550faa87f878e4`，尚未合并 |
| #162 当时原生验证 | LLD + SDK 26.5 环境的 attempt2 仍在构建；GTest 执行数为 0，浏览器回归待执行 |

### 本轮 #166 复审

已检查模型候选过滤与排序、TypeSafe 五答案校验、主备模型/profile 冻结、固定及仅本地路径、规划回退和异步派发；同时检查成本协议、逐次调用记录、持久化、WebUI 与离线核算器。指定范围内未确认新的 P1/P2 问题。

在 H 上重新执行：

- Node `22.23.1`：离线核算器 11 项通过，模型选择回归 14/14 通过，Agent UI 状态回归通过。
- 直接使用生产头文件的 C++ generation profile 与分项 token 计价 standalone 测试通过。
- 产品工作树未修改。

独立审查调用配置为 Astra/high，但执行者只自报可识别 GPT-6；该次不单独充当指定模型身份已核验的交付凭据。

**尚缺的证据：**#166 相关最终代码的实际 Chromium GTest、侧栏浏览器行为、真实 TypeSafe/生成模型联调，以及质量与成本配对实验。本轮复审和 standalone 测试不替代这些证据；#162 的有限测试范围也不能自动证明整个 #166 已验收。

## 4. 配置热更新的已知行为

必须先安装并运行包含 #166 的浏览器构建；合并代码不会更新已启动的旧程序。之后支持运行时保存模型池、路由模式及 TypeSafe 设置。

| 时点或操作 | 当前实现 |
| --- | --- |
| 新任务 | 使用新配置选择模型，并冻结任务级配置 |
| 目标理解尚未完成 | 设置变化取消待完成的 TypeSafe/目标路由 |
| 模型池、路由模式或固定模型配置变化时，生成请求等待落盘、尚未发网 | 检测旧配置 generation 并拒绝派发；Planning 失败，Running/Reflecting 暂停 |
| 仅修改 TypeSafe 开关或凭据时，生成请求等待落盘、尚未发网 | 取消待完成的目标路由，但不递增模型派发 generation；该生成请求仍可能发网 |
| 生成请求已经发网 | 保存新配置本身不会自动取消该请求，返回结果仍可能推进原任务 |
| 旧任务的下一次调用 | 重新核验目的地授权并读取凭据，仍使用冻结模型/profile |
| 从云端模式切换为仅本地 | 不保证旧云端任务立即停止，也不自动将其迁移到本地模型 |
| 立即停止某任务 | 使用任务取消操作，不能用删除池条目或切换模式代替 |
| 新增供应商协议或修改算法 | 需要代码、构建和浏览器更新，不能仅靠配置完成 |

目的地只要仍满足“当前显式固定模型目的地”或“启用且授权的池条目”之一，就可能继续获准。因此，从模型池删除条目也不一定撤销其固定模型授权。

恢复开发时应优先覆盖“请求已发出后修改配置 → 接收返回 → 下一轮授权检查”的完整服务回归。是否需要全局立即停止云端调用，是待产品决策的新语义，不能在补测试时静默改变。

## 5. 恢复条件

以下条件满足后再安排本 TODO；网络代理验收完成不会自动启动模型路由工作。

- [ ] 网络代理主线负责人确认 #162 及适用的原生、浏览器和合并后 CI 证据已完成，或明确给出新的优先级安排。
- [ ] 明确本轮要验证的 Agent 使用场景、收益目标和投入上限。
- [ ] 从最新 develop 刷新代码、规则和依赖，检查 #166 后续是否已被修复、替代或部分回退。
- [ ] 指定一个实现负责人和独立评审者，确认可用构建窗口；不并发操作代理验证占用的 Chromium source/out。
- [ ] 涉及真实服务时明确凭据来源、数据范围和调用预算；本文不会触发任何付费调用。

## 6. 分阶段开发计划

| 阶段 | TODO | 交付与完成条件 |
| --- | --- | --- |
| R0：重新建立基线 | 回读 PR/提交/配置/测试状态，列出本轮目标和不做项 | 确认新的 B/H、当前行为和测试清单；不沿用旧提交的通过结论 |
| R1：配置变化回归 | 补充派发前、发网后、暂停恢复、取消、移除主备模型、切换仅本地的边界测试 | 精确断言是否发网、调用次数、采用的模型/profile、任务状态和观测记录；区分正常完成与拒绝路径 |
| R2：原生与浏览器验收 | 将相关生产与测试代码纳入固定 Chromium 目标，执行服务/协议/路由/存储 GTest 和侧栏场景 | 记录 pin、源码 tree、args、目标、二进制哈希、非零测试数、退出码及 sourceStable；覆盖设置保存、启动、回退、取消和恢复 |
| R3：小规模真实联调 | 在约定预算内验证 TypeSafe 与实际生成模型，先从最小任务开始 | 记录实际模型版本、耗时、用量、失败及回退；缺失价格保持未知；不把 API 通路成功视作收益验证 |
| R4：配对收益实验 | 相同冻结任务分别运行固定高档、固定低档和自动路由 | 独立正确性标签、完整 cohort，纳入快筛/失败/重试/回退开销；按预先冻结阈值比较成本、正确率与延迟 |
| R5：决定是否继续 | 基于 R1–R4 结果决定保留试验、优化、扩大使用或关闭 | 形成有证据的结论；无收益或收益不确定时维持 TODO/固定模式，不宣称节省 |

### R1 必测清单

- [ ] 固定模型、未配置 profile、自定义兼容端点继续可用。
- [ ] TypeSafe 关闭、低置信度、缺失或非法答案、超时均按既有规则处理；仅本地不发起 TypeSafe 请求。
- [ ] 主模型首个规划请求临时失败时最多回退一次；永久错误、格式修复、执行及监控阶段不擅自换模型。
- [ ] 已发网后修改模式、移除模型或更换凭据，核验在途返回与下一次授权，而非只检查设置已保存。
- [ ] 取消及暂停恢复不重复工具动作，不让过期回调推进新的任务执行。
- [ ] 快筛失败、取消、任务创建失败和未返回 usage 的调用保留记录；快筛与任务关联后不重复计费。
- [ ] 缓存输入和推理 token 的子集关系正确；缺失 usage/价格保持未知；数据库恢复不把不完整账目变成完整成本。

实验样本、阈值和验收口径在查看结果前确定；沿用已有文档时，先统一其中不同用途的试点阈值，不能事后选择有利标准。

## 7. 何时考虑 revert

当前不执行撤销。只有出现可复现、可归因于 #166 的问题，再比较最小修复和 revert 的成本，例如：

- 持续阻塞网络代理的编译、链接或运行，而短期无法通过局部修复解除。
- 出现权限、隐私或不可接受的行为回归。
- 产品明确放弃该方向，并完成对后续提交和持久化数据的影响分析。

回退必须使用独立、可审查的提交/PR，保留既有提交和证据。特别检查任务数据库版本兼容：#166 将 store 版本及 compatible version 提升至 11，旧实现为 8；不能假设回退代码能直接读取已经升级的数据，也不能通过删除用户数据库来完成回退。

任何基线变化后，重新覆盖受影响的代理验证、Review、CI 和适用运行测试。流程以 [DEV CI 指南](ci.zh-CN.md) 为准。

## 8. 下一窗口 Handoff

> 当前主线是网络代理 / Access Service。#166 已合并，用户决定保留代码、不 revert，模型路由后续工作处于 DEFERRED。
>
> 先刷新 #162、develop 及真实原生/浏览器验收状态，继续代理主线，不自动启动本 TODO。不要重做已完成的 Phase 1 静态检查体系和 Phase 2 PolicyEngine 单测建设。
>
> 若收到恢复模型路由工作的安排，先读本文及以下设计、评估文档，从 R0 开始。已有任务模型被冻结；配置动态保存不等于在途调用立即取消，local-only 也不是旧云端任务的全局终止开关。
>
> 本轮审查未确认 P1/P2；离线测试通过，但实际 Chromium、浏览器、真实 API 和收益实验尚未完成。不能把 #166 已合并、#162 某些目标通过或 CI 绿灯当成整个功能已验证。
>
> 从最新基线建立隔离工作区，实现者修复、独立评审者复审，明确构建资源所有者。任何 Chromium source/out 的切换都先确认旧进程和构建锁已退出；不要破坏正在进行的代理验证。

### 入口与资料

- [TypeSafe 与模型路由设计](typesafe-ai.zh-CN.md)
- [离线配对评估协议](agent-model-routing-evaluation.zh-CN.md)
- [DEV CI 与交付规则](ci.zh-CN.md)
- [Access Service 计划入口](../plans/access-service-v1.0/overview.zh-CN.md)
- [模型路由实现](../../apps/browser/overlay/chrome/browser/aegis/agent/agent_model_router.cc)
- [Agent 服务与异步派发](../../apps/browser/overlay/chrome/browser/aegis/agent/aegis_agent_service.cc)
- [计价与未知用量处理](../../apps/browser/overlay/chrome/browser/aegis/agent/agent_model_accounting.h)
- [持久化与数据库迁移](../../apps/browser/overlay/chrome/browser/aegis/agent/agent_task_store.cc)
- [离线评估脚本](../../apps/browser/scripts/evaluate-agent-model-routing.mjs)

文档维护时只更新本文的排期和证据快照，不把尚未执行的 TODO 标为完成。
