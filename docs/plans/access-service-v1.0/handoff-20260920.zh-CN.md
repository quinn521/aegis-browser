# Aegis 访问服务开发交接：2026-09-20（更新至 2026-09-21）

## 2026-09-21：commit/publish snapshot 阶段（待验证）

本阶段基于 `develop@3154d39871ddd592a1be609abf9c90c78d7e29c6`，范围为普通 DIRECT/PROXY 网站协议组事务。此前上游晋升已经完成；本阶段只交付一个 `develop` PR，创建后停止，不继续依赖阶段。以下历史核验记录不作为本阶段证据。

冻结事务顺序为：验证可信 selector 与 store → PREPARED journal 预留 operationSequence → 从旧 durable base 构造完整候选 → 发布到浏览器 request runtime → 向所属 NetworkContext 发布精确 operation/G/S/E 与 owner/partition → 全部候选 ACK → 再原子 durable commit → finalize。PREPARED 的 `committed_policy_generation` 始终为 0，候选及成功提交的 policy generation 都等于预留的 operationSequence。请求路由只读取内存快照，不读取 SQLite。`RepublishCurrentConfigWithAck` 不能作为候选发布证据。

Profile 持有首个可信 store（含 ephemeral 会话库），后续 mutation 传空指针复用，禁止替换库。候选保留无关网站组和独立规则。浏览器同步重绑定保留端点的 policy generation，并在 DIRECT 时从 CustomProxyConfig 的 exact-host 列表移除目标网站、保留其他网站；候选 config 与 metadata 使用同一 Mojo channel 顺序发送。失败仅在候选仍精确匹配时恢复旧快照/端点，并 supersede journal；清理写入失败透传 store 错误并保留待恢复边界。提交前重新核对 runtime、权威 selection source 的 S/E、完整端点及所有 NetworkContext 的集合；新 context、版本变化、错误或丢失 ACK、30 秒超时均阻止提交。迟到 ACK 不能复活已取消的事务；失败的 exact identity 释放 tracker 容量。Network Service 在受信任的 context channel 上保存 owner 绑定、单调的候选回执；它不从数据库重建策略。BLOCK/ALLOW 的屏障及取消流程、可信 UI 入口、真实身份/节点提交与 Xray 集成不属于此阶段。

| 证据 | 当前边界 |
| --- | --- |
| 实现及测试源码 | 已增加 candidate builder、coordinator transaction、runtime rollback、transport/version ACK，以及 unit/真实主导航回归源码；顺序补丁 0152/0153 必须与 overlay 对齐。 |
| 本地 standalone C++ | 本轮执行通过 845 checks，包含 tracker version/abort 回归；不是 SQLite/GN/GTest/browser 执行证据。最终 HEAD 的完整质量报告另存 artifact 并在 PR 绑定。 |
| Chromium/GTest/真实入口 | **NOT_RUN / BLOCKED**：隔离 replay 树缺 `third_party/llvm-build`、`buildtools/mac` 与 build output。不能复用旧 workspace 的二进制，不修改该 workspace。 |
| 独立审查 | 中途 GPT-6 新上下文审查发现阻塞项并推进修复；最终 HEAD 仍须复审。指定 PRO 首次连接失败后已完成 `07e192d` 审查，指出提交后 finalize 语义；当前改为提交前核验全部 tracker 条件，随后同一 UI 同步调用栈完成 durable/内存 finalize，不把已提交状态返回成已回滚失败。新 HEAD 仍待 PRO 复审，不声明 merge-clear。 |
| Hosted CI / 合并 | 本阶段尚无最终 HEAD hosted PASS 或合并证据。原基线或旧 PR 绿灯不能代替。 |
| G0 | **UNVERIFIED**。基础质量或测试源码不能升级为 native/runtime 验收。 |

合并前必须在同一固定候选实际执行 coordinator、store、runtime、transport、dispatch/tracker 单元与回归，并执行 `MainNavigationConsumesPreparedSnapshotBeforeCommit`、`MainNavigationRoutingIsolatedAcrossProfiles` 及既有导航/redirect/Worker/SharedWorker/Service Worker/missing endpoint/BLOCK/LoadingPredictor/frame prefetch 矩阵。零匹配、非零退出、不同候选均失败。之后冻结最终 HEAD，重跑 full quality（sourceStable 与源码摘要一致）、hosted 必需 CI 和独立 PRO 复审。**存在上述欠账时 PR 保持 Draft，不开启可导致提前合并的 auto-merge。**

下一依赖阶段只能在本 PR 实际 MERGED 且对应 develop push CI 通过后，从最新 develop 新建 `codex/*` 隔离 worktree 开始；后续阶段继续同 PR 更新开发计划与 Handoff。


本页记录 2026-09-20（Asia/Shanghai）的**核验快照**，SHA、PR 和 CI 结果只对应当时的精确来源，不是未来 `develop`、本文档提交或新 PR 的通过证明。后续工作顺序见[当前开发计划](development-plan.zh-CN.md)，逐项状态更新到[A/PF 验收追踪表](acceptance-tracker.zh-CN.md)，行为和门槛仍以[冻结规范修订 4](spec.zh-CN.md)为准。分支流、最终 HEAD 检查与上游推进规则见[DEV CI 指南](../../development/ci.zh-CN.md)。

## 来源与代码状态

| 对象 | 本次核验值 / 边界 |
| --- | --- |
| 个人 Fork 开发基线 | `quinn521/aegis-browser:develop@287aa54c678e8c174b6b286e387a1457cf3a218f`；该 merge 的第二父提交为 `1f1a1c46fdb1411183a48a21b3676efc766d6a14`，两者源码树均为 `e3a26d4083a23625fad000ea56df8ca449474483`。本交接文档提交会产生新的 HEAD，不能沿用旧 SHA 的门禁。 |
| 最近源码修复 | [Fork PR #132](https://github.com/quinn521/aegis-browser/pull/132) 已合入上述 `develop`：修复顺序补丁的格式/过期内容、补齐 `content/test:test_support` 直接 GN 依赖，并增加全补丁格式门。当前 Access 补丁序列已到 `0149`。此前 [Fork PR #130](https://github.com/quinn521/aegis-browser/pull/130) 与 [Fork PR #131](https://github.com/quinn521/aegis-browser/pull/131) 恢复 0148 并修复 browser-test 直接 GN 依赖。 |
| 公开来源 | 个人 `main` 的 [Fork PR #129](https://github.com/quinn521/aegis-browser/pull/129) 是一次源码晋升；[上游 PR #18](https://github.com/gcsagroup/aegis-browser/pull/18) 当时已合入 `gcsagroup/aegis-browser:main@1f1867b30803a35d122340b980d82590b8d0bcac`，个人 `main` 当时相同。PR #132 较新修复尚未公开晋升。先前对 #129/#18 的一次性源码晋升例外属于历史事件，不是后续合并、门禁或发布的通用豁免。 |
| 同步状态 | GitHub 默认分支当时为 `develop`；[Fork PR #133](https://github.com/quinn521/aegis-browser/pull/133)（`main`→`develop`）当时 OPEN/BEHIND，后续须重新回读，不从此快照推断已合并。 |
| 当前阶段 PR | [Fork PR #135](https://github.com/quinn521/aegis-browser/pull/135) 目标为 `develop`，交付 Profile-owned `AccessServiceCoordinator` 生命周期、coordinator 单元回归和两个普通 Profile 的主导航隔离回归。`da55df40ba992d669b454bd4579cac722e395c43` 的仓库本地 full quality 与 hosted 必需检查曾成功，但独立 PRO 最终 HEAD 复审未 clear：P1 指出 coordinator 测试目标缺 Chrome unit harness，且两个直接构造 `TestingProfile` 的生命周期用例缺 `BrowserTaskEnvironment`。当前修复已切换到 `chrome/test:test_support` + `test_support_unit` + `content/test:test_support`，并给直接构造 Profile 的测试 fixture 增加 `BrowserTaskEnvironment`；顺序补丁 `0150` 同步重新生成。该修复会产生新的最终 HEAD，因此 `da55df40` 的 local/hosted/PRO 证据全部 stale，必须重新覆盖。 |

已有源码包含路由/匹配/协议组、规则原子提交与恢复、请求归属/取消、dispatch BLOCK、五项 generation 所有者及 tuple 组装；factory 入口覆盖导航、文档子资源、重定向、Worker 主脚本及有 frame/无 frame 子资源、process-backed Service Worker 主/导入脚本及子资源、frame prefetch、LoadingPredictor。`CommitIdentity`、`CommitSelection`、`PublishCommittedPolicySnapshot` 在本次源码盘点中未发现贯通生产协调器的调用，现有调用主要为定义/测试；下一实现者须在最终基线再次确认。`SetSiteProxy`、Xray/service 生命周期、托管注册/配置/租约/探测、真实 egress 与字节/额度/公平限制尚未形成产品闭环。WebSocket、preconnect、BFCache、prerender、通用 prefetch、Service Worker update checks 尚未有完整覆盖证据。

## 已有验证与明确的空白

| 证据层 | 2026-09-20 已核验结果 | 不能推出的结论 |
| --- | --- | --- |
| 仓库 hosted 基础门 | `develop` push [run 35498897269](https://github.com/quinn521/aegis-browser/actions/runs/35498897269) 对 `287aa54` 的 `quality`/`quality-gate` 最新 attempt 1 为 success；上游 `main` push [run 35488106998](https://github.com/gcsagroup/aegis-browser/actions/runs/35488106998) 对 `1f1867b` 最新 attempt 1 为 success。 | 不证明本文档新 HEAD、Chromium runtime、服务链路或 G0。 |
| PR 修复门 | [Fork PR #132](https://github.com/quinn521/aegis-browser/pull/132) 的 [基础 CI 35489294371](https://github.com/quinn521/aegis-browser/actions/runs/35489294371)、[C++ CI 35489294411](https://github.com/quinn521/aegis-browser/actions/runs/35489294411) 与该 PR 的 Codacy 检查当时通过。本地 `1f1a1c4` 来源的完整仓库质量报告为 `PASS`、`sourceStable=true`、`nativeIntegration=REQUIRED`。 | PR/source 的通过不转给新文档 HEAD；`nativeIntegration=REQUIRED` 不等于固定 Chromium 运行通过。Codacy 单次检查不证明覆盖率上传或别的分支状态。 |
| 固定 Chromium 源码/构建 | 隔离验证使用 Chromium `151.0.7922.77`、基线 `ff37cfca210138f2a40b843b4a8195ab7e4fc7ff`。旧候选重放 149 个 Chromium 顺序补丁并完成 overlay 对齐与 GN 生成；其 `-j4 unit_tests browser_tests` 构建最终退出码为 `1`。第一处真实失败为 `components/aegis_access/access_identity_generation_state.h` 的 Chromium style 错误：复杂构造函数不能 inline default，复杂类需要显式 out-of-line 析构。 | 旧候选早于 `0150`/`0151`，不能证明 PR #135 的最终 native stop-line。GN 成功和旧候选的局部对象也不能转成最终候选 PASS。 |
| 当前修复与重跑边界 | `0151-fix-aegis-identity-generation-state-style.patch` 保持不变；针对 PRO 的两个 P1，`0150-feat-aegis-add-access-service-coordinator-lifecycle.patch` 已重新生成，当前 SHA-256 为 `1a247c98930e0c007a52f0f5ee8ac4cc63c571e3145b982282930458dda5163f`。同一固定 Chromium 工作区重放后当前 patched tree 为 `49d14970dd081ee970d27c3f0f06cbabd57683e7`，V8 HEAD 仍为 `a1efc5f9eacac344a49a6b45cf321684b9556b8c`。仓库针对性 `diff --check`、GN wiring、patch format 与 Access native 834 checks 已通过。 | 这些针对性结果不替代最终 HEAD full quality、hosted required CI、PRO re-review，也不替代固定 Chromium 的实际 build/runtime；仍需在最终产品 HEAD 重新绑定 patch hash/patched tree 后构建并运行 coordinator unit、双 Profile 主导航隔离及全部列明真实入口回归。 |

状态登记：代码/合同有局部实现；旧固定 Chromium 候选编译 **FAIL（exit 1）**，PR #135 最终候选 **尚未完成重跑**；浏览器请求 runtime **无最终候选完成证据**；真实 Xray/受控服务端 **NOT_RUN**；G0 **UNVERIFIED**；G1–G3 **未达到**；签名、上传、分发 **无本访问服务结论**。此前局部原生测试、对象编译或文档预览保留原证明范围，不升级为以上任何门槛。

旧固定 Chromium `-j4 unit_tests browser_tests` 构建对应早于补丁 `0150` 的候选，现已以 exit `1` 结束并释放原构建工作区；它只证明 pre-`0150` 树存在上述 style 编译失败，不能清除 PR #135 的最终 native stop-line。下一次固定候选必须在同一 Chromium 基线应用 PR #135 的最终顺序补丁树（含 `0150`/`0151`），确认 coordinator 单测目标可生成/可运行，并让 `MainNavigationRoutingIsolatedAcrossProfiles` 与导航、重定向、Worker、缺 endpoint、BLOCK/在途取消、frame prefetch、Worker/Service Worker 入口等必需回归在同一候选实际匹配并通过。

## 现有构建的所有权与后续动作

原 `-j4` 构建已结束，权威退出文件记录 `1`，第一处真实失败已如上登记。原工作区可以用于最终候选准备，但开始改动前仍需重新核对 owner 进程、工作树状态、Chromium/V8 基线和可用空间；保留既有日志与无关脏状态，不把旧输出目录直接当作最终候选证据。最终候选应绑定 PR #135 的最终产品 HEAD、完整 patch series、patched Chromium tree、GN args 和实际测试过滤器。

## 下一工作窗口：构建和受控观测两线协调

两线在开工时共同锁定产品 head/tree、Chromium 基线与 patched tree、补丁序列、GN args、测试过滤器和受控配置；最终产物的这些键不同，结果就分别记账，不拼接成一次 PASS。隔离构建工作区只能由现有构建所有者操作；测试线可以独立准备 fixture 与设计，不能重启或改动其进程。当前状态仍是上述日期快照，接手时先只读刷新。

| 工作线 / 责任 | 立即动作 | 本窗口完成/停线判据 |
| --- | --- | --- |
| 固定 Chromium 构建所有者 | 回收既有 `unit_tests`/`browser_tests` 的最终退出码与第一处失败；记录实际执行的 prefetch filter。完成后在同一固定候选上运行导航、重定向、Worker、BLOCK/在途取消、缺失 endpoint、Profile 隔离和真实入口派发用例，辨明仅调用 helper/fixture 的范围。 | 当前全部必需入口回归在同一最终源码/补丁树实际执行 PASS，阻塞失败修复且重跑通过，准确测试名、过滤器、退出码与日志齐全后才解除新增请求入口停线。任一 `FAIL`、`BLOCKED` 或 `NOT_RUN`、或仅通过窄 prefetch 子集时继续暂停；解除停线也不等于 G0。 |
| 受控测试与证据负责人 | 不触碰构建工作区，准备代理与 origin 双端日志、请求关联 ID、可控 endpoint/离线/额度/重启/迟到回调故障注入和有界观察窗口；用同配置健康请求验证日志可观测。 | 按目标和拒绝/BLOCK 生效点核对基线与增量：合成代理自行响应时 origin 可为零，真实转发时以代理出口/连接与关联日志证明没有 DIRECT origin 路径；缺 endpoint 的被拒绝请求不得派发到任一端；合法初始代理请求可先于重定向拒绝发生，被拒绝目标不得新增派发；BLOCK 前在途请求保留历史计数，验证后续无新命中派发和终止时序。超时或无响应本身不算 PASS，凭据不得进入日志；未运行真实服务不得标服务端 PASS。 |
| 实现者与同一独立 Reviewer | 在停线解除后实施下一可评审单元；每项新产品功能同 PR 带可运行 unit 与真实入口 regression，并在最终 HEAD 执行；Reviewer 对最终 HEAD 复审。 | 两类测试缺一或未执行均 `NOT_RUN`/未验收；映射至[追踪表](acceptance-tracker.zh-CN.md)的确切子场景，不能将局部 PASS 提升主行或复用 [Fork PR #129](https://github.com/quinn521/aegis-browser/pull/129)/[上游 PR #18](https://github.com/gcsagroup/aegis-browser/pull/18) 历史一次性源码晋升例外。文档单独改动无须新增产品测试。 |

## macOS CI 分层建设交接（尚未落地）

[开发计划的 L0–L5 建设表](development-plan.zh-CN.md)是实施顺序和目标证据的入口；本页只指定下一负责事项。当前 hosted `quality-gate` 仅要求 `quality`，`nativeIntegration=REQUIRED` 仅作分类；C++ workflow 与 full 的 standalone/wiring 有重复，手动三平台 `chromium-candidate` 的默认 Mac 目标还未把 Access `unit_tests`/`browser_tests` 执行纳入必需门。上述是 2026-09-20 `develop@287aa54` 源码状态，不是计划已上线。此文档 PR 及后续 CI 改造在真正修改检查与指南前，仍跑[现有 full 门](../../development/ci.zh-CN.md)。Mac ARM64 是唯一建设范围；最低支持/当前系统矩阵由产品支持合同确认，其他平台后置。

| 负责人 | 下一可审查单元 | 完成判据 |
| --- | --- | --- |
| CI/质量门实现者 | 先做可信 L2/L3：固定 Chromium/V8 与补丁/overlay/GN，编译并运行 Access GTest 和真实浏览器入口用例；把精确产物与 run/attempt 交给可信汇总门。 | 必需层失败、缺失、取消、未知、stale SHA、零测试匹配的故障 fixture 均使汇总失败；N/A 有可信分类理由。`nativeIntegration=REQUIRED` 不能单独放行。当前入口回归停线仍按上表执行。 |
| Mac 构建资源负责人 | 评估托管 Mac ARM64 的容量、耗时和隔离；不足时才提交可销毁专用 runner 方案。 | 锁定源码/补丁/工具链/SDK/架构/GN 的缓存键与 artifact digest；一次构建供多个独立 Profile/端口分片使用，缓存不复用 PASS。外部 PR 不直接执行于长期自托管或签名环境，候选 receipt 不自证通过。 |
| 测试与验收负责人 | 准备 L3 受控 proxy/origin 观测与无 DIRECT fixture；将 A/PF 机器可读映射设计绑定[131 行台账](acceptance-tracker.zh-CN.md)。 | 先用当前必需入口用例证明可执行和可观测；后续 L4 夜间性能/并发/长跑/故障不替代 PR 必需测试，未知场景继续未核验。 |
| 发布候选负责人 | L2/L3 与汇总稳定、去重/缓存/分片审查完成后，再规划 L4、L5 Mac 候选 App 与安装包验收。 | 当前候选相关 L4 失败阻止晋升/分发；L5 分别记录启动/退出、下载、权限、睡眠/唤醒、网络切换、进程恢复、渠道隔离、Developer ID/Hardened Runtime、公证票据/Gatekeeper、Helper/Xray 完整性、安装/升级/回滚。ad-hoc 仅开发测试；缺真实包或运行记录记 `NOT_RUN`，不由 CI 绿灯推断可分发。 |

CI 去重和分类优化另立 PR，先保持当前 full 门；用必需层故障 fixture 验证新汇总后才调整保护条件。纯文档将来可轻量化，当前没有豁免。优化失败走可审查 revert，保留原 full 路径与证据。每个候选分别记录 B/H/M/S、产物 digest、run/attempt、过滤器、匹配数与首个失败日志；flake 不用重复运行掩盖，必需安全测试不得靠隔离移出门禁后变绿。普通功能锁定版本，上游最新兼容性单独监测。分支与审查治理沿用[DEV CI 指南](../../development/ci.zh-CN.md)，不从本交接推断合并或发布许可。

下一代码单元按[开发计划](development-plan.zh-CN.md)的顺序推进。交接给实现和审查者的最小清单：

1. 在新任务中刷新 `origin/develop` 和 upstream 精确 SHA，选隔离 worktree；记录 base、head、源码树、补丁集合、Chromium 基线、GN args 与所属构建任务。先按上表关闭**当前**固定 Chromium 编译/运行欠账，不能以计划中的窄 prefetch filter 代替全部入口回归；现有失败需先定位和修复。同步复核 `CommitIdentity` / `CommitSelection` / `PublishCommittedPolicySnapshot` 的生产调用空白。
2. 为最小 coordinator 写清可信 host → 普通网站协议组选择（DEV/Alpha 调试三策略独立协调）→ identity/selection/base-proxy 代次 → PREPARED → candidate snapshot publish → execution ACK → durable commit/recover → UI 的接口和版本关系；将 timeout、cancel、restart、signed-out、保存失败及无 DIRECT fallback 列为同单元回归。对应 P1/P2/P4/P5 的局部合同不能伪装成 P0/G1 完成。
3. 以真实 Profile/StoragePartition 和受控 HTTP fixture 执行正常与失败路径；随后接 Xray→REALITY 和托管身份/租约，逐层补服务端计量与限额，再扩展请求矩阵和四渠道 UI。每个新入口列测试名、实际运行结果、未覆盖范围；A01–A118、PF01–PF13 的实现/映射/执行/结果/覆盖分开写入[追踪表](acceptance-tracker.zh-CN.md)。
4. 独立 Review 检查最终 HEAD 的完整实现、overlay/顺序补丁/BUILD 接线、冻结语义、失败回退和测试；按[DEV CI 指南](../../development/ci.zh-CN.md)核对本地最终 HEAD、PR B/H/M、最新 attempt 及合并后 S。旧 run、旧 review 和本交接快照不能代替新候选证据。
5. 每完成一个阶段，在该阶段 PR 内同步更新本开发计划与 Handoff，再让独立 PRO reviewer 对最终 HEAD 复审。依赖上一阶段代码的后续阶段，等上一 PR 实际合并且最新 `develop` push CI 成功后，从该最新 `develop` 新建隔离分支；等待期间只进行只读准备。

只读刷新命令示例（在授权的仓库 checkout 中执行；不触碰正在构建的 Chromium 工作区）：

```bash
git status --short --branch
git rev-parse HEAD origin/develop origin/main upstream/main
git show -s --format='%H %T %P %s' HEAD
git ls-remote --heads origin develop main
git ls-remote --heads upstream main
gh pr view 133 --repo quinn521/aegis-browser --json state,headRefOid,baseRefOid,mergeable,statusCheckRollup
gh run view 35498897269 --repo quinn521/aegis-browser --json headSha,event,status,conclusion,attempt,jobs
gh run view 35488106998 --repo gcsagroup/aegis-browser --json headSha,event,status,conclusion,attempt,jobs
```

若 GitHub CLI 对字段或权限报错，使用同一 repo 的只读 API 查询，记录无法核验项；不要把缺失结果当成功。最终报告分别填写代码/PR、仓库 hosted CI、固定 Chromium build/runtime、真实服务/计量、G0–G3 和分发状态，每项附精确 SHA、run/attempt 或原始日志。本文档变动对应的 `develop` PR 只提交供审查，不在本轮自动合并。
