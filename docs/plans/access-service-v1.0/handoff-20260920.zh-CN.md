# Aegis 访问服务开发交接（2026-09-24 W0 续接）

文件名保留以兼容链接。当前执行入口为 [QUALITY-HANDOFF](QUALITY-HANDOFF.md)，计划见 [W0–W6](development-plan.zh-CN.md)，逐项判定见 [A/PF 台账](acceptance-tracker.zh-CN.md)，冻结行为仍由[修订 4](spec.zh-CN.md)决定。

## 当前事实与接续

Q 冻结起点 `origin/develop@7f74e0a4e971f91d08d90536e429aba7b82cf37d`；`origin/main == upstream/main == c1399852270c1bbce612dbea1558266d6fc57162`。#162 已合并，最终 H=`4670c4dd5f24db61f38f102cc60475658e63554a`，S=`ca4e1b24c750746d6e20a83fa58f1d2500791d02`。历史最终 H 的选定 unit 96 tests 与三组 browser fixture 有通过记录，但 native 总体是 `PARTIAL_PASS`，不能转用于新 H 或称 W0/G0 通过。

Q 拥有新的固定 Chromium source/out/锁/证据以及 plan、本文、tracker 的状态更新；F 并行准备 W1a，不操作 Q 构建现场。#166 保留代码，后续模型路由工作 DEFERRED。上游 #23 的另一个 Ninja 因主机重启中断，原报告/双锁保留为 INTERRUPTED_UNKNOWN；协调者已取得原 owner 的明确时段交接，Q 启动前仍须复查实际资源。当前先完成 17 unit targets、38 个 Access browser 源码定义的最小矩阵和 LoadingPredictor 真实入口测试设施；全部当前执行结果与准确目录见 QUALITY-HANDOFF。

**W0 未闭环，G0 UNVERIFIED，G1–G3 未达到。** 缺口分为测试源码、当前候选执行证据、资源占用；不能用前两项中的任一项掩盖另一项。W2 尚无受控服务端资源，本轮不部署服务、不调用付费 API、不发布安装包。新功能 W1b/W1c 必须满足计划中的前置条件。

交付先冻结最终 H，逐项运行 native 与 browser 并记录非零枚举、退出码、二进制哈希、源码稳定性，继而核验本地 full、独立 review 和精确托管 B/H/M/attempt；合并后的 S 属于另一个证据身份。历史 Auto 描述不构成本轮无条件合并或晋升授权，交协调者按当前真实授权和门槛处理。

当前第一候选 H=`cf2329a47e3e35e5e4f89f76767750758ab703f5` 的 local full、source admission 和托管 PR CI 已通过，但 native attempt 1 在 81 项通过后遇 adapter 测试启动崩溃；browser attempt 1 实际枚举 41 项，在 1 项通过后遇导航 favicon 计数污染。两次整体均 FAIL/sourceStable=true。Q 保留原始失败，修复测试启动和观测设施；后续新 H 必须重新执行，不转用第一候选绿灯。具体 B/H/M、run/attempt 和目录见 QUALITY-HANDOFF。

## 历史身份（不再是当前执行板）

下列旧记录只为追溯：2026-09-22 06:15 UTC 的 #164 基线 S=`c08b6321fe2878ba62c8d9757a3f4ae4e4f17470`；当时 #162 OPEN/Draft，H=`682997a6974fe76966e82276d7611ba59da30c42`，Coordinator BUILDING、测试 NOT_RUN，`candidate-v2/src/out/Pr129Verification` 使用 `use_lld=false` 变体。该观察已被后续 H13 最终报告和合并事实取代，不接续旧进程、不恢复旧编号、不把旧 M/CI 写到新 H。

完整旧表、命令与原始时间点保留在[本轮起点的历史版本](https://github.com/quinn521/aegis-browser/blob/7f74e0a4e971f91d08d90536e429aba7b82cf37d/docs/plans/access-service-v1.0/handoff-20260920.zh-CN.md)。H12 的 `6664528…` 快照保留在计划的历史段；最终 H13 原始报告路径、hash 与矩阵集中于 QUALITY-HANDOFF。W1/W2 架构设计仍引用[架构复核](architecture-review-20260922.zh-CN.md)，其中旧 PR 状态也只能作为当时观察。

Q 第二候选 H745bf3c 的 adapter 5 项已实际通过；native 总计 11 targets/86 tests 后在 dispatch_state 测试裸指针编译检查失败，browser 在 favicon helper 的固定 GURL API 编译失败（0 runtime）。两者 sourceStable=true，local/full hosted CI 成功不能冲抵这些失败；继续修复并在新 H 重验，详见 QUALITY-HANDOFF 第二候选归档。

Q 第三候选 Hceb6ead：设施编译修复已过，但 transport 尾点 host 接纳及 frame prefetch 缺 endpoint 直连暴露产品缺陷。NetLog 已证实后者 DIRECT/HTTP200；native 总门和 browser 总门均 FAIL/sourceStable。尾点按保守拒绝设计修复，保无 snapshot 的 native 边界；prefetch 另定位实际入口。局部通过与剩余目标补充诊断不升级 W0/G0 或131主行，详见 QUALITY-HANDOFF 第三候选记录。

后续 0175–0186 已修 PAC/尾点、预提交 document factory、sandbox opaque 归属、bundle 特殊 factory 和非 HTTP 受限 default；定向对象编译与部分 browser 用例有局部通过，但最终 H 尚未取得完整 native/browser 运行。独立预审在 Hc6f14c2 发现 HTTP URL 的 MHTML 子帧原禁网 default 仍可能被 prefetch 重建为可联网 factory，另指出 pending Clone 测试同步 Flush 会挂起；后一测试设施已在 H5fd9a25 修正，前一安全边界仍待可信资格载体和真实回归。各 H、sourceStable、失败/中断回执见 QUALITY-HANDOFF；#177 继续 Draft，W0/G0 和 131 主行不升级。
