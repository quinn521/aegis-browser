# 当前页总结延迟：诊断与修复进度

状态：局部改善已实测，全量验收未完成。三组修复已构建、通过原生回归并更新固定验收App；相同9B模型对cnBeta首页两轮总结22.739秒和17.855秒。但中文输出及分条列要点不稳定，不能称总结体验已达标，也不能将两次结果当作所有页面的性能保证。

## 实际任务记录

任务 `e00b4294-9bf2-4f0c-9910-ff6d57ac1218`，目标“总结当前页面内容，并列出重点”，来源为用户截图中的cnBeta页面。模型为 `Huihui-Qwen3.5-9B-abliterated-mlx-4bit`，本地端点 `http://127.0.0.1:8000/v1`。

| 事件 | 本地时间 | 与上一事件间隔 |
| --- | --- | --- |
| 开始理解 | 02:43:29 | — |
| 计划准备好、开始执行 | 02:43:55 | 26秒，包含截图可见的一次格式修复 |
| page.observe 核验成功 | 02:44:08 | 13秒 |
| page.extract 核验成功 | 02:48:22 | 4分14秒 |
| 任务记录进入已完成 | 02:49:18 | 56秒 |

总计349秒，模型调用计数7、工具调用计数2。时间来自原生任务记录与用户截图；中间间隔不是工具本身耗时，包含等待模型等阶段。未取得本轮逐请求服务日志，不将4分14秒武断归因为一次超时或全部用于思考。前一次相同目标任务的记录耗时207秒、模型调用计数6，说明不是一次孤立的慢响应。

## 已确认问题

1. 简单页面摘要经过模型规划、格式修复、工具参数生成和最终总结，串行轮次过多。代码进一步确认：明确“当前页”的入口直接绑定页面，不调用额外目标路由；不能将该任务的7次计数解释成包含一次目标路由。
2. `ShouldDisableLocalQwenThinking` 原来只匹配以 `qwen` 开头的名称，漏掉带发布者前缀的实际模型。已检查本机oMLX源代码：Responses端点接收并转交 `chat_template_kwargs`；单独的 `reasoning.effort=none` 只转发为模板参数，不自动等同于 `enable_thinking=false`。是否受模型独立强制设置影响仍需真实请求核验。
3. 原来短工具参数也使用8192输出token上限；本地每轮最多等3分钟，执行请求非流式。上限不是实际消耗统计，但使无效长输出和长等待缺少针对性的约束。

## 本轮已实施

- 增加带词边界的Qwen名称识别，覆盖 `Huihui-Qwen`、仓库命名空间、下划线和大小写；拒绝 `notqwen`、`qwenish` 等误匹配。保留本地OpenAI兼容端点前提，不把扩展字段发送给云端或其他协议。
- 目标路由、观察和提取的短参数请求使用1024输出token上限。复杂计划、其他动作和最终产物保留原上限，未用全局缩短超时冒充优化。
- 新增原生核心用例覆盖14个模型名及9个工具名边界，并新增实际服务请求载荷测试，检查前缀Qwen是否发送关闭思考参数。
- 正式构建 `aegis_agent_core_unittests` 成功，161项测试全部单次通过，无失败或重试；执行结果位于 `.artifacts/v2-final/summary-latency-r1/core-tests.json`。服务实现对象已编译通过。
- 原R19核心测试程序已保留为 `.artifacts/v2-final/summary-latency-r1/R19-core.original`。固定App、签名身份和验收资料未改动。
- 对实际本地Huihui-Qwen发送合成提取参数请求，使用新参数：HTTP200，4.119秒，返回正确的原生 `page.extract` 调用；输入371token、输出53token、报告思考token为0。这只是端点兼容性检查，不访问真实页面、不证明整页摘要耗时，也不是与旧配置的受控性能对比。测试时另有链接进程运行。
- 浏览器服务回归183项已单次全部通过，新增实际请求用例存在且通过，覆盖五个名称的请求载荷；结果位于 `browser-unit-tests.json`。App构建也已成功，但未替换固定入口。
- 中断后核对确认此前的浏览器单元测试链接因系统文件句柄耗尽失败，失败日志保留为 `browser-unit-link-failed-r1.log`。本机验收的 `chrome/browser/aegis/BUILD.gn` 额外限制链接并行，未改产品源码目录里的构建文件或系统限制。第一次直接设置的ThinLTO并行参数被默认配置覆盖；该次构建和183项测试实际成功，但不能据此声称ThinLTO已单路执行。已改为引用末尾链接配置，后续另行验证最终参数和重链接产物。
- 最终生成的链接命令已断言确认末尾 `--thinlto-jobs=1`、`--threads=1`，随后 `chrome aegis_browser_unittests` 构建成功。该设置只影响本机验收构建的并行度，没有改代码优化等级。
- 对最终单路链接产物再次执行完整183项浏览器服务回归，全部单次通过；单独记录为 `browser-unit-tests-serial.json`，不覆盖前一产物的测试结果，也不作为同一失败测试的重试。

## 重复正文压缩：源码与输入验证

- 最新网页回执原来同时出现在 `previous_browser_result_untrusted_json` 与历史证据的正文/提取字段中。仅在完整回执不超过12KiB，且动作编号、版本、状态、错误、消息、值和核验证据全部相同的情况下，历史条目改为引用完整回执；文档标识、来源和截断标记不删除。
- 历史保留预算仍按压缩前的大小计算，不让去重改变原来选中的历史证据。总请求超限需要移除完整回执时，先恢复历史正文；长回执、不同文档、不同核验结果、不匹配编号和最终翻译阶段保留原表示。
- 新增4项核心测试首轮通过，全套165项单次通过，记录为 `core-tests-deduplicated.json`。随后补充历史预算不变的第5项测试，再次正式构建核心测试程序，最终166项全部单次通过，记录为 `core-tests-deduplicated-final.json`；没有覆盖首轮结果。
- 单独运行代表性参数阶段与完成阶段输入测试：参数阶段13756→7522字节，完成阶段13685→7451字节；`page.observe` 与 `page.extract` 两种回执均覆盖。原始 `value` 和 `evidence` 的逐字段一致性断言通过。数据记录在 `prompt-size-sample.xml`。
- 上述数字是同一构建中，仅改变一个同长度动作编号来开关去重的输入对照；约45%的字节下降不是token下降或实际响应时间对比。
- 浏览器完整构建成功，随后183项浏览器服务回归全部单次通过，记录为 `browser-unit-tests-deduplicated.json`。本组共166+183=349项原生回归通过；源码与验收目录中的实现、测试文件逐字节一致。固定App未更新，未提交、推送或部署。

## 首步读取减少一轮模型请求：已构建并通过回归

- 模型仍先理解目标并提交计划。仅当已同意任务的首步是 `page.observe`，全部授权标签及任务自建标签合起来恰好一个，且无旧回执、刷新待办或重试时，由浏览器直接填入标签编号。该工具的现有原生实现不使用可选 `query`，因此不需要再调用模型决定一个已确定的编号。
- 原生动作仍经过同一绑定、权限校验、预算、持久化和结果核验链；不直接推进步骤，不伪造模型调用。多标签、后续步骤、重试、恢复后重新读取，以及 `page.extract` 的种类/字段选择不走此优化。
- 增加原生浏览器测试：实际宿主规划请求路径、未同意时不读取、目标标签不是当前活动标签、单标签省一轮、多标签仍请求模型。模型响应由测试模拟器提供，页面读取由真实浏览器完成，不作为本地Qwen实测。相应监控、慢页面、摘要失败及翻译预算测试同步调整。翻译场景加入规划请求以保留原预算边界，不靠扩大预算通过测试。
- 首轮完整App、浏览器测试及服务单元测试构建成功。核心166项、服务183项分别单次通过，记录为 `core-tests-native-observe.json`、`browser-unit-tests-native-observe.json`。
- 新增原生浏览器用例单次通过，记录为 `native-observe-focused.json`：实际原生读取成功、来源及文档绑定正确、未经同意不读取；单标签场景规划加总结共2次模型请求，多标签仍为3次。模型响应仍由模拟器提供，不能据此声称Qwen摘要已经达到指定耗时。
- 首次整组原生浏览器回归没有全过：`CurrentPageRejectsInternalTargetsAndStaleInvocation` 在新建 `chrome://bookmarks/` 标签尚未提交导航时比较网址失败，实际为空。该测试只调用创建任务入口，尚未执行本次首步优化。新增首步用例、慢页面、摘要失败回退、全部翻译场景和其他测试通过，原始失败结果完整保留在 `browser-tests-native-observe.json`。
- 已补充等待目标标签导航完成，并在测试内部页面拒绝前先断言网址；重新构建后整组再跑，最终73项普通浏览器测试全部单次通过，另一个冷启动 `PRE_` 前置阶段也通过。启动器共报告74个执行阶段，JSON记录73个普通测试结果。最终结果另存为 `browser-tests-native-observe-final.json`，不覆盖首轮失败。
- 旧浏览器测试程序已保留为 `before-native-observe-browser-tests.original`。更新包已使用同一个个人开发证书签名，严格验证通过，指定签名要求与固定App一致；独立验收资料已另存副本。此时固定验收App尚未替换。

## 固定App与本地Qwen实测

- 确认固定App已退出后，04:31将更新包放回同一入口 `/Users/lazy/Applications/GCSA Aegis Test.app`。沿用个人开发签名及独立资料目录，严格签名验证再次通过；未改系统授权、模型设置或端点。
- 旧包完整保留为 `.artifacts/v2-final/summary-latency-r1/fixed-app-before-performance.app`；独立资料副本为同目录 `acceptance-profile-before-performance`，没有删除原资料。
- 使用Computer Use实际打开侧栏、点击“总结当前页”和“开始任务”。模型仍为 `Huihui-Qwen3.5-9B-abliterated-mlx-4bit`，目标 `https://www.cnbeta.com.tw/`，目标文本与此前耗时任务一致。
- 第一轮界面时间线：04:35:07开始，04:35:12计划准备好并执行，04:35:30核验完成并显示总结及来源，按秒级显示计23秒。实际截图检查了总结、来源和时间线。结果是一段摘要，没有按目标单独列出要点，不能将结果格式视为完整达标。
- 相比此前349秒的观测有明显改善，但不是固定页面快照、冷暖缓存和系统负载一致的受控对照，不报告普适倍数或百分比。页面内容会更新；没有将页面新闻的真实性作为此次性能验收范围。
- 在线只读查询任务数据库遇到独占锁，未绕过锁或强行复制活动数据库；待本轮任务结束后正常退出App再读取调用统计。
- 第二轮相同页面、模型和目标：04:38:40开始，04:38:42计划准备好并执行，04:38:57核验完成。实际返回英文单段摘要，与中文任务不一致，且仍未明确分条列出重点。该问题保持未修复，不因任务内部状态“已完成”而认定用户目标完整达成。
- 两轮完成后通过应用菜单正常退出，再以只读方式查询落盘记录：第一轮 `6e7aaaca-8f16-4fcf-aa3e-853400400126` 耗时22.739秒；第二轮 `1f744af6-1a99-4b96-891c-ae26a34d1ed3` 耗时17.855秒。两轮各3次模型、2次工具、3次网络计数，`page.observe` 和 `page.extract` 均有成功核验回执。统计另存 `real-qwen-summary-measurements.json`。App已正常退出，不遗留进行中的测试任务。
- 首轮和第二轮均未出现规划格式修复；旧任务有修复和额外调用。因此实际7→3次调用差异不能全部归因于首步观察省掉的1次调用。

## 输出语言与格式：源码和构建完成，实机验收待解锁

- 实机两轮发现中文任务偶发英文输出，且未按“列出重点”分条交付。原有系统约束虽要求跟随用户语言，但完成工具描述仍是英文“final summary”，最终交付请求没有单独重申格式要求；这是已发现的约束薄弱处，不把模型随机输出的唯一原因武断归结于此。
- 修改完成工具及其 `summary` 字段说明，明确这是直接展示给用户的结果，不是给执行器看的英文工作摘要；在完成阶段添加原始目标优先的语言和格式要求。明确指定的输出/翻译语言优先，否则跟随用户请求；需要重点时逐条列出，其他格式按原要求，不强制所有任务使用中文或列表，不为凑数编造事实。
- 工具参数阶段不加入该段约束，不新增模型调用，不改权限、来源或文档核验。完整翻译与非列表任务的要求保留。
- 新增2项核心测试，覆盖完成工具两种模式、中文列表、指定英文及数量、英文单段非列表、指定日文翻译，以及首次与纠正请求。168项核心测试全部单次通过，结果为 `core-tests-output-contract.json`；源码与验收副本逐字节一致。
- 浏览器App及服务测试程序重新构建成功；183项服务测试全部单次通过，结果为 `browser-unit-tests-output-contract.json`。本组168+183=351项原生回归通过；未将上一构建的浏览器测试重复计入本组。
- 侧栏已有 `white-space: pre-wrap`，可保留结果换行，无需额外改UI。模型是否按语言和格式要求交付，仍须真实输入检查，单元测试不能代替这一点。
- 确认固定App未运行后，沿用同一签名和独立资料目录更新固定入口。指定签名要求与上一包完全一致，更新前后严格签名验证均通过。上一包保留为 `fixed-app-before-output-contract.app`，资料副本为 `acceptance-profile-before-output-contract`，都位于本报告证据目录，没有删除旧包或资料。
- 随后Computer Use尝试打开固定App时返回“Mac已锁定，自动解锁失败，需要用户手动解锁”。没有尝试绕过锁屏或修改权限；本版尚未完成实机Qwen验收。上一版22.739秒、17.855秒测量不能移用于本次新包的结果语言/格式验收。

## 锁屏期间补充回归：监控日志冲突已修复并通过原生回归

- Computer Use再次确认Mac仍锁定，没有尝试解锁或修改权限。独立原生测试可继续执行；新增87项通用回归全部单次通过，结果为 `common-tests-output-contract.json`。
- 最新输出约束对应的浏览器测试程序构建成功，整组回归记录为 `browser-tests-output-contract.json`：73项普通测试中72项通过、1项失败，冷启动前置阶段另行通过。失败用例是 `RecoveryNotifiesOnceAndDoesNotExposeTheTargetPath`；两次通知及不泄漏私密路径的断言通过，最后 `RemoveMonitor` 返回false。不能将整组记录为通过。
- 同一用例显式诊断重复5次均通过，记录为 `monitor-removal-diagnostic.json`；这些重复不覆盖或抵消整组的失败。
- 代码检查发现URL检查和网页检查都使用固定的 `monitor:<monitor_id>` 写入每次变化日志，而日志表以任务/动作编号为唯一键；第二次插入被忽略且返回false，异步回调会将其视为关键存储失败并停用Agent。原测试没有等待落盘，可能在失败回调到达前结束。已先补充等待存储队列完成与服务仍启用的断言，正在构建用于稳定复现；尚未改产品逻辑。
- 仅增加等待和断言的测试重新构建后，单次运行明确失败：`IsEnabled()`为false、`RemoveMonitor()`为false，证据为 `monitor-log-flush-red.json`。确认不是通知显示或锁屏造成的断言错误，而是实际异步写入触发停用。
- 随后才修改产品：URL及网页监控的审计动作编号加入现有每轮检查的随机 `request_id`，在释放检查对象前复制编号；通知仍使用稳定的监控编号，权限、脱敏内容、重复通知判断和真正存储失败时停止执行的保护均不放宽。
- 新增连续两次网页变化的浏览器用例，逐次等待日志落盘，核对服务可用、两次通知、实际读取/模型调用计数，以及仍可暂停和删除。完整App、浏览器测试和服务测试正在重建；固定入口暂未安装这项日志修复。
- 完整App、服务和浏览器测试程序构建成功；168项核心、87项通用、183项服务回归分别全部单次通过，文件名为 `core-tests-monitor-log.json`、`common-tests-monitor-log.json`、`browser-unit-tests-monitor-log.json`。两项定向浏览器用例均单次通过，记录为 `monitor-log-focused-green.json`，修复前失败记录保留。
- 正在重跑整组原生浏览器回归；模型响应仍是测试夹具，系统通知由测试接收器核验，不代替固定App、本地Qwen或真实系统通知显示验证。
- 修复后整组浏览器回归全部单次通过，结果为 `browser-tests-monitor-log-green.json`：74项普通测试，另有1个冷启动前置阶段，启动器共报告75个执行阶段。和168项核心、87项通用、183项服务合计512项原生测试，定向用例不重复计入总数；最初整组失败、诊断重复和修复前失败记录均保留。
- 确认固定App仍未运行后，将同一开发证书签名的新包更新至 `/Users/lazy/Applications/GCSA Aegis Test.app`，严格签名验证再次通过。前一包保留为 `fixed-app-before-monitor-log.app`，资料副本为 `acceptance-profile-before-monitor-log`；沿用独立资料目录，没有删除旧包、修改系统授权或提交发布。
- 本次未再尝试锁屏下的UI操作。固定App包含监控日志修复，但该包的真实Qwen语言/格式、用户界面操作和系统通知显示仍需用户手动解锁后验证。

## 原始验收清单复核与Android源码同步

- 再次核对R19原始验收清单发现，前述 `Aegis*BrowserTest.*` 过滤器没有覆盖15项设置与DevTools安全测试。512项通过的计数真实，但并非原清单的全部覆盖，不能据此放行完整验收。
- 使用相同最新浏览器测试程序，按原清单补跑这15个精确名称，全部单次通过，结果为 `browser-tests-original-checklist-extra.json`。逐项验证四类结果的并集无重复且完整包含原清单，当前共168项核心、87项通用、183项服务和89项普通浏览器测试，合计527项，另有1个冷启动前置阶段；后续应按名称清单验证，不再只凭类名后缀过滤。
- Android专用容器当前停止，无活动构建、无新版APK。旧源码逐文件仍匹配R15冻结输入，17项原有删除保留。当前macOS与Android有10个清单内产品/测试文件差异，另新增的 `agent_model_protocol.h` 不在旧53文件清单内；已将其纳入新候选，避免只同步实现而遗漏声明。
- 在专用容器停止且没有其他运行容器的条件下，先归档54份旧源码并逐文件验证可恢复，再通过补丁同步11个差异文件。同步后54份源码均匹配候选哈希，17项原删除及Android GN参数不变，`android_static_analysis = "on"` 保留；macOS专用链接参数未复制。
- 新候选和前后归档位于 `android-current-source-r20/`，清单摘要 `635434f4d2447361ef52174d31812cf8fecfd58eb43077b1031529d4d8ab053b`。此处只证明源码同步；尚未启动本候选Android构建、生成新APK、安装到Pixel或通过真机验收。Mac仍锁屏，未借Android源码检查替代真实Qwen验证。

## 全量剩余验收

首步读取已减少一轮模型请求，固定App已更新且两轮真实本地模型计时已完成。中文输出与要点格式约束已修复并构建，下一步需在最新固定包上验证实际模型遵循情况；流式显示、更多页面的重复性能测量及完全一致页面/缓存/负载的前后对照尚未完成。本轮不更新全量通过状态，不将源码测试当作用户实测。

短流程仍必须保持先理解用户目标、浏览器构建范围、可见计划、逐步权限检查、文档绑定、内容安全和结果核验。不得为提速退回无浏览器证据的聊天摘要，或取消复杂任务所需的统一Runtime。

完整M0–M7目标和三平台全部十项验收保持未完成；macOS单项修复不代替Android、Windows、自动化、隐私和反钓鱼真实行为证据。

## Android本轮构建入口：验证完成，正式编译未启动

- 为54文件当前候选准备了 `build-android-current-r20.mjs`，复用已验证的资源检查、供电/时钟检查、源码冻结和精确进程组中断逻辑，保留静态分析。只允许诊断构建，不安装设备、不解锁、不改变系统授权。
- 193项构建安全自测通过，其中43项以真实Shell控制流和模拟发送器检查中断身份边界，未发送真实信号。重新核验当前527项原生结果及对应程序哈希；不同测试程序的同名用例按程序区分，浏览器补跑与主组保持同一组去重。初次自测的两处统计规则错误已修正，记录保留在 `android-current-source-r20/build-preflight-validation.json`。
- 只读预检曾通过：磁盘余量195482976256字节、可用句柄479836、虚拟机18核及25159270400字节内存，无活动本地大构建。首次正式启动检查发现新出现的其他容器，在写入构建目录和启动专用容器之前拒绝。
- 后续只读检查确认那个短时容器已自然退出后再次尝试。专用容器启动成功，但随后再次出现其他任务的容器，检查在启动编译器前退出。原始记录在 `android-current-build-r20/status.json`：`phase=container-start`、`build_started=false`、`source_unchanged=true`、`apk_sha256=null`，没有构建退出成功或新APK证据。
- Docker只读事件检查显示，45秒内其他任务创建了36个短时容器，最后仍在连续启动；不能把一次空闲快照当作稳定独占构建窗口。没有停止、修改或按名称放行这些容器。仅在核对本轮专用容器只有PID 1的空闲等待和检查进程后，将由本轮启动的专用容器正常停止，恢复为停止状态。
- 本轮编译启动失败记录及全部源码归档保留，不覆盖重试。后续需要空闲构建窗口；最新固定App的真实Qwen与系统通知仍待用户手动解锁Mac，Windows人工告警确认边界也未取消。全部十项验收仍未完成。
