# Aegis Browser Agent v2 跨平台验收记录（2026-09-05）

## 结论

本轮仍是部分通过，尚未满足全部十项完成标准。不能据此发布，也不能将旧 Android
安装包或 Windows 单元测试替代最新候选的真实界面验收。iOS 按已确认范围跳过。

**2026-09-09 13:34 UTC（本地21:34）目标受阻，等待用户处理系统提示**：
第三次连续核对同一R19进程2307、SecurityAgent2318及真实界面，仍停在about:blank，
夹具日志没有新App请求。可独立进行的原生回归、Android安全自测和环境前置已完成，
当前没有新授权或安全替代路径可推进实际验收。目标标记受阻，不代表完成；保留同一
签名App、资料和失败证据，不重复启动构建、不重签、不操作密码或权限。用户手动
检查处理钥匙串提示后，再恢复同一候选的Qwen界面测试。未变化的自动检查应保持安静。

**2026-09-09 13:32 UTC（本地21:32）等待系统确认，Android只读前置完成**：
同一R19进程2307仍存活，界面仍为about:blank加载态，夹具日志仍止于宿主探测121；
没有新的App页面请求或翻译结果，不重启或重签。填入真实原生摘要后，构建安全
自测161项通过（固定摘要已就绪分支比占位符分支少1项），原146项安全用例和
43种模拟Shell分支保留，真实信号0次。环境8项与共存22/35/33项也通过。
prepare与build两条preflight均实际执行结束0：53份源核对、516项结果核验通过，
没有并行构建；磁盘约209GB、句柄余量298330、虚拟机18核约25GB、接电开盖且
时钟一致。prepare的execution_ready=false、build的ready=false，原因仍是缺少
当前候选真实Qwen通过收据且未暂存。没有写源码、改容器、启动构建或安装设备。
下一步需要用户手动检查并处理当前App的钥匙串提示，再继续相同签名包的真实验收。

**2026-09-09 13:29 UTC（本地21:29）R19完整回归通过，真实界面待手动检查钥匙串**：
本次服务入口修复及11组新增场景已真实编译，13:11:42至13:19:08构建退出0。
13:24:41完成四套516项单次回归、无重试：159核心、87通用、182浏览器单元、
88浏览器，原始摘要515条加成功PRE原生日志。关键翻译测试28组场景全部通过，
包括正确partial候选进入独立复核、语义拒绝、缺段/重复/未知编号、多译、模型和
网络预算不足、确有未完成项；scope与预算未扩大，复核前没有可见完成结果。
候选清单`8a922e7e1c4f72a3e0edcd15bf15dd2696abc0d2fca73f62c80729e0e95cd12c`，
构建摘要`a432c1b470bbdec02bf55f913b037ad988bdb50fed693f5543827154dc8b71e8`，
测试摘要`ad19df24fc8fd100a7bd2cf5dbedf096b99335eb34d12c6ab0d4b91f8e3c45b6`。

旧R18已正常菜单退出，会话26375退出0，原资料完整保留。R19独立签名App于
13:25:17启动，会话77585、PID2307，原生入口与首次引导可见，无密钥本地Qwen
连接保存成功。但导航合成article仍停在about:blank加载态；宿主HTTP检查200、
约0.0014秒，夹具日志121仅对应宿主检查，不是App成功请求。浏览器只读采样
显示异步安全存储线程停在KeychainPassword/GetPassword及SecItemCopyMatching
路径，系统SecurityAgent存活。没有直接查看系统弹窗内容，不能假称已看见某按钮。
需要用户手动检查并处理系统钥匙串提示；没有读取、保存或代填密码，没有更改授权。
必须保留当前同一签名包和资料再继续，不能用重签名或模拟钥匙串绕过。

已通过界面保存恢复8000并重新展开确认，诊断进程2380精准SIGINT结束，会话12912
真实退出0、日志0条。保留当前App供用户操作，两句翻译尚未提交，不记通过。
详情`macos-page-evidence-ui-r19/startup-keychain-wait-r1.json`。
Android准备30项、构建162项及环境/共存安全自测通过，53份真实源核对确认5份
待同步、旧17处删除保留；未暂存或构建。构建/原生摘要已固定，Qwen收据门仍关闭。
Windows安全告警、Pixel隔离调试授权和全部十项验收缺口不变，不能发布。

**2026-09-09 13:10 UTC（本地21:10）R18全文通过，主标题完成状态仍失败**：
真实App全文原句21:02:22至21:02:35完成，七段完整含“独立”、18小时、24个月及
测试免责声明；范围选择、译文和独立语义复核各一次，严格三个true与空issues。
主标题原句21:03:53至21:04:09为部分完成：范围正确选编号2、仅该段翻译，
其余六段均按用户要求排除，但模型返回partial且未完成项为空，宿主因此未进入复核。
不是前一版本的标题角色丢失，不能将结构正确直接记为界面通过。
失败收据`macos-page-evidence-ui-r18/translation-scoped-ui-r1.json`摘要
`d05de0aa65839eff3937510ce78fbcc15bf660e2a5e3a906e68752eee9abee26`；
原始诊断摘要`83db7b2c4bdd72ba629102032b472a9913840c6e63c540afe5e6327eaa340638`。
两次均通过真实界面与截图核对；服务地址已保存恢复8000，诊断进程66873正常
SIGINT退出、会话89794退出0，没有读取系统密码或更改权限。

当前仅改服务入口及浏览器回归两份源码：无未完成项且已独立选区的partial先作为
待复核候选，仍须原文/编号/选区完整绑定及独立语义复核通过后才允许完成；有真实
未完成项不升级。新增11组完成状态、缺段、重复/未知编号、多译、语义拒绝和预算
反例，仍待新候选构建与实际原生测试。Android未暂存、没有新构建，不能用失败收据放行。

**2026-09-09 13:02 UTC（本地21:02）R18完整原生回归通过，真实界面继续**：
构建12:42:47至12:49:31真实退出0，四套测试至12:54:30实际516项全部单次通过，
无重试；159核心、87通用、182浏览器单元、88浏览器（摘要515条，PRE原生日志成功）。
新构建摘要`a0e1478c0a7b00c5fe35c9828a9cec417152617629bbaaa3f5566a549e419e63`，
新测试摘要`b8826ed02aa66b293fbd2b9d0c29f644801600e2adcb14c13d604231a1583ffc`。
新增标题元数据保留、旧语义检查、选区绑定均真实通过，不是仅查看历史结果。

R17通过原生菜单正常退出，会话56653退出0、PID35875消失，旧资料及失败记录保留。
新R18隔离App于12:55:14启动，会话26375、PID66776，整包与启动参数记录在
`macos-page-evidence-ui-r18/app-identity.json`；复核原生证据后才复制签名启动。
用户询问密码仅留本机的方案时暂停了操作，未读取、保存、填写系统密码或点击授权。
13:01只读重新核对App界面后保存了不含密钥的本地Qwen连接，21:02:22已通过真实
界面提交全文翻译原句，尚未核对最终结果；不得提前将两句翻译或500书签判通过。

Android新候选宿主准备/构建脚本已完成只读自测：准备26项、构建162项（含146项
原安全用例、43种模拟Shell分支）、环境8项、共存保护22/35/33项均通过，无真实信号。
源身份实际核对53份、仅4份待同步、17处原删除保留；专用容器只读空闲检查通过。
macOS构建与测试固定摘要已填写，真实Qwen收据仍待完成，因此未暂存Android源码、
未新开Android构建、未安装APK。原R15增量文件保持不变，Windows与Pixel授权边界不变。

**2026-09-09 12:44 UTC（本地20:44）翻译范围复核修复进入构建**：同一R17独立App
真实执行两句原目标。全文翻译20:29:15至20:29:27显示已完成、七段均输出，但标签
标题译文省去“独立”，不能断言完整忠实；仅正文主标题20:32:59至20:33:38返回
部分完成。原样转发诊断确认范围选择正确为编号2，候选译文正确，第一次语义复核
却猜成子标题；第二次三个boolean都true但issues含风格建议与范围疑虑，最终拒绝。
界面和截图已实际核对，失败记录为`macos-page-evidence-ui-r17/translation-scoped-ui-r1.json`，
明确`passed=false`。误点产生的找官方下载任务不计入翻译测试，未执行下载。

已恢复App直连`http://127.0.0.1:8000/v1`并重新展开设置确认；精确停止诊断进程43775，
会话8474实际退出0，原始诊断摘要
`69186c02bc00d68670069331d93bb9d58ac36415a34fe8b7c9f7aa7a021d11cd`。
修复仅改翻译复核及测试两份源码：保留浏览器角色、字号、来源，明确复核只比较已
选范围内的语义；issues只报告实质错误，不按同义词偏好重新猜范围。原有原文绑定、
漏译/错选/多译拒绝及严格boolean和空issues通过条件未放宽。

直接读取新产品契约，使用同一本地Qwen执行8项正反对照、无重试，全部符合预期：
正确主标题/自然译法/全文通过，遗漏“独立”、数字改变、否定反转、中文未翻译、
数据内指令伴随错误均拒绝。摘要`844811d8efe87b1ac2bdba126e8258d5030374ff8559630b4b7260c15d7c835b`。
这只是模型对照，不代替新App；新增原生元数据回归和完整516项仍待运行。
R18已冻结53份候选源，清单
`77047b0bd3d915f6d6295149ad58e1dd55f3e15ff8143189456a13566647bd33`，
旧R17程序和失败资料保留，Android仍R15、没有新构建或安装。未改变默认App或系统权限。

**2026-09-09 12:22 UTC（本地20:22）当前页绑定修复已验证**：R17构建于12:11:02
真实退出0，随后515项原生回归全部单次通过、无重试，12:15:04退出0；浏览器88项
含四组新增绑定测试全部通过。新构建摘要
`5e6cb01581690a986926f28b20551e88e702158d62d7cc24e3bdf83bea4c74b1`，
新测试摘要`6034392ba32c9434fcc915d7f9fc6b8e94f0127172037287d84efd0026955752`。
这次确实重新运行了515项，不是只读重验旧记录。

通过原生应用菜单正常退出R15，原会话36975退出0、PID17257消失；原监控退出前
仍为1条、HTTP200、准确变化摘要、下次20:21:23。资料未删除，退出期间不会运行，
且未迁移到新资料。新独立签名副本R17于12:16:33启动，会话56653、PID35875，
整包和独立资料绑定至`macos-page-evidence-ui-r17/app-identity.json`。

真实界面连接同一本地Qwen，先加载原失败使用的`/slow`测试页，再保留它在后台。
空白页和书签管理页分别点击同一句“总结当前页面内容，并列出重点”，均显示
“请先打开一个普通公开网页，再让Aegis处理当前页面”，没有误读后台结果或宣布完成。
切换到实际测试网页后再次提交，20:20:54开始、20:21:03完成；总结正确含18小时
续航、24个月保修、本地合成资料声明，来源严格为当前`/slow`，R0、
`page.observe/page.extract`。三个结果均经Computer Use及真实截图目视核对。
证据为`macos-page-evidence-ui-r17/current-page-binding-ui-r1.json`。

因此本次发现的当前页绑定错误已关闭；全页来源关系只证明macOS原生测试通过，
不替代Android设备验收。Android仍停在保留的R15源码和增量产物，未同步R17、未安装；
Windows认证、Pixel隔离调试授权及其他三端验收缺口仍在，完整目标保持未完成。

**2026-09-09 12:06 UTC（本地20:06）当前页修复进入原生构建**：已修正主目录的
`ContentTab`：普通页面以活动标签为准，不再被旧右键上下文或窗口末尾网页替换；
仅Agent全页可沿原生直接来源标签读取HTTP(S)内容，并核对来源仍属同一标签列表。
无有效来源时保留当前内部页，让正文任务明确拒绝，书签类工作流仍可使用。
已核对Android现有Java入口传入当前父标签，原生`GetOpenerForTab`经JNI实际使用
`target.getParentId()`，不是另加URL参数或模型自行指定来源。

新增四组浏览器回归覆盖：内部页及旧入口拒绝、活动公开页优先于旧选区、全页
使用真实来源而非末尾网页、来源缺失或非公开时拒绝。加上既有用例，完整预期为
515项（浏览器88项），尚未执行完毕。R16首次编译明确失败于本版GURL没有
`host_piece()`，已对照本地头文件改为`host()`；失败日志和冻结源原样保留。
修正版R17清单摘要`2e883962fe01e9ac7c0f7a404c5d201562fc47e12553f5549371dd3c2a7439d1`，
53份候选源码核对一致，旧R15程序以写时复制方式保留，Android源码仍为R15。
准备脚本4项、测试记录核验器22项自测通过，不计作新的产品测试。

为修复已确认问题，本任务主动正常中断Android第六轮；91577于11:55:50.312
真实退出1，SIGINT精确送达，源码未变，7080项完成、0项编译失败、68115项剩余。
没有APK或安装，原包装/调用/防闲置进程均已退出，专用容器仅休眠与已退出僵尸进程。
状态摘要`ab86ab4b43eca3d22add77bd6324e71d8ce94c128b9fd4b1e54959ddb3527a83`，
原`siso.INFO`及`siso_output`已另存，增量产物未删除；不是未知容器再次触发中断。

R17 macOS会话97492、构建进程29073已启动，修复文件真实编译成功并进入框架
单路链接。按实际可用句柄约30万判断启动条件，保留启动20万、运行10万余量、
单路链接和磁盘保护，不再用“已占用必须低于3万”代替剩余容量判断；没有调整
系统上限或停止其他项目容器。当前尚未产生成功构建、515项新回归或新界面通过证据，
R15仍在运行且保留原监控资料；修复完成门槛仍待新程序实测。

**2026-09-09 11:52 UTC（本地19:52）新增实机失败项**：同一R15 App、进程17257和
隔离资料实际跑了两个“总结当前页面内容，并列出重点”负例。当前选中书签管理页
时，19:48:35开始、19:48:45结束；选中已有`about:blank`时，19:51:08开始、
19:51:16结束。两次均没有切换活动标签，却读取后台`http://127.0.0.1:52861/slow`，
输出合成产品18小时续航、24个月保修，并显示“已完成”。活动页、来源、结果和
时间线经Computer Use实际核对，书签页的上下两屏截图也确认不一致。

因此这不是显示残留，而是**当前页目标绑定失败**。证据存于
`macos-page-evidence-ui-r15/current-page-target-mismatch-r1.json`，明确`passed=false`。
源码`aegis_agent_page_handler.cc`的`ContentTab`在活动页非HTTP(S)时，遍历并选择
窗口末尾的其他HTTP(S)标签；注释虽为Android全页入口，实际没有平台或入口条件。
显示快照与创建任务都使用此函数，与实测吻合。不能用旧511项原生通过结果覆盖
此缺口；尚未修复，也不推断Android或Windows已实际复现。

修复需让桌面普通侧边栏绑定活动标签；Android全页入口明确保留并校验来源标签，
不能按标签排列猜测；同时保留书签类无需页面正文的操作。当前Android第六轮
会话91577仍存活、日志约18分钟推进6135项，本轮没有改动其冻结输入或重启构建。
在修复及同候选重新实测之前，当前页总结门槛判失败，全部十项标准仍未完成。

**2026-09-09 11:44 UTC（本地19:44）真实界面补验**：macOS桌面当前已可操作，
不再沿用此前锁屏状态。通过Computer Use读取同一R15独立App，并实际查看窗口截图：
监控仍为1条，显示上次检查HTTP200、下次本地19:51:23，以及“电池续航从24小时
缩短至18小时，保修期从36个月缩短至24个月”的准确变化摘要。原公开页日志序号112
于11:36:23.031读取`/article`版本1、HTTP200。新记录
`macos-page-evidence-ui-r15/monitor-restart-r1/change-summary-visible-r1.json`
关闭此前“只看到请求、未核对最终摘要界面”的缺口；不推断首次生成时刻、睡眠期间
每次运行结果或系统通知送达，后者仍未验证。

同一App进程17257、独立测试资料和签名包保持不变。经原生书签管理器导入500条
合成书签，前置核验全部网址与夹具一致，未导入日常数据。19:40:50通过Agent常用任务
提交原500条标题分类预览目标，19:41:20实际完成。模型第一次计划及一次自动修正后
仍未满足格式要求；浏览器明确显示使用批准的只读步骤，技术详情仅允许
`bookmark.list, bookmark.plan`、R0。结果为其他200、开发200、研究100，代表标题
均来自真实夹具，明确“尚未应用修改”，完整结果及纠错时间线已目视截图核对。

既有独立核验器4项自测通过，并以实际导入资料分别执行snapshot和verify，均退出0；
500条书签前后整树、名称、网址、父节点与顺序完全不变，树摘要
`456cf1c20736f9d54d3ade636d0cf954580d027ac778e6e2a8c5c3d5d071d9c3`。
任务期间公开页服务没有新增请求，与只读标题预览相符，但不将此当全局网络隐私证明。
新结果`macos-page-evidence-ui-r15/bookmarks-500-title-classification.json`已绑定
前后证据摘要；记录绑定和序号112原始日志再次核验通过。此次补验不代表模型首次规划
成功、网址有效性检查或真正移动书签通过，也不替代三端全部十项验收。

Android第六轮会话91577仍真实存活，11:41日志约8分24秒、3412项推进；未重启，
未修改任何绑定的产品源码或构建脚本。Windows认证、Pixel隔离调试及通知授权边界
不变。后续继续完整场景与跨平台验收，不再将单纯构建轮询当作整体进度。

以下为 **2026-09-09 11:33 UTC（本地19:33）恢复检查**：第五轮会话72935于11:23:00.962
真实退出1，源码未变、没有APK或设备安装。11:22:28另一项目启动Redis测试容器
`0e66467f9f0df659f1cc3b45819f746b5893c192a1d267018553d5504bff603e`，
11:22:40触发未知容器保护，中断已确认送达。原始日志汇总为25627项完成、0项
编译失败、38651项剩余和信号中断；不是源码编译失败。原三个宿主进程均已退出，
专用容器没有活动构建，只有原休眠主进程和已退出的僵尸进程。

第五轮`status.json`摘要为`03a3524e2adaa293978275f05f1f6092cde1731533f8f9172afd2b7e28a8ee04`，
中断请求为`eaee642d9313b9be9f5b08d5474ff4cc2be1b4ca6a1f98ed5015a21722309a65`，
原构建日志为`7bca644708f8f129becfb31b76284e272caee290da64477c9c8ea68b96469081`。
已将会随新构建变化的`siso.INFO`和`siso_output`另存至第五轮证据目录，宿主与
容器摘要逐项一致；前者为`b89cc33a10dd1b52db5141f31e3dab1162b4762e73e11ef159ee47fe70d01d01`，
后者为空文件。第六轮继承并绑定第五轮终止证据与所有既有源码、原生结果及脚本。

Redis现场只有初始化PID11608的单个`redis-server *:6379`进程，6线程、
0.63%单核CPU、18.54MiB内存，镜像、入口与命令均已核对。第六轮新增精确容器ID、
镜像、名称、入口、命令、进程表和资源检查；不是按名字放行。该服务与原数据库、
空闲Go容器合计仍不得超过半CPU核、512MiB、32线程预算；额外进程、未知容器及
异常状态仍拒绝。沿用15秒运行期采样和只中断本任务的保护，检查间隔不保证零
瞬间重叠；未停止或修改其他项目容器。首次第六轮前置仍被未知容器真实拒绝，
新增33项Redis正反例后，合计270项安全自测通过；这不是新的产品或真机测试。

11:31完整前置通过：53份源码一致、511项既有原生结果只读核验，接电、机盖开、
时钟一致、229GB余量、262395句柄余量，无其他本机大构建。第六轮会话91577随后
真实启动，包装进程9618、容器调用10453、防闲置睡眠进程10435；后者临时绑定9618，
系统断言已确认。新日志`page-evidence-r15-r6-evidence/build.log`在31秒已推进157项。
尚无第六轮APK或新真机验收，全部十项完成标准仍未满足。

以下为 **2026-09-09 09:01 UTC（本地17:01）恢复检查**：Go测试结束后实际仅剩空闲
`tail`，但观测内存仍为147.5MiB；新增128MiB单容器门槛误拒绝这一空闲状态。
最终改为所有允许共存的其他容器**合计**不超过原512MiB、半CPU核、32进程预算，
没有增加总体资源预算。空闲Go容器仍必须只有精确身份的单个休眠`tail`进程；
任意工作进程及未知容器仍拒绝。完整自测由227项增加到236项全部通过，重放
已观察147.5MiB空闲容器与38MiB数据库资源值通过，明确为读数重放而非新现场。

09:00容器范围等待会话67095真实退出0；随后实际运行列表只剩Aegis专用容器，
两个其他项目容器已退出，未由本任务停止或修改。旧等待容器消失的宿主命令
PID17282由本任务精准结束，原会话34292退出143；只关闭自己的等待，不操作目标
容器。第五轮完整前置09:00:43通过：53份源码一致、511项既有原生结果只读核验，
接电/机盖开/时钟一致、231GB可用、288801句柄余量，无本机其他大构建。随后启动
第五轮增量构建，真实会话72935收到启动回执；包装进程25423、容器调用26526、
临时防闲置睡眠进程26508，日志`page-evidence-r15-r5-evidence/build.log`在24秒
已推进110项ACTION。尚无APK成功或新的真机验收，全部十项标准仍未完成。

以下为 **2026-09-09 08:57 UTC（本地16:57）检查**：确认新的Go容器是常驻容器，
不能以其持续存在推断实际编译，也不能无限等待容器本身退出。第五轮新增
`android-build-companions-r15-r5.mjs`：仅精确核对该容器ID、镜像、名称及启动命令；
只有单个已匹配初始化PID的休眠`tail -f /dev/null`、1进程、CPU不超半核、内存
不超128MiB时允许共存；出现任意工作进程或未知容器仍拒绝。原数据库保护、
本机大构建拒绝、资源/供电/时钟检查及15秒运行期检查均保留。每15秒采样存在
检查间隔，不声称能够保证其他任务启动时零瞬间重叠，也不停止其他项目。

新增26项精确身份/实际工作/异常进程表/资源拒绝自测，连同原201项共227项通过。
实际复核期间该容器启动Go测试及测试子进程；新第五轮前置真实退出1，原因明确为
“其他容器已有实际工作进程，等待其完成”，没有启动构建。接下来按实际工作
进程结束核对空闲状态，再做完整源码/原生/环境前置，不再要求常驻容器消失。
没有新APK或产品验收通过，完整目标保持未完成。

以下为 **2026-09-09 08:50 UTC（本地16:50）检查**：第四轮于08:47:22.805实际退出1，
源码未变，保护中断已送达，APK为空、未安装。08:46:44另一项目启动Go测试容器
`0ae0fd21073ae596b0e080d343da03d2d7d73bec08b9422b212526e8b11b7537`，08:46:59
触发未知容器保护；原始汇总703项完成、0项失败、80056项剩余及信号中断。
原会话60273及包装/防休眠进程均已结束，专用容器无活动构建。第四轮状态摘要
`a155b6ebbef19535248a8917c0d98181333ce4a1c87f839c7a56876905abb6b0`，日志摘要
`dc0344a5a6f42685f9bf830a5df21b565b5459b376fde7c480e79fc46aa7f712`；另保留原
`siso.INFO`及空`siso_output`，逐项摘要与容器原件一致。不把0项编译失败当成功。

只读确认另一项目运行`go test`及竞态测试，未停止、修改或加入允许并行名单。
等待会话85547确认该容器退出0后，运行列表又出现后续Go容器
`f2da51d24c275740b3a7b19b832ffdf1bd61279628b635810340bace3a81e407`
（`solana-ci-repair-20260909`）。因此第五轮前置仍拒绝未知容器，**尚未启动**。
新`build-android-page-evidence-r15-r5.mjs`及对应自测/环境模块已准备，复用原
第四轮容器保护，不放宽任何阈值；200项既有安全自测及新增1项第四轮终止核验
合计201项通过。完整源码/原生前置尚待其他构建结束后执行；此处不声称第五轮
前置通过、APK构建成功或新增设备验收。08:51读取该新容器进程时仅有
`tail -f /dev/null`，CPU0.00%、约49MiB、1进程：这次采样未观察到实际Go编译，
不能将容器运行等同于持续构建。容器仍存活且未纳入当前允许范围，等待会话34292
仍在运行，暂不启动第五轮；不擅自停止该容器。全部十项目标仍未完成。

以下为 **2026-09-09 08:44 UTC（本地16:44）恢复检查**：Android第三轮于08:36:30.399
实际退出1，保护中断已送达且来源未变，未产出APK、未安装设备。08:36:03另一项目
新启动`etf-repair-pg-20260909`，08:36:10触发未知容器保护；构建原始汇总为
29997项完成、0项失败、35104项剩余及`interrupt by signal`，不是编译错误通过或
完整构建成功。原会话36541已结束，宿主构建及防闲置睡眠进程均退出；专用容器
已核实无活动构建，仅有已退出僵尸。第三轮状态摘要
`1a3c157ad804094e9a99971a475481143b053fdda813a4022a93f87653cf2f74`，原日志摘要
`2ce13a0762735a96dd3ae867a1ba21ac438735903c9f6e8e2849fed723a1d4a2`。
另保存原`siso.INFO`与空`siso_output`，宿主副本与容器原文件逐项摘要一致；本轮
没有`siso_failed_commands.sh`，不制造失败命令文件。旧失败和增量缓存均保留。

新数据库只读核实为原PostgreSQL16镜像、约38MiB、6进程、CPU0.00%；第四轮只
更新允许共存的精确容器ID和名称，保持半CPU核/512MiB/32进程上限，不停止或
修改其他项目。新文件`build-android-page-evidence-r15-r4.mjs`、对应自测和容器
保护模块、`android-build-environment-r15-r4.mjs`不覆盖旧脚本；绑定第三轮退出、
中断、日志及原脚本证据，继承原供电与时钟检查。169项安全自测、22项容器保护、
8项环境自测和1项第三轮终止核验合计200项通过；53份源码、511项既有原生结果
只读校验通过，不代表重跑原生或设备测试。08:43前置确认接电、机盖开、时钟一致，
约232GB可用空间、267349可用文件句柄，无其他本机大构建；随后启动新的第四轮
增量构建，真实会话60273已收到启动回执：包装进程13365、容器调用进程14257、
临时防闲置睡眠进程14238；新日志为`page-evidence-r15-r4-evidence/build.log`。
尚无第四轮APK成功或真机验收结论，完整十项标准仍未完成。

以下为 **2026-09-09 04:46 UTC（本地12:46）补证**：R15原生构建区App在全新独立资料、
无窗口模式完成4项真实浏览器控制授权回归。初始空白文档首次连接成功、重复使用
被拒绝；畸形握手消耗一次授权；公开页面正常并发连接；先确认初始授权确实可用，
再验证跨文档后旧授权失效。验证脚本退出0，测试专用浏览器无残留，调试端口关闭。
测试使用模拟钥匙串，不读取或操作已签名验收窗口；不算系统签名权限或模型敏感数据
传输验证。执行前后整包身份及既有511项原生结果再次只读校验通过，未重跑511项。
日志`macos-cdp-r15-r1.log`和绑定`macos-cdp-r15-r1-binding.json`保留，临时资料不删除。

同轮Android第三次构建在04:43仍存活，约20分钟、6616项，临时防闲置睡眠进程
及原包装进程保持运行，暂无新APK。原网页监控新增04:21:20和04:36:20两次版本1
读取；不抹除此前睡眠中断，也不据HTTP200确认最终变化摘要或系统通知。

以下为 **2026-09-09 04:17 UTC（本地12:17）终止检查**：Android第二轮于
04:16:15.284实际退出1，无信号、无保护中断；源码未变，APK摘要为空。原始构建
汇总38575项完成、69项失败、21756项剩余，69条原始错误均为 `future mtime`。
原会话51407已结束，容器无活动构建，仅保留已退出僵尸；不会重启该记录。
完整日志摘要`c2d90c873ee430e3129f16c346745aa4b42a673e13bfc5c21beb8c0507ab52b3`，
宿主状态摘要`0e68475c5ca07589084d39e3d14de78e6fd204227948fcdcb0f0d84828679138`。
另归档即将被后续构建更新的三份Siso详细日志，归档摘要
`c79a7b23f99e7f5aeb8de9eed88a40a77a438760a3045cd49190057b01599dac`；逐项流式
解压摘要与容器原文件一致。首次内存式核验因128MiB缓冲上限失败，未影响归档；
改用流式核验通过，保留原失败事实。

04:20只读检查确认已接电、机盖打开、宿主与容器时钟一致。新第三轮脚本复用R15
源码和增量缓存，绑定第二轮退出状态与原始失败日志，不覆盖旧记录。新增临时防闲置
睡眠进程，随本轮退出结束；每15秒检查供电、机盖和时钟，异常仍走原精准中断保护。
不点亮或解锁屏幕，不修改系统时间、永久电源设置或其他容器。169项原安全自测、
22项容器保护自测和8项环境自测均通过；前置再次核验53份源码及511项既有原生
测试证据通过，不代表重跑原生测试。第三轮会话36541已实际启动，宿主包装进程
62634、容器调用进程63435，四路编译；新日志为
`out/AegisAndroid/page-evidence-r15-r3-evidence/build.log`，宿主记录为
`android-page-evidence-r15-r3/`。30秒时已进入ACTION/CXX；临时防闲置睡眠进程
63417的系统断言已实际核对，随包装进程退出结束。尚无新APK成功或真机通过证据。

以下为 **2026-09-09 03:18 UTC（本地11:18）历史检查点**：Android第二轮仍未退出，
原会话51407及容器调用进程21393存活，但已出现多项 `future mtime` 输出校验错误。
日志中一项产物时间为01:00:05.362 UTC，构建进程当时读取的时间为00:53:18.000 UTC；
后续单独采样宿主与容器时间均为01:00:37 UTC，不能据此否定构建期间的偏差。
系统电源日志确认08:35、08:53、09:00后反复睡眠和短暂维护唤醒，与偏差时段重叠；
这是环境时间异常证据，不是C++语法错误或整轮成功。03:18时日志约38379项，
仍处理其他编译任务；尚无最终退出码或新APK成功记录。未重启活跃构建、修改系统
时间、停止其他容器或删除缓存，原资源保护和失败日志保留。

macOS原UI进程17257及公开页服务49656仍存活。公开页请求在00:05:21后中断，
02:35:24恢复一次版本1的HTTP200，间隔约150分钟；不得将睡眠期间记为连续15分钟
监控通过，也不能以这次恢复读取代替最终摘要、通知或界面验收。仍待手动解锁并保持
电脑唤醒后核验，未绕过锁屏。全部十项标准仍未完成，不发布、不安装旧APK代替。

以下为 **2026-09-08 17:36 UTC（本地9月9日01:36）历史检查点**：Android第二轮
已持续约33分钟、完成约9700项，原容器调用进程21393仍存活，包装会话仍运行，
暂未有新APK。macOS原UI进程17257仍在；01:35:18又有一次自然定时读取版本1，
请求序号20，与之前的独立参数清理回归区分。最终摘要界面仍等待手动解锁核验。
原每小时自动跟进已更新为按最新状态继续推进，频率和权限边界不变，未创建重复任务。

以下为 **2026-09-08 17:33 UTC（本地9月9日01:33）补证**：同R15原生App新增
跟踪参数开关对照，开启时移除`utm_source`/`fbclid`并保留`keep=yes`，关闭时
原查询参数完整保留。普通钓鱼页与隐形字符混淆变体均进入安全拦截页，日志分数
均为97；正常Unicode对照保留中文、重音字符和emoji且未被拦截。四次独立测试
浏览器进程均退出0、无残留；临时ASCII服务已关闭，测试资料和日志保留。
报告及摘要绑定见`macos-link-ascii-r15-binding.json`。此处仍为独立无窗口机制回归，
不替代锁屏后尚未核对的监控摘要或最新Android/Windows真机窗口验收。
共享公开页请求序号16–19属于本次参数清理对照，不是监控的新增定时执行。

以下为 **2026-09-08 17:23 UTC（本地9月9日01:23）检查点**：Android第二轮继续运行，
已进入C++编译，约6200项完成，暂未出现编译错误；尚无新APK，不计真机通过。
Pixel当前在线、解锁亮屏；既有独立UI工具完成6项真实输入/点击/拒绝边界自测，
宿主51项相关测试通过。设备上的旧APK摘要为`326057099a0020849ed7f28762b8d87e8b6042056159db544224bbd8b04f3764`，
当前仍运行默认`app_chrome`资料，未在其中操作Agent或覆盖资料。

R15原生构建区App完成7组指纹防护运行回归，**346/346断言通过**、11次独立
测试启动均正常退出0；要求真实WebGPU，Canvas/Audio/Worker、关闭/暂停/重启/
资料隔离对照通过。日志逐项摘要、前后产物/源码和原生历史记录均核验一致，未重跑511项。
完整报告`macos-fingerprint-r15-runtime.json`摘要
`dd28bd9809768bc4357d599ce9ed972c50b2842311fc94ced64ac146f7f7dc2d`。
使用原生链接器签名App和新临时资料的无窗口模式，不是锁定屏幕操作，也不是已签名UI副本的窗口验收。
正式两阶段发行身份未提供，程序退出2表示行为通过但不具备发行资格。

同R15原生App的跨站合成密码表单：开启防护进入中文安全拦截页，关闭防护显示
原表单；两轮均退出0、无测试残留进程，未填写或提交表单。首轮因旧正文预期失败
保留为`macos-phishing-r15-on.json`；按冻结源码的新正文重新验证后通过，未改变检测阈值。
新通过报告为`macos-phishing-r15-on-r2.json`和`macos-phishing-r15-off.json`。
NetLog仍观察到Google相关及防护列表更新的后台URL请求事件，不能宣称全局零出站。

R15原UI实例重启后，01:05:15首次真实读取版本2，界面显示HTTP200并自然推进
下一次时间；随后只改变公开测试页版本为1，01:20:15再次自动读取到新版本。
第二次最终变化摘要尚未目视：01:08左右CUA报告Mac锁定，未绕过锁屏，已非阻塞请求手动解锁。
35秒慢页面任务在正文返回前保持执行，01:06:12完成且保留24小时/36个月及否定免责声明；
但模型额外称声明位于“页面底部”，与原页面位置不符，记录为剩余摘要准确性问题。
以上均为分项进展，**完整十项标准仍未关闭**。

以下为 **2026-09-08 17:04 UTC（本地9月9日01:04）历史检查点**：Android首轮在
16:54因保护逻辑把其他项目新启动的轻量Postgres容器判为冲突而中断；退出码1，
已完成2790项、失败0项，不是编译错误。原记录与日志保留。现场核验该数据库
约58MiB、0.03% CPU后，新建同源码第二轮构建脚本，只允许该完整容器身份共存，
增加CPU、内存及进程数限制，仍拒绝未知容器、本机另一个大构建和资源不足。
未停止或修改其他项目。169项安全自测及22项新增保护测试通过；已暂存源码
和511项macOS原生证据再次只读核验通过。第二轮实际运行于
`android-page-evidence-r15-r2/`，容器调用进程21393，当前尚无新APK。

同一R15 macOS App通过菜单正常退出后，以同包、同profile、同六项参数重启，
进程17257；334项整包记录与原签名快照逐项一致。界面及截图确认监控仍为1条、
下一次运行仍为本地01:05:15，未重置计划或改系统时间。
`macos-page-evidence-ui-r15/monitor-restart-r1/`保存退出前快照和重启记录；
**这里只通过重启恢复，真实定时到点与变化摘要仍待验证**。

以下为 **2026-09-08 16:46 UTC（本地9月9日00:46）历史检查点**：Android实际启动首轮。
R15的52份overlay及1份Chromium启动文件共53份，已同步至原Android构建区，
逐文件与macOS和冻结快照一致；共12份内容变化，旧源码压缩备份逐项解包核验通过，
原有17项删除保持不变。阶段记录为`android-page-evidence-source-r15/stage-status.json`，
准备记录摘要`ae904de53473f72e8fe69ddf0305f8acc501a7485bf13d2c3f10bfe820c235b1`。

旧Android只有50文件清单，遗漏新版新增的service factory两文件和启动补丁。
这三份旧输入均与原Git基线实测匹配后才纳入迁移，未借用新快照的before摘要。
真机源码核验器增加对`chromium_files`的同等路径、摘要、重复与Git改动检查，
新增六项真实临时Git测试；全部38项通过。新准备/构建脚本169项自测通过，
包含146项原有安全检查、43项真实Shell模拟分支和原始翻译范围证据的反例检查。
完整`pnpm run quality:fast`退出0。511项macOS原生结果已重新核验并绑定程序和源码，
本次没有重新运行511项，也没有把脚本自测计为Android产品通过。

同一专用容器已启动，宿主构建包装进程14184、容器调用进程14971、容器内Siso
进程23已实际运行；构建目标为`chrome_public_apk`，4并发，静态分析保持开启。
启动前剩余空间241474985984字节、可用文件句柄478030；虚拟机18核、
25159270400字节内存，宿主与虚拟机时钟检查通过。原15秒资源保护仍启用，
没有并行启动第二个大构建。日志位于原构建区
`out/AegisAndroid/page-evidence-r15-evidence/build.log`，宿主记录在`android-page-evidence-r15/`。
该首轮现已因上述容器冲突判断中断；尚无新APK、安装或真机场景通过证据。

Pixel已于9月8日22:07重新通过USB连接并确认解锁，不再属于连接缺失。
Windows原账号密码已从用户先前消息找回，不再要求重复提供；原端口可达，
但RDP仅认证检查仍返回登录失败，已停止重试，未关闭证书校验或绕过Defender告警。
本轮没有安装/启动手机浏览器、改调试设置或默认资料，也没有发布、提交或推送。

以下为 **2026-09-08 09:07 UTC（本地17:07）** 的检查点：用户确认解锁后，
同一R15测试App、进程70491、独立资料的真实界面已可操作。通过工具栏入口，
使用指定本机Qwen完成两条原句，未修改原目标或重新提交任务：

- “把当前页翻译成英文，不要整理书签。”：17:00:46至17:00:59，界面显示已完成。
  真实页面提供七个原文片段，选择1至7，英文结果包含文档标题、正文主标题、
  免责声明、两个小标题和24小时/36个月正文；一次完成候选、一次语义复核。
- “只把正文主标题翻译成英文，其他不要翻译。”：17:02:38至17:02:55，
  界面显示已完成，结果仅为`Synthetic Product Public Information`。
  原生页面证据区分标签页标题与正文标题，实际主标题字号XL、小标题L；
  模型先选编号2，最终只输出此片段。首次完成候选缺少3至7的编号，
  被产品内格式检查拒绝；一次纠正后七项完整且仅编号2有译文，随后语义复核通过。
  不能写成“首次即通过”或“零内部纠正”；旧R12/R13失败记录保持不变。

两次均已检查实际结果截图与可访问性文字。仅这两个合成目标的协议诊断原样
转发至127.0.0.1:8000，15次HTTP均200；七条有界诊断保留模型原生参数。
界面结果逐项对照原文和真实响应，不以HTTP状态或模型自评替代目视验收。
已在界面保存并重新展开确认恢复直连8000，模型不变、未输入密钥；
临时转发器正常退出0，51551端口不再监听。详细结果为
`macos-page-evidence-ui-r15/ui-translation-r1.json`，日志摘要
`8c6618c985f0a2c44d0a6d896b7c8a80cbc59688a681e6339fe6ae90bf008f68`。

跨平台检查：Pixel的ADB、USB设备识别、无线调试发现均为空，不能认定设备仍锁屏；
当前问题是测试连接未检出。已找回原Windows主机地址，3389及5986端口实测可达，
但本轮没有登录会话或Windows界面证据。没有重试之前被Defender拦截的上传，
没有改变安全设置。Android源码仍R8，尚无最新APK或真机通过；完整十项仍未达到。

以下为 **2026-09-08 08:29 UTC（本地16:29）** 的检查点：R15完整构建成功，
08:18:46至08:24:44结束，源码与默认App均未变化。511项原生回归于08:28:02全部
通过，单轮、零重试：158/87/182/84；510条主JSON记录及另行核对的PRE成功日志。
独立逐名、程序/源码摘要复核亦通过。翻译范围与预算浏览器用例3437毫秒通过，
空正文用例1187毫秒通过。结果为`macos-page-evidence-tests-r15/tests-status.json`，
摘要`b44fc23251ef258ceec29e8d09a8d66cd1e065046ccf4937da07d207d4503280`。
旧R13失败未覆盖，不把模拟模型的原生测试算成真实Qwen完成任务。

R15已复制为独立测试App、临时签名，以新资料和原六参数启动，进程70491；
完整签名包摘要`b736ea837cf105005c584104c09bb30fa744b5a2aa59af7c7ecd4436040713b2`。
首次Computer Use界面检查报告**Mac锁屏，无法自动解锁**，已停止界面操作，
没有绕过锁屏、改权限或检查系统安全对话框。需要用户手动解锁后，用同包同资料
继续两条原句的真实Qwen验收。本次没有提交翻译任务，也未启动临时模型转发器，
不能声称R15真实App翻译通过。现场记录为`macos-page-evidence-ui-r15/ui-blocked-r1.json`。
App及原公开夹具保留运行；Android仍为R8，跨平台验收与发布门槛仍未完成。

以下为 **2026-09-08 08:19 UTC（本地16:19）** 的源码检查点：R13构建退出0，
07:58:22至08:08:49完成19步；完整原生回归实际运行510项，509通过、1失败、零重试。
三套单元回归158/87/182全部通过；浏览器失败仅为
`TranslationCompletionRequiresFaithfulReviewAndPreservesBudgets`。新增部分目标没有
“当前页”等显式页面指代，测试创建入口进入了未模拟的任务路由；返回旧任务快照，
目标一致性检查失败，后续两个部分选择场景未验收。没有将此失败归为Qwen语义结果。
真实章节提取测试已验证主标题XL、小标题L和正文非标题属性；R13没有启动独立App。

R15修正该测试入口，显式限定已授权夹具来源并先检查创建错误，原目标文字未改。
另外对缺少完整翻译原文且无法保留来源的分支补充明确失败；空白页若仍有文档容器
证据则可以部分完成，两者都不能停留运行或误报完整。新增空正文浏览器回归。
这是一处源码审查得到的防御性修正，**没有声称已用旧App复现空页卡住**。
中间R14仅冻结、未构建；复核空页容器语义后另存R15，未改写旧快照。
R15清单摘要`2ef296fe90de81c9f4b39c0fa4e0fe053566d7d0527e259605426843535ca19d`，
53文件与macOS逐项一致，Android仍为R8；目前正在独立构建，预计511项原生回归，
尚无新原生或App通过结论。翻译范围选择与复核合同未变，不重复宣称模型探针是App验证。
新冻结预检及4项自测通过，测试与启动脚本各22项自测通过；R14启动自测曾发现一个
测试样例计数与新预期相同，R15已修正该反例并重验，未绕过门槛。

以下为 **2026-09-08 08:00 UTC（本地16:00）** 的源码检查点：R13已冻结并仅同步macOS，
清单摘要`fa6cf0890880763c46df3672bda561fe2de3d3a156f944d2b4adfdde0209ff1b`。
共53个受控文件，本轮只改页面桥接、执行协议及头文件、执行服务、核心测试与浏览器
测试6个文件。原R12源码、4个程序、完整构建包及独立签名包均保留；Android仍为R8。
当前原生构建正在运行，尚无R13原生测试或真实App通过结论；不得沿用R12的505项。

修复前先做真实本机Qwen对照：原合同仍错收标签标题；只增加标题角色的13个候选
正反例仅10个通过，加入相对字号后14个也只有13个通过，均不合入。
最终采用生成译文前的独立原文范围选择，9个场景均精确选中预期编号；随后14个
正反例中4个错选/漏译/多译被宿主编号检查直接拒绝，另外10个实际模型语义检查均
得到预期判断，包括否定反转与数值错误。四轮探针分别保存在`translation-role-scope-probe-r13`、
`translation-role-size-probe-r13`、`translation-scope-first-probe-r13`、
`translation-scoped-review-probe-r13`；无自动重试，失败记录未覆盖。
这些是有界合成输入，不是App验收。相对字号推导自已核查APC算法和无样式夹具，
还必须由真实原生页面输出验证；没有把字号或第一个标题冒充HTML的H1等级。

R13保留已授权且已脱敏的APC标题角色和相对字号。同文标签/正文标题保留双角色，
不重复输出。新增内部`agent.select_translation`调用不接收译文或排除理由，选择绑定
原目标、文档身份、原文及结构；宿主拒绝无效编号和与该范围不符的译文。正文变化
会失效旧选择，语义复核只比较已核对覆盖的片段，不再猜测原文—译文对应。
增加的一次模型调用仍消耗原预算，未扩大权限、工具动作、网络或结果长度上限；
预算不足或范围不能确定只能部分完成。新增5个核心回归及同一浏览器测试内的
范围不明、无效选择、额外译文和预算边界场景，预计510个原生执行阶段。
`pnpm run quality:fast`本轮已退出0；冻结脚本4项及原生测试脚本22项自测通过。
完整原生测试、真实原句和部分选择App测试仍是下一道门槛。

最新界面检查点为 **2026-09-08 07:34 UTC（本地15:34）**：R12原句“把当前页翻译成英文，
不要整理书签。”真实Qwen端到端通过：网页标签标题、正文主标题、两个小标题、免责声明、
24小时和36个月均有正确英文结果，7个逐段译文与最终显示一致。15:21:40完成，原生
完成调用和复核调用均已捕获；之后正常退出App、只读核验持久化动作仅page.observe和
page.extract，没有书签动作。记录为`macos-page-evidence-ui-r12/resume-r1/translation-full-r1.json`。

但选择范围场景仍不通过：“只把正文主标题翻译成英文，其他不要翻译。”两轮都交付了
网页标签标题“Aegis Change Summary Independent Acceptance”，且界面仍标完成。
第二轮原样观测明确证明正确正文主标题已作为source_id=2提供，模型只翻译id=1的
document_title，排除了id=2至7；独立复核仍返回三个true和空issues。第一次原生格式
无效经既有有界修正后获得函数调用，但这没有修复语义选择错误。两轮失败分别保存在
`translation-selection-failed-r1.json`与`translation-selection-failed-r2.json`，未覆盖。
当前产品源码仍为原R12，尚未合入下一修复；Android门槛仍关闭，整体不能发布。

本次恢复前旧App和夹具均已退出，原资料记录Crashed且有陈旧锁。先完整备份并校验
274个资料文件到`resume-r1/profile-before`，未手动删锁或修改数据库，再按原包、原资料、
原六参数启动进程49723，由浏览器自行恢复。夹具保留原端口52861和revision2，新日志
独立记录；4种真实HTTP自测通过。恢复脚本10项自测与真实预检通过，未将本次恢复冒充
正常退出重启验收。浏览器现能实际加载页面，之前钥匙串与导航等待的因果仍未证明。
诊断结束已通过UI实核模型地址恢复8000，两个临时转发器都正常退出；App经菜单正常
退出，退出码0。夹具继续运行，旧签名包、资料、原生505结果及全部失败证据保留。

以下为 **2026-09-08 02:12 UTC（本地10:12）** 的界面检查点：Mac已可交互，仍是原R12
进程24054和原独立资料，没有重启或重建。工具栏Agent入口成功打开，保存了指定本机
Qwen连接；但导航到既有测试页时地址正确、文档一直停在about:blank，未观察到浏览器
请求到达测试服务器。只读curl的独立请求正常取得revision2（24小时、36个月），
不能用它替代App成功读页。系统安全进程24066从本次App启动起即存在；系统工具禁止
检查该安全界面，已停止该检查路径，未输入密码或改变权限。两者的因果关系尚未证明，
需用户手动查看是否有Aegis相关钥匙串提示，并在核验后自行处理。

本轮没有提交Agent任务，也没有实际调用Qwen翻译。临时原样转发器记录为空，已正常
关闭（退出码0）；通过界面重新展开确认连接恢复为`http://127.0.0.1:8000/v1`，
不遗留对临时端口的依赖。失败现场保存在`macos-page-evidence-ui-r12/navigation-pending-r1.json`。
“AI已连接”标签和保存成功仅是配置状态，不算模型连接或翻译功能已验收。
Android仍未更新、Pixel未连接，Windows及系统通知授权边界不变；整体仍未完成。

以下为 **2026-09-07 22:58 UTC（本地09-08 06:58）** 的原生检查点：R12完整构建与505个
原生执行阶段均单轮、零重试通过；独立逐名复核、程序与源码身份检查也通过。
实际计数为153/87/182/83，JSON为504条，另核对冷启动PRE仅运行一次且成功。
翻译浏览器回归2622毫秒通过，含4个新增结构错误场景；不把模拟语义判定当作Qwen。
构建22:48:12至22:53:26成功，19步；完整回归22:56:52成功，记录为
`macos-page-evidence-tests-r12/tests-status.json`，摘要
`8a3dac6c49d84454a1f5c56fe3fef7445021aa47ad82b2509a2d97d131d68d78`。

旧R11经菜单正常退出且退出码0，暂停监控及旧资料保留。R12于22:57:53从独立新
资料、空白页启动（进程24054），临时签名完整包摘要
`a6cba36b6a6e59404d21bcd40e0a5ff094f8288599f5aa2f96ff7c60d40f9b51`；
未改默认App或系统权限。但首次Computer Use核验返回Mac锁屏，**没有任何R12
实际界面或Qwen翻译通过记录**，没有启动临时模型转发器，需用户手动解锁后继续。
本机只读HTTP检查确认测试页当前revision2（24小时、36个月）、模型列表可访问，
这不是App读页或模型实际完成任务的证据。原R11失败和监控证据均独立保留。

R12已冻结52个overlay文件与1个
启动源文件，清单摘要`893f1f992afab556d788086359d3471c9828803fab2a4b3f28698eca95393573`。
实际只同步5个修改文件到macOS，旧4个程序和完整App均有独立备份；Android仍为R8。
R12结果来自本次独立构建和测试，不沿用R11的499项通过记录。
冻结脚本4项自测与真实预检、测试脚本22项自测通过。预检曾因其他项目的PostgreSQL
容器而拒绝；只读确认其没有挂载Aegis目录后，检查限定为Aegis容器冲突，没有停止
其他项目。启动时磁盘约79GiB、句柄可用478577，原有磁盘和句柄停止保护保持不变。

R11在未打开Agent面板的重启后，于06:01:02.834自然读取基线；06:16:02.940再次
自然读取修改后的合成页。真实Qwen变化摘要准确报告18→24小时、24→36个月，
原页面仍显示旧正文，未被监控刷新。06:17通过界面暂停后，至06:31:21跨过原定
06:31:04检查时间，App和夹具均仍运行，界面仍暂停，夹具日志停留在序号174，
没有新增请求。此证据仅证明这一观察窗口，不证明无限期无访问或系统通知已交付。
证据为`macos-page-evidence-ui-r11/restart-r1/`中的`change-summary-verified-r1.json`
及`pause-no-access-verified-r1.json`；最终监控保持暂停。

翻译诊断第二轮恢复18小时/24个月的原正文，06:05:51再次部分完成：复核连续两次
返回普通消息而无原生函数调用，已保留真实请求/响应结构摘要。第一轮另有漏译主标题
却被模型判通过的问题。单纯改写指令或让模型自行寻找逐段对应的两份草案都未通过
标题反例，未直接合入。浏览器事先绑定原文—译文对应关系的诊断覆盖6个语义样例，
另有2个明确选择部分内容/不得任意跳过全文片段的样例，全部得到预期结果；这些是
合成输入模型探针，**不是新版App验收**。临时转发器已正常关闭，App连接恢复
`http://127.0.0.1:8000/v1`，原监控未依赖临时转发器。

R12仅修改执行协议、执行服务、相应核心测试和浏览器测试共5个文件：翻译完成结果
增加原文片段编号，宿主拒绝缺号、重复号、额外编号及自由说明替代译文；网页标签标题
与正文主标题区分，最终显示按源片段顺序重组。明确未请求的片段保留编号与排除理由，
独立复核仍按原始用户目标判断，不能借理由扩大或缩小目标。原4KiB结果长度、工具
数组上限、权限和调用预算不扩展，超限不能宣称完整。新增6项核心测试和同一浏览器
用例内4项结构错误场景；预期505个原生执行阶段，504条主JSON结果及独立PRE成功日志。
原生编译与测试已通过，实际App验收待解锁。Android仍未更新，Windows实机及系统通知
门槛未关闭，整体不能发布。

以下为 **2026-09-07 22:05 UTC（本地09-08 06:05）** 的真实界面检查点：Mac现已可操作。
R8已通过应用菜单正常退出，原失败现场保留。R11已复制到独立测试目录、临时签名，
使用独立资料启动；未修改默认App、系统权限或旧资料。完整签名包摘要为
`7de4aa3a0aaed47605e789c0cd00fd4029f08cbd72d352f128ea81d2d0c53444`。

真实Qwen原句“把当前页翻译成英文，不要整理书签。”在05:46:53结束为**部分完成**：
浏览器确实观察、提取了合成页面，但未交付经过复核的完整英文译文。
错误完成提示已被挡住，不代表翻译功能通过；本次模型交互原文尚未捕获，根因仍待定位。
证据为`macos-page-evidence-ui-r11/translation-qwen-partial-r1.json`，通过标记为false。

原始15分钟监控请求在05:46:05真实创建成功，限定同一个本地公开测试来源，
唯一监控原定06:01:02检查。05:53:35正常退出R11，退出码0；只读核验原资料、
加密目标、原周期和原deadline均保留。05:53:52以原包、原资料和原六个参数重启，
新进程31951，界面仅about:blank，Agent面板关闭。真实首次请求在06:01:02.834发出，
测试页记录GET、HTTP 200、revision1；到点后截图仍只有空白页且面板关闭，再打开面板
核验唯一监控显示上次成功、下次06:16:02。**原包原资料、不打开Agent面板的真实冷启动
首次15分钟检查已通过**，证据为`restart-r1/first-timer-verified-r1.json`。
系统通知和变化摘要尚未验证。重启脚本41项自测通过，不计入产品原生测试数。

追加本机模型诊断：只对固定合成页的原句翻译采集原生工具参数，转发内容不改；
18项转发自测、筛选自测和代表性Responses HTTP往返验证通过。06:03:57实际交付
英文正文，24小时/36个月及免责声明正确，但人工检查发现漏译“合成产品公开资料”
主标题，模型独立复核仍误判覆盖完整。此条仍为未通过，保留在
`translation-diagnostic-r1.json`，不以UI“已完成”替代人工验收。
此前05:46第一次失败原因为待定位；诊断轮不覆盖旧失败，也不调整原生门槛。
人工打开测试页时一度因输入丢失冒号而生成Google搜索URL，已停止并纠正为明确本机
URL；此段不作为“无外网访问”证据，未修改Agent自身的来源权限。

以下为 **2026-09-07 21:19 UTC（本地09-08 05:19）** 的原生验收检查点：R11完整构建成功，
499个原生执行阶段（147/87/182/83，含1个PRE前置阶段）单轮、零重试全部通过。
原始JSON有498条主测试记录；完整日志另外确认PRE仅运行一次且成功，没有忽略前置阶段。
独立逐名复核、四个程序摘要、53个冻结源码、补丁与完整App内容核验均通过。
完整结果为`macos-page-evidence-tests-r11/tests-status.json`，摘要
`b0a3ea9d75f494df55f51c611ccd75f57b2870ed527e7e67cc658b166e12b68a`。

冷启动测试的PRE阶段639毫秒通过，重启后的正文19794毫秒通过：未打开Agent面板、
未主动创建服务、未修改原deadline或时钟，原加密监控恢复并自然发出一次HEAD、取得
HTTP 200；原任务仍完成，工具/模型调用均为0，网络预算使用1次，下一次仍为15分钟后。
这关闭了原生浏览器测试中的冷启动服务创建缺口，但不替代真实签名App、用户钥匙串、
15分钟界面等待或系统通知交付验证。

R11构建21:09:11至21:15:48，15步成功，源码与默认App未变；完整包摘要
`f01f5f1bae846d43dea200ed9e25092126e95dc23a5df1c2b3d1a675758d6681`。
仓库`quality:fast`真实退出0。新R11独立启动器22项自测通过，只读预检确认产物就绪、
原R8实例仍运行且启动门槛未关闭；要求明确启动参数，禁止自动结束旧实例或覆盖资料。
本轮没有复制、签名或启动R11界面App。
本轮界面检查明确返回Mac锁屏，原R8进程及监控现场保持原样。需手动解锁后继续
真实Qwen译文、实际App重启和系统通知验证；Android仍为R8且无新安装包，Windows
实机门槛仍未完成。整体仍未满足十项完成标准，不能发布。

以下为 **2026-09-07 21:09 UTC（本地09-08 05:09）** 的开发检查点：旧产品的真实浏览器
PRE/重启测试已复现冷启动缺口。首次21:01记录及21:03完整输出确认轮均保留，
PRE通过正常接口启用、加密持久化监控和完成原任务；重启后不打开Agent面板，
仅查询现有服务即在`service != nullptr`断言失败。确认轮明确包含PRE原生成功正文，
不是将编译、夹具准备或解密失败当作产品缺口。
证据位于`macos-monitor-cold-start-red-r1/`和`macos-monitor-cold-start-red-confirmation-r1/`。

R11已在普通资料启动入口有条件恢复已启用的Agent；默认关闭和无痕资料不主动创建，
避免工厂提前缓存空服务。仅新增工厂方法、声明、启动调用及PRE/重启测试，测试最后
经正常接口清理自己创建的监控。原R8实际监控、15分钟间隔、时间与资料未改。
R11冻结52个overlay文件及1个Chromium启动文件，补丁0107与镜像正反向检查均通过，
清单摘要为`21e4b3e2242cc8e8d31383e90172a92a632642dc18922c789c98903b85a8c590`。
目前正在完整构建，尚未取得修复后的原生或真实App重启通过结果。

本候选完整回归预期执行499个阶段（147/87/182/83）；其中新增PRE成功结果按Chromium
规则不独立写入JSON，因此原始摘要应有498条，必须另查完整日志中的PRE成功与唯一执行。
新版结果校验器22项自测通过，不能将数量差异视作漏跑或直接忽略PRE。
测试用新建监控首次20秒到期、后续15分钟周期，不改时钟，不替代真实App的15分钟验收。
Android仍为R8，真实Qwen译文、系统通知和Windows实机门槛仍未完成。

以下为 **2026-09-07 20:44 UTC（本地09-08 04:44）** 的验收检查点：R10完整497项
原生回归单轮零重试通过，147项核心、87项公共模块、182项浏览器单元、81项浏览器
测试全部通过，独立复核原始清单及运行结果也通过。R10构建20:40:27成功，只重编译和
链接核心测试程序；产品App与R9逐内容摘要完全相同，整包摘要仍为
`ccdcf56760d41e1b0207fe2054cdd28cd11a3124a1928f85a495fa508c226ddb`。
源码、测试程序、清单与原始结果绑定一致；旧R9断言失败记录未覆盖。
结果位于`macos-page-evidence-tests-r10/tests-status.json`，清单为
`page-evidence-candidate-r10/manifest.json`。当前没有启动R10独立App，真实Qwen
侧栏译文复测尚未完成，不能把原生模拟模型通过当作真实模型验收通过。
已请求确认Mac是否解锁；原R8监控现场保持原样，冷启动创建服务缺口尚未修复，
Android仍为R8且无新安装包，Windows实机门槛也未关闭。因此整体仍未满足十项标准。

以下为 **2026-09-07 20:40 UTC（本地09-08 04:40）** 的检查点：R9完整构建成功，
20:25:46至20:32:27约6分40秒，源码与默认App未变，整包摘要
`ccdcf56760d41e1b0207fe2054cdd28cd11a3124a1928f85a495fa508c226ddb`。
完整497项回归20:36:47结束，496项通过，仅旧测试仍搜索已删除的英文提示
`same primary language`而失败。新增3项核心测试全部通过，81项浏览器测试全部通过，
其中翻译完成门槛的7种场景实际通过（2507毫秒），包括模型/网络预算耗尽和26次读页后
早期证据丢失。测试使用真实浏览器与模拟模型，不能替代真实Qwen译文验收。

R9失败收据保持原样，整体仍为失败；R10只将上述旧断言改为检查“默认使用用户语言”、
“明确翻译目标语言优先”和“无法完整忠实交付时部分完成”，没有删除测试或改变产品代码。
冻结清单摘要为`af9f4f57a4dcf78f7c65948f0e4e4d912183037cd697df34b48f0e5a34228930`，
50文件中仅1个测试文件变化，预期仍为497项。原R9程序与App备份逐摘要校验后同步
macOS镜像，当前重新构建，尚未取得本候选完整回归通过收据。Android仍保持R8未改。

以下为 **2026-09-07 20:25 UTC（本地09-08 04:25）** 的开发检查点：翻译结果完成门槛
候选R9已冻结并同步macOS验收镜像，50文件中精准变化8文件，清单摘要为
`723c52c1ec3a26ce58713e0c3134c5e25ace1ba645f79aa3fe52a07471eff2d8`。
候选在原任务预算内对翻译产物复核目标语言、忠实性及覆盖；失败只允许一次重新生成，
格式错误只允许一次纠正。来源按浏览器标签/文档身份保留，不按去参数后的网址合并；
正文截断、完整性字段缺失或历史原文被淘汰时不认定完整翻译，复核通过的译文也不被
后续收藏统计覆盖。新增3项核心测试与1项含7场景的浏览器测试，预期497项原生回归
（147/87/182/81），尚未获得编译、原生回归或真实Qwen界面通过结果。
R8原程序及整包已备份逐摘要核验；运行中R8独立App和资料未改，Android仍保持R8，
不启动新构建或安装。仓库`quality:fast`本次真实退出0，但不替代C++及真实界面验收。

原翻译协议的两例失败各做一次格式纠正后，均产生原生拒绝判定；总计8次请求，
原始6次报告仍为失败，补充结果单独保存于`translation-review-probe-r1/recovery-report.json`。
产品使用有界文档列表和调整后的契约，仍须重新做产品级真实Qwen验证，不能继承探针通过。
20:25 R8同一重启进程仍存活，但19:46以后无新fixture请求；源码同时确认Agent服务
工厂默认延迟创建，启动处只注册工厂，恢复监控依赖交互入口或Agent页面加载。
这构成明确的冷启动设计缺口，但尚不足以坐实当前锁屏实例的唯一根因。下一轮须用
不打开Agent页面、不主动创建服务的真实重启回归验证；此次R9未修改工厂或运行中调度。

以下为 **2026-09-07 20:03 UTC（本地09-08 04:03）** 的验收检查点：R8监控首次
19:46:11.788真实GET并建立24小时/36个月基线，随后原实例19:47:12正常退出0。
界面读取工具误重开同包无独立资料参数的实例，守卫正确拒绝重启；只正常关闭误启动
实例，没有在默认资料执行验收，但不能排除应用启动自身的自动写入。此后按原六个参数
重启原测试资料（PID97030），唯一监控、900秒间隔、原20:01:11时间及目标/基线密文
经重启前只读数据库检查确认。重启后Mac锁屏，20:03:32仍无下一次网页请求，恢复验收
未通过；没有调整调度或用新监控替代。此时只读采样未见此前钥匙串等待栈，不能沿用
先前的疑似归因。原App、数据及失败证据保留，详见R8 UI目录`restart-r1/`。
翻译结果复核协议进行了6次串行真实Qwen合成测试、无重试：4项符合预期，遗漏声明和
交换数值两项返回正文伪工具调用而非原生工具事件，严格拒绝，整体协议未通过。
`translation-review-probe-r1/report.json`的`protocol_evidence=false`及
`product_acceptance=false`保持不变，尚未将该复核方案加入产品源码。

以下为 **2026-09-07 19:45 UTC（本地09-08 03:45）** 的检查点：R8真实英文翻译
验收失败。原句“把当前页翻译成英文，不要整理书签。”的规划正确，但最终只有中文摘要，
仍显示“已完成”；失败已保存在`translation-qwen-false-completion.json`，未改写目标冒充通过。
因此通过已验证的守卫精准停止Android R8构建，容器内PID=SID=PGID=23、完整参数和
工作目录核验通过，仅发送一次SIGINT，19:42:08真实退出1，源码未变。2290步已完成、
0步编译失败，剩75641步；这是主动中断，不是编译成功或编译错误。未生成/安装APK，
缓存与源码保留；确认容器仅剩原sleep进程后正常停止专用容器。真实停止证据见
`android-page-evidence-r8/interrupt-attempt-001.json`与`stop-rationale-r1.json`。
同R8包的否定句摘要、空白起点35秒慢页面及10条标签标题查询已通过真实Qwen，
不能覆盖翻译失败。监控首次03:46:11尚待观察；R8源码与程序暂保持冻结以完成同包验证。

以下为 **2026-09-07 19:36 UTC（本地09-08 03:36）** 的检查点：Mac界面恢复可访问，
原R8进程未重启；恢复原因和系统提示处理方式未获确认，不称作钥匙串修复通过。
500条书签已成功导入，03:29:25用原句实际调用指定Qwen35B，03:29:43完成：
开发200、研究100、其他200，代表标题均来自合成书签，明确说明只是预览、没有应用修改。
期间发生一次计划格式修正，最终使用浏览器核验结果后的摘要回退；不是模型直接生成
准确统计的证据。03:29与03:30整树核验保持相同名称、网址、父节点、顺序和ID，
记录为`macos-page-evidence-ui-r8/bookmarks-500-title-classification.json`及前后快照。
这只关闭本候选500条标题分类门槛，不关闭网址有效性或全部十项验收。
原15分钟监控目标受控重试成功：03:31:06从空白页导航、03:31:13创建1条独立监控，
首次到点为03:46:11，当前等待真实触发，未改调度。首次失败记录继续保留。
同包实际打开普通钓鱼页和隐形字符变体均被拦截（97/55），后者明确显示还原检测原因；
正常Unicode对照页显示正常，没有越过安全警告。记录为`ascii-smuggling-three-pages.json`。
Android R8源码备份逐文件解包校验后精准同步7个增量文件，50文件与冻结源一致，
原17项删除保持不变。完整160项脚本自测及真实Qwen收据校验通过，专用容器已开始
本地4并发构建，静态分析保持开启；尚无新APK或安装结果，Windows及Pixel门槛未关闭。

以下为 **2026-09-07 17:49 UTC（本地09-08 01:49）** 的检查点：R8完整原生回归
已于17:37:04单轮零重试通过493/493（144项core、87项common、182项browser-unit、
80项browser），原始结果与冻结源一致；测试收据摘要为
`d7ab8d36bc92347476966a613e1701b14c74fbe2d26586a16ef210bcd3826c7f`。
独立R8测试App于17:38:01启动，未替换日常App，签名整包摘要为
`c74b83fb91218ec5f44021ab96e92ee28d81e20dcbf08829eb33b0307c9102ec`。
真实Qwen35B完成监控规划，但网页未加载，17:40:25安全停止，监控数仍为0，
本次明确失败，不是自动化验收通过。手动打开第二个本地测试网页也未收到HTTP请求，
500条合成书签HTML导入仍显示等待，尚未执行分类复测。独立命令行HTTP200仅用于
确认测试服务可达，不替代浏览器证据。浏览器只读采样89/89次显示一个后台线程在
系统钥匙串读取中等待；同期系统授权进程存在，但没有读到授权框，不能认定最终根因。
计算机操作工具明确禁止访问该系统授权应用，未绕过限制、输入密码、修改钥匙串或
关闭安全检查。保留App及导入现场，需用户手动检查系统提示后再验证。
网络日志导出尚未启动成功，输出为0字节，不作为诊断证据。失败记录为
`macos-page-evidence-ui-r8/monitor-first-attempt.json`，等待记录为同目录
`keychain-wait-observation-r1.json`。Android R8安全脚本160项自测通过，但缺少R8
真实Qwen书签收据，仍未同步、构建或安装；Windows及Pixel实机门槛保持未完成。

17:52 UTC补充：R8启动包装器同一句柄与主进程仍在运行，但计算机操作返回Mac已锁屏，
未尝试绕过解锁或系统授权。Android R8首次真实只读预检查退出0，验证原生493项、
Android旧50文件与17项原有删除、冻结新源及专用容器停止状态；新旧源差7文件。
预检查明确返回`execution_ready=false`、`mac_qwen_receipt_required=true`，没有同步源、
启动构建或安装。记录为`android-page-evidence-r8-preflight-r1.json`。继续验收需先解锁Mac，
并由用户手动检查系统钥匙串提示；不能将进程仍存活或预检查通过当作真实场景通过。

以下为 **2026-09-07 17:34 UTC（本地09-08 01:34）** 的构建检查点：R8仅修正测试清单，
清单摘要`8129ef6ced66744e3ef16960a3a80853ffb9d9363fecc56ee25c9754edc60559`，全部50个
源码文件与R7逐字节相同。R7构建17:28:41成功（5分15秒），整包摘要
`332eee58553402fb0c82190355d278358d7b09d0db3ef192867248738a3bc657`。R7原生回归
144项core、87项common、80项browser通过，新增四种预算场景通过（2174毫秒）；
但browser-unit因漏计同源测试2项，在执行前被清单校验拒绝，整体结果明确为失败。
原R7清单和失败收据均保留，未修改产品或删测试。正确集合为493=144+87+182+80；
R8验证构建17:33:36退出0、实际0步无需重编译，所有程序及整包摘要保持不变。
当时R8完整493项单轮零重试回归正在执行，尚未启动独立App；后续结果见顶部，
Qwen实机门槛未关闭。

以下为 **2026-09-07 17:23 UTC** 的R7冻结记录：R7预算修正候选已冻结，
50文件清单摘要为`d0f4a11e497ed4469e24a7e917959c4f5f98beffa948efcd466733b6bf1eefa4`。
原生定向测试已真实复现默认500总网络预算扣除20次
模型开销后只剩480次网址检查；原失败程序与JSON均保留。模板现改为500次网址请求
加模型预算（当前总520），旧任务或收紧为500的授权不扩大。完整144项核心回归
于16:56:46单轮零重试通过，证据为`macos-bookmark-budget-green-r1/green-result.json`；
这不代表浏览器Runtime或真实Qwen已经通过。完整规划→授权→500次HEAD→最终结果
双预算浏览器回归已于17:07:42真实完成：默认520场景通过；收紧为500时准确检查497条、
留下3条未检查，但任务被标为失败且丢失完成摘要，故该原生测试明确失败。
证据为`macos-bookmark-runtime-red-r1/red-result.json`，原失败程序、原始JSON及被测源码
均保留。主目录service已补预算耗尽后的既有浏览器核验回退，仅在计划步骤全完成、
无待批动作及具备必要页面证据时保留结果，不扩大预算；仍待编译与完整491项回归。
同时补充模型预算仅够执行检查、以及不足以执行检查的两个边界场景，测试名称保持不变。
MAC物理镜像已在备份后同步更新browser测试及service，全部50文件与主目录/冻结源一致，
相对R6精准修改4文件；R6程序和App已分别备份，冻结快照及历史收据未改。准备与同步
入口各38项无副作用自测通过，完整491项原生回归尚待运行。17:20桌面已可读取界面，
本地模型服务列出指定Qwen35B；尚未启动R7独立App或实际调用模型执行本候选任务。
Android仍为R5旧50文件且停止，Windows与Pixel门槛未关闭。

17:05 UTC只读核对发现旧R5验收App的启动包装器已于16:42:40退出0、无信号，进程不在；
日志为空，退出原因尚未确定。没有自动重开旧App或读取锁屏后的资料目录。此前16:19
暂停网络观察仍是有效的有界记录，时点后的可见界面确认尚未补齐。

以下 **2026-09-07 16:21 UTC（本地09-08 00:21）** 为已验证的R6基线，不代表当前修改后源码：50文件的R6候选
`928243f8c21bc395fa61b14f73290ba4674e121013d318e6599df7c903b591b0`已冻结并同步到
macOS镜像，完整原生构建于16:17:18 UTC退出0，源码及日常App前后未变，整包摘要
`9bfdbcb83b8cc35f1da6557653eec3c01a68cfd6f5f74a7342b559991e0b5e71`。
488项回归于16:20:44 UTC单轮零重试全部通过：142项core、87项common、180项
browser-unit、79项browser。新增500条分类正常收尾/格式失败回退双路径用例通过
（1150毫秒），35秒慢页面用例通过（36066毫秒）。构建收据摘要
`b69994db77435ab5e99aed5158ee1ad4141fa6bfaac7c2f2804c6569d1eb8d9b`，测试收据摘要
`fd3edecaf5c991c766e6c386f5fc3d4b6c39a2c26913f3dce7c8c7e50a01868b`；原始证据位于
`macos-page-evidence-r6/`和`macos-page-evidence-tests-r6/`。
真实Qwen复测仍待Mac手动解锁，尚未签名或启动R6独立App。本轮仅修改
4个文件，包含完整预览分类计数/真实样本、浏览器最终报告核验、6项核心回归和1项
双路径侧栏回归。`quality:fast`通过，候选/暂存/测试/启动脚本自测分别2/6/17/17项
通过。沿用此前确认的有限Docker维护，正常关闭并恢复Desktop，147条容器身份
完整保留，句柄从144239降至12559，无删除、强制退出或系统配置修改。Android保持停止。

R5监控重启后的第二轮于00:03:04.946真实GET，准确显示续航24→18小时、保修36→24个月。
00:03:51通过界面暂停，00:19:20核对已越过原00:18:06时点约75秒，App与测试服务
仍在运行，服务端没有新增请求。由于Mac锁屏，时点后的界面状态尚未复查；只记网络
非访问观察，不关闭完整暂停界面验收，也没有系统通知送达证据。记录为
`macos-page-evidence-ui-r5/restart-r1/pause-deadline-network-only.json`。以上仅属于旧R5
同包独立监控结果，不替代R6书签修复验收。

只读核查还发现500条网址检查的容量缺口：默认管家模板总网络预算500，同时扣除
模型请求，现有500条直接工具测试没有覆盖这部分开销。后续需补完整Runtime回归；
本轮未改冻结输入，也未向外部站点发送500次请求。详见
`bookmarks-500-network-budget-review-r6.md`。

16:46 UTC新增Android R6脚本准备：已修正停止守卫的两个精确工作目录、完整七项
参数及PID=PGID=SID校验，并区分资源停止与外部信号。补测先复现异常参数尾部、
记录写失败阻断发送、异常附带成功收据仍重发三项问题，再修正为明确拒绝、不中断
安全停止流程、识别成功收据，以及结果未知时不自动重发。146项脚本自测通过，含
43项真实Shell控制流的内存模拟；没有发送真实信号。只读预检查重新核对488项
原生结果及Android旧50文件；缺少R6真实Qwen收据时不允许同步或构建。证据为
`android-page-evidence-r6-script-acceptance.json`及两份失败收据。真实Linux信号链路
尚未验证，旧R5脚本和所有历史证据保持不变，没有重启Android或安装APK。

此前最新已构建候选为50文件的
`page-evidence-candidate-r5`，清单为
`e78b534b7dc04bb4b63825d549bb0524b21f2556060214ecbda775d1670a8e6b`。
主目录、冻结源和 macOS 镜像全部50文件一致，相对R4精准修改8个文件。
完整构建于15:17:04 UTC退出0，整包摘要为 `87a4c3a9…`，资源守卫和日常App保护
检查通过；8项定向原生回归及 `quality:fast` 全部通过。481项完整原生回归于
15:20:04 UTC单轮全部通过：136项core、87项common、180项browser-unit及78项browser。
35秒慢页面原生用例用时36257毫秒，新增内容回退三分支及侧栏“部分完成”用例通过。
独立R5 App已启动，签名整包摘要 `8438eb04…`，没有替换日常应用。首次真实Qwen
慢页面请求约38秒后才到达服务端，随后35秒响应超过60秒产品等待门槛，界面明确停止，
没有误报完成；保留 `macos-page-evidence-ui-r5/slow-page-first-attempt.json`，不计为
标准慢页面通过。随后从空白页受控重试成功，请求约20毫秒到达，35秒延迟结束后才
完成正确摘要。否定句原句、仅查询标签标题及英文翻译也通过真实Qwen复测，4项收据
保存在 `macos-page-evidence-ui-r5/qwen-page-evidence-receipt.json`，不代表全部十项通过。
独立15分钟监控于23:48:04.851本地时间真实发起GET并返回HTTP200，完成首次基线读取。
正常退出后，用同一签名App、原资料及六个参数重启，监控数1、间隔15分钟及下一次
00:03:04均保持不变；随后变化摘要已验证，暂停不访问及系统通知仍待验证。只读数据库检查记录目标密文
35字节和观察密文515字节，没有改动调度。证据为R5 UI目录的`monitor-first-tick.json`
及`restart-r1/`。
500条合成书签已通过原生HTML导入，真实Qwen选择了全部500条并生成整理预览；
但正式运行配置禁止检查本机地址，实际URL检查为0条、范围阻止500条，界面明确
显示部分完成。未开启夹具豁免，不计作450条有效/50条失效判别通过。事后核对
500条标题、URL和单一文件夹均保持原样；证据为
`macos-page-evidence-ui-r5/bookmarks-500-qwen-partial.json`。后续纯标题分类真实复测失败：
模型编造“开发496条、研究4条”和不存在的代表书签，仍显示已完成。此候选不能通过
常用场景验收。失败保存于`bookmarks-500-title-false-completion.json`，正在修正完整预览
统计与最终摘要核对，未用提示词重试掩盖失败。防护真实行为仍待验收。
Android R5在发现上述缺陷后受控停止，15:49:45 UTC退出1，源码保持不变，未生成或安装
新APK；容器和增量缓存保留。首次中断守卫因Siso实际工作目录进入out而拒绝信号，
随后逐项核对精确PID/会话/进程组/命令与out目录，仅中断本轮进程组；详见
`android-page-evidence-r5/interrupt-resolution.json`。Windows仍待安全告警状态核查。

### R4 检查点及失败复现（历史证据）

2026-09-07 14:35 UTC检查点：当时最新已构建候选是 50 文件的
`page-evidence-candidate-r4`（清单 `5c07966d…`），R2至R4仅修正慢页面测试驱动的
编译与嵌套等待问题，产品行为代码不变；R1 相对旧49文件候选修改9个文件。
旧49文件 App 已实际显示正确的监控变化摘要，但35秒慢页面任务在正文返回前就报完成，
并编造了页面不存在的内容，因此旧候选不通过。修复已在主目录和 macOS 镜像中落地，
R2、R3完整构建成功，但473项回归均为472通过、慢页面用例超时，不能记为整体通过。
R4 完整构建退出0，473项原生回归单轮全部通过；其中35秒慢页面用例实际用时
36220毫秒。14:27 UTC，独立 R4 App 通过真实 Qwen 35B 原句实测：空白起点打开
35秒延迟页面，正文未返回时持续执行，22:27:55本地时间才完成，摘要正确给出续航
24小时、保修36个月，来源为实际 `/slow` 页面，工具为 `page.observe` 和
`page.extract`。证据为 `.artifacts/v2-final/macos-page-evidence-ui-r4/slow-page-qwen-observation.json`。
并行审查随后发现原生标题查询、否定句衔接、翻译、繁体工作流及缺少内容结果时的
回退边界问题。14:32 UTC真实Qwen进一步复现：在已有正文的页面输入
“不要整理书签，总结当前页。”后，仅执行 `tab.list` 却完成任务，并声称没有正文。
失败证据为 `.artifacts/v2-final/macos-page-evidence-ui-r4/negated-prefix-false-completion.json`。
当前开发源码正在修复，因此R4的慢页面单项通过不能授予后续源码，也不能宣布
全部常用场景通过。新修复尚未构建验收；原生标题查询应保持元数据读取，而已绑定
网页的内容任务须检查实际读取回执，内容结果生成失败时显示“部分完成”。
此前大型链接因系统文件句柄耗尽失败；用户要求继续后，实时检查已无运行
容器、系统句柄降至约1.14万，因此直接启动受资源保护的完整构建，无需再退出 Docker。
框架链接已完成；第一轮完整构建随后因新增测试的回调编译错误退出1，该处已修复。
未调高系统限制，旧失败记录均保留。
Android 旧候选构建已确认中断退出，源码与缓存保留，尚无新 APK，也尚未同步新50文件
候选。Windows 安全告警、Pixel 独立测试资料及系统通知门槛没有关闭。完整证据见文末；
以下早期检查点仅作历史记录。

### 较早检查点（2026-09-06）

截至 2026-09-06 08:32 UTC，最新已构建的冻结候选仍为 `monitor-summary-candidate-r2`：在下述提交基线上叠加 47 文件增量，包含网页变化 AI 摘要。独立 macOS 正常整包及全部 452 项原生回归通过，后续真实 App 从空白页经本地 Qwen 完成续航、保修摘要；但创建监控后正常退出、重启，首次真实到点只推进时间而没有读取网页，定时闭环不通过。已在开发 overlay 修正入口误分类、模型保存反馈及监控计划收尾，新增权限 Core 回归 119 项通过；这些增量还不在当前 App 或正在构建的 Android 中。Android 仍在同一 47 文件构建进程，没有新 APK；上一轮 44 文件失败已保留。canonical 仍保留上一 44 文件候选，Pixel 仍是旧安装包，Windows 仍待人工核查传输安全告警。不能将基线提交、增量源码、原生通过和实际安装版验收混为一谈。最新证据与十项门槛见文末。

08:46 UTC 追加：已将修正固化为 49 文件的 `monitor-registration-candidate-r1` 源码快照，
并绑定下一轮必须执行的 458 项原生测试清单。该候选尚未构建或完成整套测试，运行目录
仍保留前述 47 文件版本。新的持续运行对照 App 已启动专用空白资料，但 Mac 再次锁屏，
尚未配置模型或创建监控；不能将启动进程记为新的定时验收通过。

09:02 UTC 追加：49 文件候选已备份后同步到独立 macOS 验收源码目录，九个变化文件
经补丁应用，全部 49 个文件逐一校验通过。Android 输入仍为 47 文件 R2；macOS 二进制
也仍是旧 R2，尚未开始新整包构建。构建前检查确认资源门槛未满足，未绕过守卫。

候选 Chromium 提交基线为 `383d157c6f3601101038aef6ff964e61b6af7b4f`，
tree 为 `2c509e07ec25fa8811adae17f9826e967c9bcee1`，包含 106 个顶层补丁。
新增 0106 修复 Windows 界面线程读取语言资源导致的断言崩溃，并补充浏览器回归测试。
旧候选 `1e1341b51e3254d4638bc1917b140f30d4c1e9d7`（tree
`babd10e2e757d7b57f1b7ef18eac26ee5becc9cb`）的通过记录仅为历史证据，不能直接
授予新候选验收资格。下表原有产物测试均属于旧候选或更早版本，须逐项复测。

### 早期候选进度（历史记录，最新状态见文末增量）

- Windows 已对齐最新提交和源码树，实际日志完成 10 个编译/链接步骤；构建封装未可靠
  取得退出码，因此保留失败状态，采用显式等待方式另起 R2 复核构建和原生测试。
- macOS 修改的四个产品源文件已编译；ThinLTO 链接因系统文件句柄耗尽失败，旧应用
  和清单已经恢复。当前默认路径的应用仍为旧候选，不能与最新源码混称同一产物。
- Android 原构建也因系统文件句柄耗尽停止，尚未产生最新 APK。已停止本任务的专用
  构建容器，保留源码、对象缓存和失败记录；待资源回落后串行恢复构建。
- 未重启整个 Docker、调整系统全局限制或关闭任何安全防护。Windows 可见界面测试
  仍需移除已报告的安全弹窗；macOS 仍需解除锁屏，不能用无界面测试替代。

## 本轮实际验证

| 项目 | 结果 | 证据边界 |
|---|---|---|
| Windows HTTPS WinRM 5986 | 已连通并认证 | 已实际上传候选源码、启动构建并取回结果；未改变服务器认证配置 |
| Windows 原生构建 | 通过 | 0106 构建完成 41 个步骤；最终界面 0107 增量构建完成 11 个步骤，退出码 0 |
| Windows 旧候选源码身份 | 曾对齐 | 对应旧候选 1e1341；仅保留原有非构建文件 `tools/gn/README.md` 的修改；最新状态见上节 |
| Windows 最终窗口验收 | R23、R24 未通过 | R23 在接收焦点时停止；R24 截图仍有防火墙弹窗，等待菜单超时；保留两轮失败，不能计为产品入口通过 |
| Windows 共享验收脚本自测 | 通过 | 实际进程收到单个完整 host-resolver 参数；窗口辅助 C# 代码编译通过，不等于界面操作通过 |
| Windows 原生单元测试 | 80/80 核心、141/141 浏览器通过 | 使用单进程方式；不代表真实窗口验收完成 |
| macOS 原生单元测试 | 80/80 核心、146/146 浏览器通过 | 独立于真实界面与公开站点网络结果 |
| macOS 最终界面增量 | 编译与本地签名通过 | 包含页面不可用的明确提示；该失败界面尚待可重复 fixture 验证 |
| macOS 同提交应用包重建与身份绑定 | 通过，仍为内部诊断资格 | 冻结输入后完成 77 个增量打包步骤；前后输入及构建图一致，整包哈希与前次候选一致；随后重新通过 80/80、146/146 原生测试 |
| macOS Agent 入口 | 通过 | 从指定 `.app` 的工具栏按钮打开侧栏，使用 `Aegis v2 QA 8e99` 隔离资料 |
| macOS 本地模型定时任务 | 创建、暂停、重启后保留、恢复通过 | IANA URL 每小时检查；未等待真实一小时触发，测试任务最终保持暂停 |
| macOS 自动化状态 | 通过 | 真实界面已显示“等待定时检查”和“已暂停”；暂停后不再显示正在执行 |
| macOS 本地模型摘要 | 一次失败、重试通过 | 首次网页未就绪，30 秒有界等待后停止；网页后来加载，重试于 15:01:46 完成读取、三点总结和核对 |
| macOS 指纹保护真实运行 | 7 组、346/346 断言通过 | 最终 `.app`、11 次隔离启动，覆盖 WebGPU、Canvas、WebGL、Audio 和 Worker；包含关闭、站点暂停、重启及不同 Profile 对照，无可见窗口操作 |
| macOS 反钓鱼真实运行 | 拦截与关闭保护对照通过 | 跨站密码表单触发中文安全拦截文档；测试进程关闭保护后显示原始表单，未填写或提交表单 |
| macOS 跟踪参数清理 | 开启与关闭对照通过 | 开启时删除 `utm_source`、`fbclid` 且保留 `keep=yes`；只在测试进程关闭清理时保留完整原始参数 |
| 共享代码质量 | 通过 | 最新 `quality:fast`，25 个测试文件、163 项测试；状态测试 20/20，页面错误提示测试 2/2，中转自测及传输余量参数测试 3/3 |
| 本地 Qwen 协议预检 | 24/24 通过 | 两轮 route/plan/execution 请求；其中两次收藏夹计划依赖浏览器既有只读恢复，不是模型首次输出全部正确；不是窗口端到端测试 |
| Windows 实际调用本地 Qwen | R2 12/12 通过 | Windows Node 经既有 WinRM 通道调用 Mac 的指定本地模型；三份计划均首次通过，13 次请求均返回 200；不是浏览器窗口端到端测试 |
| 临时模型中转 | 自测与退出检查通过 | 仅监听 Windows `127.0.0.1:18768`，不接受密钥；结束后监听和所属 Node 进程均为 0，本地工作进程已退出 |
| 慢网页与路由 fixture 自测 | 11/11 通过 | 35 秒延迟场景、非法延迟拒绝、客户端取消，以及无 URL 目标的模型路由；界面行为须另测 |
| Android | 构建因资源耗尽停止 | 当前安装的旧 APK 不计入本候选验收；须对齐 383d157c 后增量编译，再安装 Pixel 真机 |

指定模型为 `Qwen3.6-35B-A3B-Uncensored-Heretic-MLX-4bit`，通过
`http://127.0.0.1:8000/v1` 访问，无模型密钥。服务器密码未写入源码或测试脚本。

测试中转源码为 [agent-winrm-model-relay.mjs](../../apps/browser/scripts/agent-winrm-model-relay.mjs)，
不是浏览器产品组件，也不是持久部署服务。自测入口为
`pnpm --filter @gcsa-aegis/browser test:model-relay`。远程预检显式使用
`--transport-grace-ms 30000`；不应将带中转余量的耗时当作本机模型速度或产品超时验收。

## 保留的失败及处理

1. Windows 计划任务启动的多进程测试触发 `!IsProcessBackgrounded()`，所有子测试
   未形成有效结果。保留该失败日志，再采用已有单进程运行方式，得到上表通过结果。
2. macOS 摘要首次失败时，同一程序手动打开网页也一直加载，但命令行直连及经本机代理
   访问均返回 200。随后两个浏览器标签页都加载成功。底层延迟原因尚未查明，不能宣称
   网络问题已修复，也不能把一次重试成功当作稳定性验收。
3. 网络导出选择了“移除私密信息”和 5 MB 上限，但记录未实际启动，生成文件为零字节。
   该文件不是有效网络诊断证据。测试程序已正常退出，不继续采集。
4. 页面不可用的错误已改成说明可能的加载、关闭或跳转原因及重试方式，不再笼统指向模型。
   macOS 最终文案已编译并通过本地签名校验；Windows 增量构建也已完成，真实故障界面复测尚待完成。
5. Windows Defender 将 Git bundle 传输中的一条编码 PowerShell 追加命令标记为
   `Trojan:Win32/Commando.A!ml`。已停止重试该传输，未关闭防护、添加排除项或恢复被拦截内容。
   该告警不能直接解释为浏览器感染，也不能无依据判为误报。随后仅核对服务器已存在的源码
   文件哈希，通过普通 Git 操作和明文提交元数据对齐源码身份，没有执行被拦截的传输内容。
6. R23 界面验收在 `SetFocus` 处失败。原始截图显示另一个程序 `guard_localapi` 的防火墙
   授权弹窗；未批准该程序联网。另有独立脚本缺陷：带空格的 host-resolver 参数被拆开，
   导致启动时出现了本不该打开的标签页。修复后的参数已用服务器上的真实 Node 进程验证
   为一个完整参数。随后核对历史实测脚本，发现焦点与控件定位修复未同步回共享脚本；
   已将必要修复合回，未复制旧脚本的宽泛弹窗取消逻辑。
7. 新 R24 脚本保留所有新状态检查，并新增慢网页停止后的清楚提示和重新启动任务、无 URL
   输入的模型选页流程。显式 URL 任务的路由本来由浏览器解析，测试现在据实检查模型计划
   和执行，不再强求该路径调用 `agent.route_goal`；无 URL 场景单独检查该调用。
8. macOS 在本轮追加界面验收时已锁屏，Computer Use 无法自动解锁。须手动解锁后继续；
   Windows 安全弹窗也已请求用户取消。锁屏和弹窗期间不生成虚假界面通过记录。
9. R24 已修复启动时错误打开标签页，实际起点为 `about:blank`；但截图显示防火墙授权
   弹窗仍覆盖程序。Windows 返回默认输入桌面与浏览器前台焦点，不代表画面没有被弹窗遮挡。
   最终于 07:54:46 UTC 等待菜单超时停止；不继续自动重试，也不把该失败解释为已证明
   产品没有入口。须移除界面阻挡后，在新的证据目录重新验收。
   已核实 R24 创建的浏览器与 fixture 进程均退出，未触碰其他程序的进程或防火墙授权。
10. macOS 追加二阶段身份绑定后，使用另一个工作目录默认的 Node 25.2.1 复核曾被拒绝。
    与构建后快照逐字段比较，唯一变化为 `build.host.node`：构建时使用 Node 24.14.0。
    未修改清单或削弱校验，改用同一 Node 24.14.0 后，固定清单摘要、源码、构建图、补丁、
    overlay 和整包哈希均通过复核。保留错误日志；原生单元测试没有改变源码或应用包。
11. 本次重建完整保留了旧应用和旧清单。新应用仍位于原 `out/AegisRelease/GCSA Aegis.app`
    路径，整包 SHA-256 为 `9ac305eff356141c11fbe22ef51e0d869c5bb75f19634ddc0e00abde15c7cad4`。
    这是增量重建与本地协作流程证据，不是从空目录编译的可重复构建或受信任远程证明。
    集成仓库仍有未提交修改，因此仍标记 `diagnostic-only`；没有升级为候选发行或正式发行。
12. Windows Qwen R1 在模型发现的 5 秒时限内没有接上中转控制循环，客户端约 4953 ms
    后关闭请求，未进入 Agent 执行。失败状态和中转元数据均保留；该轮错误日志为空，不能
    作为具体异常栈的证据。R2 在预检脚本中显式增加 30000 ms 传输余量并记录到报告，
    默认直连时限和浏览器产品时限均不变。R2 于 08:52:06–08:53:53 UTC 完成 12 项
    语义检查，包含点名京东、cnBeta、收藏夹只读计划及观察/提取/完成/定时创建工具协议。
    这证明指定 Windows 主机能发出并接收真实 Qwen 请求，不证明浏览器实际完成了这些操作。
13. 中转只用于隔离验收：限定两个模型端点、指定模型、请求大小、队列、单次时限和
    15 分钟总寿命；控制请求必须携带专用标头，拒绝浏览器来源、Cookie 和认证头。
    通过 WinRM 在内存中转交合成验收数据，不开放公网模型端口，也不连接日常 Profile。
    中转报告仅保存大小、摘要、耗时和状态，不保存完整提示词或原始响应。R2 完成后
    四个无触发器测试计划任务均非运行状态，中转监听及所属 Node 进程均为 0。
    临时改变的本地控制终端模式已恢复，未改动 Windows 防火墙或认证配置。
14. macOS 指纹保护测试实际使用最终候选，构建身份在运行前、后与报告发布前均通过
    固定摘要复核；346 项断言全部通过，11 次浏览器启动均正常退出且无所属进程残留，
    没有新增或修改的崩溃记录。返回码为 2 表示行为通过但不具备发行资格，不能写成
    测试失败，也不能写成已具备正式签名或发行资格。全部测试使用新建隔离 Profile。
15. macOS 反钓鱼 R1 使用英文预期文案而未通过；原生日志已记录页面风险评分 94 与
    导航拦截。按实际中文文案重跑，R2、R3 均进入 `chrome-error://chromewebdata/`，
    标题为“疑似钓鱼网站”；关闭保护的独立对照显示原始登录表单。这不是降低检测阈值
    或修改产品行为，原始失败保留。R3 同时验证了最新测试脚本的崩溃覆盖范围声明。
16. 多站点验证器原先无条件查找 macOS Crashpad 路径；在 Windows 上，目录不存在
    不能证明全局无崩溃。现明确标记 Windows 全局崩溃目录未覆盖，只检查测试 Profile
    与进程日志。此修改仅影响验收报告，不改动浏览器防护或系统配置。
17. Windows 无界面防护 R1 因 PowerShell 5.1 数组被重复包装而在产物校验前停止；
    原生诊断确认原始清单有五项，修改读取方式后仍严格要求五项路径和哈希完全匹配。
    R2 通过校验并启动真实浏览器，但连接 CDP 时触发 `ScopedDisallowBlocking`
    断言。完整栈指向 Aegis 自定义远程控制提示条调用 `GetApplicationLocale`，在
    Windows 上检查语言资源文件并发生磁盘读取。测试 Profile 的崩溃转储与日志均保留，
    不能把该轮当作隐私/反钓鱼测试通过，也不能仅凭无界面崩溃推断普通窗口复现。
18. 补丁 0106 使用浏览器进程已缓存的语言设置，保留远程控制安全提示条，并修正
    相同模式的 Aegis 防护页、摘要准备与设置入口；新增两个原生浏览器回归用例。
    独立临时索引从旧候选重放该补丁得到最新候选的精确源码树；测试尚待新产物运行。

## 本地证据

以 `/Users/lazy/Projects/GCSA-aegis/.artifacts/v2-final/` 为证据根目录：

- `windows-protection-headless-r2/evidence.zip`：保留的真实 Windows 崩溃测试，SHA-256
  `36e8420b25e16e5364624e6b9287ee13e0a7b37e991964433b7bd202c251f637`。
- `windows-protection-headless-r2/chromium.log`：完整错误栈，SHA-256
  `2e8926e54690599a5b6f9dfe3dd18fff8cee076c6b3dc3ac29e8ecb4415e9471`。
- `windows-source-383d157c.zip`：明文源码补丁与构建脚本，SHA-256
  `9190df260e92650b4c4cacfc97813af5a39cae0cd513325d301ee943c36fe50b`。
- `macos-383d157c-bound-build-20260905/`：保留的系统句柄耗尽失败；旧应用恢复记录。
- `macos-0106/core-tests.log`：80/80。
- `macos-0106/browser-tests.log`：146/146。
- `macos-privacy-1e1341-r1.json`：346 项真实运行断言及三阶段固定身份复核，SHA-256 为
  `fde9b481968dae8116700ba57285c4b5071920c5b58bc982f4e57a1a737d56bc`。
- `macos-phishing-1e1341-r1.json`：保留的英文文案不匹配失败。
- `macos-phishing-1e1341-r3.json`：中文安全拦截通过，SHA-256 为
  `2cc71a30d74483d280bc86aa5345cd643d30e2274646bb9cc88378121951f408`。
- `macos-phishing-1e1341-off-control.json`：关闭保护对照，SHA-256 为
  `0009e9e795a1d792aea0a04c93d42b736a52cf8a23d24fd1b5ccb180cd6b6e22`。
- `macos-tracking-1e1341-on.json`、`macos-tracking-1e1341-off-control.json`：清理开关对照，
  分别为 `76e781591b9533611cfcf7adc939b27b58bdd73a56617d22f50badcbe707de7f`、
  `101f8687846da61f1ab055135e9c120588fc0d0936ed1b4baec76222fa4dace5`。
  同前缀的 fixture 请求与就绪文件保留；对应服务、浏览器进程均已退出。
- `windows-0106/aegis-tests-0106-direct.json`：Windows 80/80、141/141 及二进制哈希。
- `windows-ui-0107.zip`：最终界面增量，SHA-256 为
  `351009bc1dfa17c8de8533345700f2af05f716393b9ff5355711fe48cdc6cfc3`。
- `chromium-1e1341b51e.bundle`：候选提交，已执行 `git bundle verify`。
- `windows-ui-1e1341-r23/failure-evidence.zip`：保留的 R23 失败，SHA-256 为
  `a3c9c9b26f48978dce60656bd8ddb7729d5bdf64c60d38bebb87f4e20b6b5653`。
- `qwen-local-model-1e1341b51e.json`：两轮 24 项协议预检及只读恢复边界。
- `windows-qwen-r1/`：模型发现超时的失败状态及中转元数据，保留空错误日志的边界。
- `windows-qwen-r2/model-preflight.json`：Windows 12 项真实 Qwen 协议检查，SHA-256 为
  `e9d073cbe883fce115b105ce2dc1632544be63beafc4b2ef54acd30ca5aa2dbd`。
- `windows-qwen-r2/relay-metadata.json`：13 次请求均 200、无待处理请求，SHA-256 为
  `9bec78a3cb200a2a523e6b5927b111a32c8fc128cf9931f214f83cb467adc5d6`。
- `windows-qwen-r2/status.json`：正常退出记录，SHA-256 为
  `319054534f9f672a177463f8da84f27fea56be00080e7eefacb90ed82fe387d4`。
- `qwen-relay-1e1341-r2b.zip`：本轮实际使用的测试脚本，SHA-256 为
  `10e1392c6953b3cbed4ee4c28bcf75decfbfba6c439d13184a8301526a03a879`。
- `fixture-v5-routing-self-test-1e1341.json`：11 项 fixture 自测，非产品验收。
- `android-final-source-1e1341.tar`：从冻结提交导出的最终 13 个文件，逐一 Git 内容哈希
  比较为 13/13 一致，SHA-256 为
  `bb8106df5f2b80744788505289c177c2d049ac095842e474df47b4f25ced8b3f`。
  该归档属于旧候选，未覆盖 Android 源码；最新同步须改用 383d157c 的完整差异文件。
- `windows-ui-1e1341-r24c.zip`：待复测的共享脚本与执行器，SHA-256 为
  `b6e368b9ef15e039990805d65db969f65e4f038b2dc51863dea93a4a7ca7c877`。
- `windows-0106/final-1e1341-artifact-hashes.json`：服务器最新可执行文件、DLL 及资源包的 SHA-256。
- `windows-ui-1e1341-r24/ui-evidence.zip`：R24 未通过的真实界面记录，SHA-256 为
  `61b5914773fda3a0f6bc77af639333e41bf26162be76542974f5856bc1a20e42`。
- `windows-ui-1e1341-r24/script-test-report.json`：独立的脚本自测通过记录，明确标记
  `ui_interaction_tested=false`，不能与上项失败报告混为同一结果。
- `macos-0106/1e1341-artifact-snapshot.json`：本地应用目录和源码的事后快照，标记为
  `diagnostic-only`、`sourceArtifactBinding=false`；不能伪装成可信构建证明或发行资格。
- `macos-1e1341-bound-build-20260905/`：后续更强的二阶段本地构建证据，包括构建前清单、
  构建后清单、77 步构建日志、本地签名、固定摘要复核，以及重跑的 80/80、146/146 测试。
  构建后清单 SHA-256 为 `0b5b3942c688987741ac372b08773e116a9abac1089de0f56b2fa20dbfce574b`，
  `localSourceArtifactBinding=true`，仍有 `trustedBuildAttestation=false`。
  同目录下的 `previous-GCSA Aegis.app` 是完整旧应用备份，尚未删除。

补丁 0103–0105 已在独立临时 Git 索引中从既有 0102 提交
`13807aaf086948bdff0370e356718d0c2ac54d27` 重放，得到与候选完全相同的
`babd10e2e757d7b57f1b7ef18eac26ee5becc9cb` 源码树；没有改动工作区文件。

macOS 真实操作与截图保存在当前任务的 Computer Use 工具记录中，不能替换成模拟截图。
macOS 使用本地临时签名；没有 Developer ID 签名或公证，不具备公开发行资格。

## 下一步验收门槛

以下十项保持原定范围，不以较易通过的测试替代实际使用结果：

| 标准 | 必需证据 | 当前仍需补齐 |
|---|---|---|
| 1. Android、Windows 入口可发现 | 最新安装产物的可见入口和成功打开的完整任务页 | 两端最终候选真实窗口/真机 |
| 2. 无需预先打开网页 | 空白起点到目标页的完整任务记录 | Windows、Android，以及慢加载稳定性 |
| 3. 理解—规划—执行—观察—验证 | 模型工具调用与实际浏览器结果相互对应 | 两端最终候选完整流程 |
| 4. 小格式错误有界恢复 | 错误输出、一次修复、权限不扩张及最终结果 | 窗口内错误 fixture，不仅是解析器单元测试 |
| 5. 常用任务可用 | 收藏夹、摘要、点名站点、官方下载及定时任务的真实结果 | 各平台完整场景矩阵，定时触发证据 |
| 6. 隐私与反钓鱼生效 | fixture 的跟踪参数/敏感值处理与真实拦截页面 | 三端最终候选行为记录 |
| 7. 指定真机与主机测试 | Pixel 安装身份、Windows 主机窗口及交互结果 | 最新 Pixel 安装及 Windows UI 验收 |
| 8. 凭据和浏览资料不泄漏 | 授权边界、脱敏模型请求和日志/存储检查 | 最终跨平台运行记录，不能只凭代码推断 |
| 9. 新手看得懂并可恢复 | 规划、执行、等待、暂停、失败提示与实际状态一致 | 慢页面等失败条件的最新界面复测 |
| 10. 最终源码与产物身份一致 | 同一提交、源码树、平台产物哈希及重建记录 | Windows/Android 同步最终提交和产物；文档身份更新 |

本地 Qwen 的真实调用与可控模型 fixture 分别记录，不能把 fixture 响应称为 Qwen 结果。

执行顺序：

1. 完成 macOS/Windows 最终界面增量构建、签名/产物核对；统一源码提交及哈希。
2. 在 Windows 实际窗口重跑入口、摘要、500 条收藏夹、下载、自动化、敏感数据与反钓鱼
   fixture。自动化须验证“等待定时检查”，不能错误地等待一次性任务的“任务完成”。
3. Android 构建结束后同步最终提交，增量编译并安装指定 Pixel；重新验证完整 Agent
   页面，不能只证明原生入口存在。
4. 针对首次页面加载延迟和失败提示进行可重复的慢页面 fixture 测试。
5. 汇总十项标准逐项证据；所有标准有最终候选的证据后，才可标记目标完成。

## 23:25 增量：当前窗口标签统计（非最终候选）

已修正创建任务只绑定一个可操作标签、导致当前窗口统计只返回一个标签的问题。新增
浏览器绑定的只读窗口元数据范围；模型不能扩大它，也不会因此获得关闭或读取正文权限。
原生用例覆盖六标签、二十六标签与二十条明细截断、其他普通窗口和隐身窗口隔离，
以及额外标签关闭被范围校验拒绝。当前页摘要和收藏任务不获得额外窗口范围。

当前诊断结果为核心 88/88、浏览器单元 150/150、原生浏览器 46/46，集成快速检查通过。
本地 Qwen 在合成证据中分别回答六和二十六，完整原生完成处理流程通过；不等于真实
App 模型规划至浏览器操作的界面验收。首轮测试与模型原始引用断言失败均保留。

详细诊断记录保存在本地证据文件
`/Users/lazy/Projects/GCSA-aegis/.artifacts/v2-final/当前窗口标签统计修复-20260905.md`
（不随仓库发布）。
源码基线仍为 `383d157c`，十九个受控修改已与 overlay 对齐，未生成最终提交。
macOS/Android 十七个外部删除的测试夹具文件仍待恢复确认；没有将删除吸收进发行候选。
Windows 原构建仍在运行，尚未包含这些增量。默认 Mac App 保持旧包，不能作为新修复验收。
上述十项完成标准继续保持未完成。

## 2026-09-06 补充：部分结果界面、Windows 原生失败与 ASCII 走私防护

部分完成结果已贯通原生状态、Mojo 和 WebUI：显示“部分完成”、未完成项和对应时间线，不再将部分结果一律显示为成功；窄屏不再隐藏状态。该增量原生浏览器 47/47、核心 88/88、浏览器单元 150/150，快速质量检查通过。此前因 Markdown 证据链接导致的质量检查失败已保留并修复。

Windows `383d157c` 原生 browser_tests 已在 HTTPS 5986 WinRM 上编译完成，2845/2845，测试程序 SHA-256 为 `c8541da50da7494a9f9241df96061b40d4332a7989c5c433214cee61ab5365a4`。
实际运行结果是 45 项全部 CRASH，不能判为通过。日志显示关闭窗口时 `AegisToolbarButton` 释放悬空 WebContents 指针的检测失败；不能归咎于没有交互式桌面而忽略。证据保存在本机 `windows-browser-tests-run-383-direct-r1/`，其中 JSON SHA-256 为 `53357292115228a2156cf75a0fe710739150fb5abea21be0276a1fca2e99821b`。脚本启动曾被系统执行策略拒绝，未修改策略；后续采用 WinRM 直接启动原生测试 EXE，最终以测试 JSON/日志而非空 exit-code.txt 判断结果。

用户新要求对应的字符走私防护已在共享源码补充，详见
[ASCII 走私防护实现与验证](ascii-smuggling-protection-2026-09-06.md)。
该专题的 macOS 测试和真实界面通过，不代表整体跨平台目标完成。

## 2026-09-06 01:39 补充：Windows 关闭窗口修复候选

已将工具栏缓存的页面引用改为 `WeakPtr`，避免页面先销毁后的悬空访问；新增两项原生生命周期回归。macOS 同版本编译 72 步完成，通用防护 87/87、核心 91/91、浏览器单元 151/151、原生浏览器 53/53 均单次通过，快速质量检查通过。真实诊断 App 再次验证字符走私拦截、正常 Unicode 对照与菜单退出；独立进程正常退出，默认应用保持运行且主程序哈希未变。冷启动短暂空白与关闭窗口后的新标签现象仅记录，不据此授予完整稳定性资格。

Windows 已应用与 macOS 和 overlay 一致的 38 个受控增量，备份原有源文件及测试程序，保留非本任务修改和依赖文件。候选目录 `C:\a\ascii-toolbar-candidate-r1` 的编译及四套原生回归已启动，结果尚待返回；旧 45 项 CRASH 记录不删除，也不被此次“已启动”状态覆盖。没有改变系统执行策略、防火墙或内存安全检查。

同源码的 Android 最新 APK、Windows 可见界面、本地 Qwen 完整场景矩阵及最终产物身份仍未闭合；上述十项完成标准保持未完成。

Android 工作区已同步上述 38 个文件并通过前后哈希核对；修改前源文件已备份，17 项外部夹具删除未动。专用容器仍停止，未把源码同步记录算作构建或真机通过。

## 2026-09-06 后续：同源回归、摘要实测与定时缺口

当前冻结候选仍以 `383d157c` 为基线，包含 38 个受控文件，清单 SHA-256 为 `80a9b488d24fb1487a6b14e4680be3bf4827ae1064fec4d832680b70236005b2`。对应本机 `summary-locale-candidate-r1/`，不是新的正式提交或发行包。

- macOS：独立源码工作区完成新编译，通用防护 87/87、核心 92/92、浏览器单元 151/151、原生浏览器 54/54 均单次通过。最新二进制和测试集合见 `macos-cross-locale-tests-r1/tests-status.json`。
- 正文摘要：修复把章节标题当字段值的问题；保留主文档、脱敏、内容预算和来源绑定，新增未解析及截断标记。02:43:48–02:43:55，真实 App 从空白页在 Qwen 驱动下打开唯一允许的本地资料，正确总结两项正文事实；未启用 CDP、未搜索其他网站，记录见 `macos-source-acceptance-r1/summary-ui-result.md`。
- Windows：上一候选桌面原生结果为 50 成功、2 失败、1 崩溃。已修正中英文拦截提示断言与遮挡窗口动画帧等待；工具栏生命周期两项通过。新候选完成 10 步增量编译；03:15:17，桌面普通权限、正常优先级下的四套原生测试分别 87/87、92/92、146/146、54/54 单次通过。下载原始日志和 JSON 后严格核验集合与构建哈希，见本机 `windows-summary-locale-result-r1/`；这不是 Windows 安装版全部用户流程验收。
- Android：已恢复专用容器，编译同一冻结候选；静态分析开启，源码未在编译期间修改。尚无最新 APK 或 Pixel 安装证据，旧设备结果仍不计入。
- 原 canonical 与 Android 的 17 项删除未恢复；仅在全新 macOS 验收目录补齐基线测试资产。日常资料和默认 App 保留，没有变更 Windows 执行策略、Defender 或防火墙。

同源与测试集合综合核验记录为本机 `summary-locale-candidate-r1/verified-results.json`：四份本地源目录各 38 文件逐字节一致，Windows 上传清单与该清单哈希一致，两端八套原始测试均无漏项、重试或失败。Windows browser_tests SHA-256 为 `c8fd73b35934c6ec6575a9e58842b482080caa94669c3162eb19f4923b412eaf`，结果 JSON 为 `da3a826526caacc185083ce7b44409ff025213e373dc4baedc060b8ff3a4f9be`。共享 Core 168/168、类型检查、脚本/界面状态自测和完整快速质量检查重新通过。

### 定时任务真实到点失败：标准 5、9 尚未完成

02:45:40，真实 Qwen 创建每 15 分钟检查本地 `/status` 的只读自动化，唯一来源为 `http://127.0.0.1:50657`。应用正常退出后重启，03:00:12 界面保留原到点时间 03:00:40，证明本案例的保存、恢复和到点时间连续性。

03:00:40 实际检查失败，界面显示连续失败 1 次并退避到 03:30:40，本地 fixture 没有收到到点请求。源码 `AegisAgentService::ExecuteDueMonitor` 仅查找已打开且地址匹配的标签；重启后只有空白页，没有目标标签便直接失败。即使目标标签存在，当前实现也只观察旧页面，未主动获取最新内容。因此不能称定时功能可用，也不能归咎于模型连接或此前独立的安全存储问题。

待补的是浏览器持有固定目标的安全开页/更新、结果验证、临时权限及标签清理，以及可理解的失败原因；不能通过永久扩展模型来源或工具权限解决。现有的“创建、重启保留、暂停”记录不能替代这项真实到点验收。

到点失败证据保留。尝试暂停本测试监控时 Mac 已锁屏，操作未执行，不能把监控写为已暂停；需用户手动解锁才能继续可视化清理和复测。当前诊断 App 使用专用验收 Profile，不是日常资料。上述十项标准继续保持部分通过，未授予发行资格。

## 2026-09-06 04:00：构建失败和后续增量边界

Android 上述冻结候选构建已于 03:42:36 失败，退出码 1；日志为写入 `host_resolver_source.o.d` 时遇到 `Too many open files in system`。源码前后校验一致，没有新 APK。专用容器在确认无编译任务后停止，缓存和失败记录保留；不能继续描述为“编译中”。

定时功能的新开发增量将 URL 状态检查改为浏览器原生、无凭据的有界网络检查，不依赖已打开标签，并保存可解释的 HTTP/超时/登录/限流等固定状态。范围仍只限 URL 状态；价格、库存和内容变化的刷新流程尚未补齐。新增原生浏览器用例仍待构建和运行，不以代码存在或已有核心测试替代真实到点验收。

macOS 首轮新构建在主程序 ThinLTO 链接时出现系统文件句柄不足。随后全局限制链接并发的临时诊断引发过多重编译，已停止该轮并恢复原编译配置；没有改变默认安装包或系统限制。当前仅在独立验收工作区继续原配置构建，日志为本机 `macos-url-monitor-r3/build.log`。此轮尚未完成，不能套用上一 38 文件候选的全套通过结论。

ASCII 走私防护已按专题重新核验，详见其末节；与该防护无关的进度不合并成总体成功。当前 Mac 仍锁屏，旧测试自动化未成功暂停的记录保持不变，十项标准仍未全部满足。

## 2026-09-06 04:15：URL 检查候选与 Windows 传输阻断

当前 URL 状态检查候选包含 41 个受控文件，相比已验证的 38 文件候选有 12 个文件内容变化。canonical、集成 overlay 与独立 macOS 源码逐字节核对一致；清单 SHA-256 为 `09212abc05b359e9fa49e88787a4ce475d80ef71d288e5b21da67ac700678eea`，见本机 `url-monitor-candidate-r1/`。它仍不是最终提交或安装版验收。

04:10 运行完整 Agent Core：94/94 单次通过，实际测试集合与先列出的清单一致，没有重试。二进制 SHA-256 为 `877d278bf8dccfc02898b255d5f1931c383772d21f7e424c402fa7fae7643a6f`，结果 JSON 为 `32215a4be7ea27e86e612b14bc3a94da41237919c5e39bb2c93b9b7d557edc5f`。本机 `url-monitor-core-full-r1/` 保留原始清单、日志和结果；这仍不替代新增六项浏览器测试或真实到点任务。

macOS 系统句柄占用已从此前高位降至约 1 万，系统上限未改。经进程身份核对，于 04:05:19 正常中断单路 R3 构建，结束记录确认源码未变化；随后保留缓存、原 GN 编译配置，以四路并发继续 R4。新脚本实际验证了“已有构建时拒绝启动”的保护分支。本机 `macos-url-monitor-r4/` 记录本轮进程和日志，尚未完成；没有重置缓存或伪造文件时间/哈希。

Windows 的传输尚未到应用源码阶段：预期 100230 字节的压缩载荷实际只有 96180 字节，哈希不一致。Defender 的 1116/1117 事件确认部分 PowerShell 分片追加命令被以 `Trojan:Win32/Commando.A!ml` 拦截。该告警不直接证明产品源码含恶意代码，也不能忽略。停止此分支，保留失败文件为服务器 `C:\a\url-monitor-candidate-r1\source.failed-upload-1.json.gz`，未换编码重试、放行告警或改变 Defender/执行策略/防火墙。

再次读取 Windows 全部 41 文件，均仍匹配上传前状态；候选执行脚本不存在，未启动这轮编译。记录见本机 `url-monitor-candidate-r1/windows-transfer-status.json`。Windows 后续需人工核查这次安全告警，不可将上一候选的通过记录套用到这次新增量。

另修正本地传输助手的失败处理：首个分片失败即终止，不再继续解码生成残缺目标；发布前在远端核对完整 SHA-256，拒绝覆盖已有目标，不回显分片内容。内存桩七项自测通过；当前已认证客户端仍是旧进程，没有为重新传输而重启或读取其凭据，新助手尚未作远端成功声明。

## 2026-09-06 04:42：恢复通知修正，验收快照与开发增量分离

复核发现 URL 检查从登录、限流、网络失败或超时恢复为正常时，旧通知判断会遗漏恢复：首次失败没有旧结果哈希，或者恢复到失败前相同状态码，都不会触发提醒。将实际生产判断提取为固定状态与摘要的函数后，新增六项调度器测试中的恢复用例按预期失败；没有用模拟“产品成功”的标志替代失败复现。

现已修正为：首次正常不提醒，重复相同错误不重复提醒，失败恢复和实际 HTTP 结果变化会提醒。完整 Agent Core 96/96 单次通过，包含 v5 到 v6 的加密监控记录迁移；默认 App 主程序和框架的 SHA-256 前后未变。证据分别位于本机 `monitor-recovery-core-red-r1/` 和 `monitor-recovery-core-green-r1/`，首轮失败未覆盖。绿色结果二进制 SHA-256 为 `e38b871c8e01ff33ada21b76fa97e9cea5b258cd67e572c7eadfa422c3feb1cc`，原始 JSON 为 `1969219e8e80c3962b76f0aa48472ab6c66c56c44aca985ca8dc2e500ff5f6c0`。

另补浏览器级回归：本地服务连续两次 403 后恢复为 200，预期只发出一次故障通知和一次恢复通知，且通知不包含目标路径。首轮编译因测试辅助调用未声明失败，改用已使用的标准回调绑定后，生产服务与浏览器测试对象编译成功；日志在 `monitor-recovery-browser-objects-r1/`、`monitor-recovery-browser-objects-r2/`。这项新浏览器用例尚未执行，不授予运行通过资格。头文件依赖检查还指出两个已有私有依赖，补充测试目标的直接依赖后，指定目标的生成与头文件检查通过。

共享 TypeScript 168/168、类型检查、Agent 界面状态 25/25、颜色 6/6、页面上下文提示 2/2 和仓库合同重新通过；这些不是最新 App 的手动操作或 Qwen 全流程验收。

为避免长编译阻止独立开发，04:20:23 核对进程身份后正常结束 R4，保留缓存与源码未变化的结束记录，随后开始 R5。R5 只验证固定的独立验收源码、原编译配置和基线；启动时开发目录与其一致，结束时会明确记录开发目录是否已前进。没有伪造缓存命中、修改系统限制或重置输出。资源检查通过后使用八路本地并发，并增加磁盘余量不足时停止本轮子进程的保护。

04:42 再次逐文件确认：R5 的 41 文件快照未变；canonical 与集成 overlay 内容一致，其中上述恢复通知和测试依赖共六个文件已在开发目录前进。R5 构建仍运行，当前源码不会冒充冻结快照已验证的版本。下一步须先完成冻结快照的构建与浏览器回归，再同步这六个文件、增量编译并执行新通知用例。

本次可视化工具仍返回 Mac 锁屏，未自动解锁，也没有新增手动界面通过记录。Windows 等待人工核查传输告警，未重试上传或放行；Android 未恢复构建，价格、库存和网页变化的主动刷新也仍未完成。上述十项标准保持未全部完成。

## 2026-09-06 05:24：页面监控主动刷新候选与两端构建

已补入价格、库存和网页变化的主动刷新实现：每次检查新建后台空白标签，以独立 Actor 绑定唯一目标来源，再绕过缓存加载固定目标。语义读取仍经过生产观察器及结果校验，只保存变化摘要；原任务的工具、来源和标签范围不增加，也不刷新或关闭用户原有页面。中文售价和全角人民币符号加入价格匹配。

每轮检查限时 30 秒，暂停、删除、关闭 Agent、任务取消或安全存储失败会清理在途检查及临时标签；迟到结果以请求身份和运行时间校验，不能恢复已停监控。用户接管临时页面时保留页面并停止本轮读取。结果使用固定的加载、登录、限流、超时等状态；没有发起模型调用、表单填写或交易。

跨平台审查还发现 `ActorNavigationThrottle` 在 Android 的 Glic 开关关闭时直接跳过。已移除该前置退出，仍以实际活动任务和标签绑定决定是否添加防护；未开启 Glic 产品功能或取消来源检查。新增四项浏览器用例覆盖三类监控的新内容和缓存刷新、关闭 Glic 后的跨来源重定向、暂停迟到结果、超时清理。它们已编译，但尚未运行，不能算行为验收通过。

生产服务、Actor 适配器、浏览器测试及导航拦截器四个文件已使用真实 GN 编译配方生成独立诊断对象并全部通过；默认 App、源码和配方前后哈希不变。记录在本机 `monitor-page-compile-r2/`。共享 TypeScript 168/168、类型检查、仓库合同、差异格式检查以及浏览器测试头文件依赖检查通过。

首次尝试通过默认输出目录的对象目标检查测试编译时，依赖图意外触发应用框架链接。已在进入框架输出复制前停止该轮，默认主程序与框架的 SHA-256 均未改变；本机 `monitor-page-objects-r1/` 保留中断日志。该入口脚本已停用，后续仅使用独立对象诊断或独立验收工作区。进程检查同时修正为识别 Siso 切换到 `out/AegisRelease` 后的实际工作目录，不能只比较源码根目录。

R5 于 05:14:35 以退出码 1 结束：10699 步完成，框架 ThinLTO 再次因系统文件句柄不足失败，剩余 3689 步。源码未变化，失败记录在 `macos-url-monitor-r5/`；不能继续写为运行中或通过。

当前统一候选包含 43 文件，清单 SHA-256 为 `979e887796593055471d54e3939e33d3318c4d17067e0150ca717e23a5961cb7`，位于本机 `page-monitor-candidate-r1/`。canonical、集成 overlay、Android 及独立 macOS 验收源码均已逐文件核对。Android 的 16 项更新前源码、macOS 的 10 项更新前源码分别归档，原 canonical/Android 的 17 项夹具删除未动。

Android 已在专用容器中以两路并发构建该候选，静态分析保持开启；本机 `android-page-monitor-r1/` 及 Android 输出目录的 `page-monitor-r1-evidence/` 保留日志。05:24 编译进程仍实际运行，未生成最新 APK，也未安装到 Pixel。

macOS 仅在独立验收目录的 `chrome/BUILD.gn` 和 `chrome/test/BUILD.gn` 添加大型框架与 browser_tests 的单线程链接配置。全局编译器配置、系统文件上限、优化等级及安全检查未改；三个相关目标在该配置前后的编译参数摘要完全一致，生成的链接参数以 `--thinlto-jobs=1 --threads=1` 结尾。该构建差异单独记录，不冒充产品源码提交。新轮 `macos-page-monitor-r1/` 正在运行，保留已有成果，约 3700 步待完成；成功后还须执行全部原生回归和真实 Qwen/定时操作。

Windows 本轮未上传或重试安全告警载荷，仍待人工核查；Mac 手动界面验收仍需解锁。源码、诊断对象、正在构建的产物与用户已安装版本继续分开记载。十项完成标准尚未全部达到，未提交或发布最终版本，默认 Mac App 未替换。

## 2026-09-06 05:44：框架链接完成，完整原生验收已排入执行链

本轮继续核对实际进程及同一运行句柄，macOS 和 Android 构建仍在运行，没有因观察超时重启。macOS 主框架在本轮第 11 分 05 秒完成链接并进入框架复制，当前剩余浏览器测试对象继续编译；这只证明主框架链接完成，不代表完整构建或产品测试通过。默认 App 保持原样。

新增本机 `test-macos-page-monitor-r1.mjs`，仅接受本轮构建成功且 43 文件清单、两项独立链接配置、构建参数及默认 App 身份未变的结果。实际测试清单必须恰好覆盖原有用例与新增的 4 项核心、11 项浏览器用例；预期四套数量为 96、87、151、65。先列清单再运行，禁止重试；失败的进程状态、日志和原始结果单独保留，不用最终退出码掩盖漏测或失败。

该验收脚本以 15 组代表性输入自测通过，覆盖有效结果、空/重复/缺失/额外用例、失败、重试、多轮及无效构建状态。另实际执行了“构建尚未结束即拒绝启动”的分支，退出码 1，未创建产品测试结果目录；不把此预期拒绝计为产品测试失败或成功。

已启动执行衔接进程，持续核对本轮构建进程身份及结束结果，只有构建成功才启动上述完整原生验收。构建若失败或中断则停止，不重启构建、不放宽测试条件。05:44 尚未开始产品测试，本机运行句柄为 67311；macOS 构建句柄为 17230，Android 为 41947。

再次核对四份源码，43 个受控文件全部与清单 `979e887796593055471d54e3939e33d3318c4d17067e0150ca717e23a5961cb7` 一致，本轮未修改产品源码。只读资源检查显示宿主机最大文件句柄持有者是 Android 虚拟机进程，约 15.5 万项；未调整系统限制、关闭分析或删除缓存。

ADB 实际列出在线 Pixel 9 Pro Fold，设备处于可连接状态。新 APK 仍未生成或安装，因此不能计为最新 Android 真机通过。已向用户请求解锁 Mac 并人工核查 Windows Defender 上传告警；这两个用户操作不影响继续编译及原生测试，也没有因此放行安全告警或更改系统策略。完整十项完成标准仍未达到。

## 2026-09-06 06:08：暂停监控的预算与状态回归修正

复查确认调度顺序有误：父任务暂停或等待用户接管时，`ClaimDueMonitors` 仍先扣预算、推进“上次检查”时间并交给执行器，执行器随后拒绝执行。这不是实际网络检查，却会消耗预算并可能显示失败。另一条预算耗尽分支还会保留上次 HTTP 200，造成状态含义混淆。

新增两项原生回归，经真实服务、任务暂停/接管入口及调度器复现：暂停与接管均误扣一次预算、改变上次检查时间，预算耗尽仍保留 200。红色结果为 2/2 预期失败，不含崩溃或重试；记录位于本机 `monitor-held-owner-red-r2/`，原始 JSON SHA-256 为 `8c5ca08256ac3b48728ca94421fbb344bdf6444fcce50197b9e2ae86f2a26ed5`。

现将运行状态检查提前到预算扣减之前：等待中的任务不派发检查、不改写上次结果，保留下次调度时间以避免忙循环；恢复后仍按原预算检查。预算/存储失败同时清除不属于本次检查的旧 HTTP 状态。修正后的两项回归通过，再运行该诊断程序的完整浏览器单元测试，157/157 单次通过，实际清单与既有 151 项加 6 项新用例完全一致。

绿色记录位于本机 `monitor-held-owner-green-r2/`，程序 SHA-256 为 `44485b3fb68b30925d9cb069d4678dd33734e154e463c8452994fa14af62127e`，完整原始结果 JSON 为 `39357bab287e8ff170214d3f8a2d7a11826b332b6e32d15e2b206128c9e938d6`。源码、配方、默认 App 主程序与框架在诊断前后未变；全部测试用例只运行一次，不将红色回归与绿色结果合并计数。

首轮独立链接曾复用旧版调度器测试对象，因旧 `MarkFinished` 签名缺失而失败，未运行产品测试。失败记录 `monitor-held-owner-red-r1/` 保留。第二轮从真实 GN 配方重新编译本次链接中的全部 58 个 Aegis 对象，再链接独立诊断程序；没有伪造兼容符号、删除测试、运行默认 App 的完整依赖图或改写既有测试程序。

同时纠正前节完整验收的预期数量：四项新核心用例也会编入浏览器单元测试。因此冻结的 43 文件候选应为核心 96、通用 87、浏览器单元 155、原生浏览器 65；已在产品测试启动前修正等待中的验收脚本，并重跑 15 组脚本自测。该候选仍在独立 macOS 构建，未把本节新增两项的 157 结果套用给它。

本节开发增量仅改动服务与其测试两个文件，canonical 与集成 overlay 一致。增量清单在本机 `held-monitor-candidate-r1/manifest.json`，SHA-256 为 `7f4cde21a2d5949516085ec00f87c0cb45380d558188e28b64cd5208376f7b87`，基于冻结的 43 文件清单。macOS/Android 进行中构建源码仍逐文件匹配原 43 文件候选；须在该轮结束并完成相应验证后再同步这两个文件，不在运行中修改源码。

06:08，macOS 构建及其测试衔接进程仍在运行，Android 构建仍在运行。指定本地 Qwen 的模型列表返回 HTTP 200 且精确模型名称存在，这只证明连接可用，不替代 Agent 全流程。Windows 告警处理、Mac 解锁、最新 APK 安装和最终产物同版本验收仍未完成；默认 App 未替换，完整目标未完成。

## 2026-09-06 06:27：后台标签安全清理与 macOS 冻结候选编译完成

Android 原生标签创建逻辑在低内存模式下可以只创建后台标签而不创建页面对象。监控流程此前没有处理这个状态，连接页面会失败，而清理时无条件调用页面的 `Stop()` 还存在空指针崩溃风险。现仅对本次新建的空白监控标签调用原生 `LoadIfNeeded()`，重新检查页面对象；仍不可用时关闭该临时标签并返回明确状态。清理也先判断页面是否存在，不切换或刷新用户已有标签。

本节增量重新编译服务对象，复用上一轮已完整重编译并核验的另外 57 个 Aegis 对象，链接独立诊断程序。157/157 原生浏览器单元测试单次通过；源码、配方和默认 App 在测试前后未变。程序 SHA-256 为 `8490fc0cd5588e91e77718088bb449a7f6187aa6475333bfa802393d14e59b33`，原始结果 JSON 为 `24cc74a07a24d9067c20986b0c177339b6e08aacb20242f1f6d12a43b7d18c66`，本机证据目录为 `monitor-lazy-tab-r1/`。这组 macOS 单元回归不能证明 Android 延迟加载分支已在真机通过；最新 APK 构建后仍须以低内存模式单独验证。

暂停预算修正与本节延迟加载修正的合并增量清单保存在 `held-lazy-monitor-candidate-r1/manifest.json`。旧 `held-monitor-candidate-r1/` 清单保留，不覆盖历史哈希。本节结束前核对，canonical 与集成 overlay 的 43 个文件逐字节一致，其中只有服务和服务测试两个文件超前于冻结候选；macOS/Android 两份构建源码仍与原 43 文件清单一致。

06:25:29，独立 macOS 构建成功，退出码 0，2645 步，无资源停止，源码与默认 App 均未变。随后自动启动已排队的原生验收；核心 96/96、通用 87/87、浏览器单元 155/155 已单次通过，65 项原生浏览器回归仍在运行。新 browser_tests SHA-256 为 `619d2875b363181f786a4202b5d102c4d7eed05386003ed8a5205c1d105337b8`。本轮冻结候选不包含前两段的服务增量，不与 157 项诊断结果混合计数。

另发现尚待补齐的产品语义边界，不能通过降低提示承诺来算完成：当前“降价提醒”“到货提醒”只对筛选后的价格/库存文字比较哈希，尚未判断价格下降方向、缺货到到货的转换；“重要更新时总结”尚无变化摘要。需要补充明确的条件判断、受保护的前次状态及反向/无变化对照测试，才能关闭常用自动化标准。会话专用提示也仍将隐私会话笼统解释为“安全存储不可用”，需按实际原因区分。以上是源码发现，不是已实现功能。

ASCII 走私专题的 8 个相关文件在四份源码中仍与已测候选一致；共享 TypeScript 168/168 与类型检查重新通过。Computer Use 本轮仍返回 Mac 锁屏，未新增手动界面证据。Android 构建继续运行，Windows 安全告警仍待人工核查，默认安装包未升级，完整十项标准未关闭。

## 2026-09-06 06:34：首次完整监控回归失败，保留红色结果并定位

冻结候选的原生浏览器 65 项于 06:28:04 结束，58 项通过、2 项失败、5 项崩溃，均单次执行；完整候选不通过。原始结果 SHA-256 为 `3eda800c191415cdd2ebe43f4849c558b4ee9517c3df5af5b8454197b6b03c38`。此前 96/87/155 三套绿色单元结果不替代该失败。ASCII 加盐凭据页的真实原生拦截用例在本轮仍通过，监控失败不改写专题既有结论，也不能因此宣称整包合格。

针对通知崩溃，以同一冻结程序执行有界调试，停在 `StubNotificationDisplayService::Display` 的保护断言；源码要求 TRANSIENT 通知有非空 delegate，而生产监控通知传入空值。已为生产通知补齐无额外操作的引用计数 delegate，没有关闭断言或删掉通知测试。调试证据保存在 `monitor-notification-debug-r1/`；调试器退出码 0 仅表示调试结束，不计作产品测试通过。

界面用例错误导航到 `chrome://aegis-agent/`，并从该页面导入可信资源；实际 Agent 是 chrome-untrusted 侧栏。现改为通过真实侧栏入口获取页面，沿用产品的同协议资源导入，仍检查结果文字、HTTP 403 和实际布局高度。

页面监控还存在新空白标签尚未提交就绑定 Actor 的时序问题。现等待真实 about:blank 提交后只绑定一次，再导航目标；原先严格的空白页、单来源、标签和工具校验仍保留，30 秒总等待上限不变。该修正尚须本节新诊断确认，不把源码推断当作通过。

本机 `test-monitor-runtime-diagnostic-r1.mjs` 正在用真实 GN 配方编译服务及浏览器测试对象，两项已编译成功，随后链接独立测试程序并单次运行上述 7 项失败用例。canonical 与集成 overlay 已同步本节修改；冻结 macOS/Android 源码和原有测试程序未动。本轮新增服务头文件只声明私有非虚方法，不修改类布局；该对象替换诊断仍不能替代最终整包重建。旧清单与失败记录全部保留。

## 2026-09-06 06:43：定点复验 3 项通过、4 项仍失败，无崩溃

服务及浏览器测试对象编译、独立链接均成功。脚本首次在解析测试清单时没有剥离 `# GetParam()` 注释而提前拒绝，未执行测试；修正清单解析后用 `--test-only` 检查源码、配方、原程序和已完成链接状态一致，再执行 7 项用例，没有重复编译或覆盖此前失败记录。

06:38:34，定点结果为 3 项成功、4 项失败、0 项崩溃，均只运行一次：恢复通知去重与路径保护、真实侧栏的 HTTP 403 说明、暂停页面监控并清理独占临时标签通过。页面新鲜内容、30 秒超时、跨来源重定向状态/清理、原有标签范围生命周期仍失败，不能声明监控整体修复。

本机 `monitor-runtime-diagnostic-r1/` 保留完整证据；独立程序 SHA-256 为 `df3f6a22a142d833c28f129eaf70576c83388844c7b9342a755485d61d003920`，原始结果 JSON 为 `1c54dc4c238d3dc5878de92ed5694e558c934b118d8d81126a154813fc7f4323`。完整 65 项复验入口 `test-monitor-runtime-full-r1.mjs` 要求这 7 项先通过，目前不满足前置条件，尚未执行完整绿色复验。

后续定位线索，尚非已确认修复：新页面已经可以发起请求并被暂停清理，但正常价格页返回 `kCheckFailed`；现有 Actor 读取将截图失败也视为整次读取失败，需要核对语义内容和截图各自的实际结果，不能直接放宽验证。超时用例的默认测试等待与产品 30 秒上限相撞，应在保留产品时限的同时明确测试等待预算。跨来源跳转未请求外部目标，但返回 `kPageUnavailable` 且临时标签仍保留，需区分实际用户接管与策略触发的等待状态。上述问题仍阻止第 5、9、10 项标准关闭。

## 后续修正：完整 44 文件候选构建与 407 项原生测试通过

前节的跨来源失败线索已收敛：失败用例的首个目标请求计数也是零，不能归因于用户接管。Glic 关闭时不会创建 Actor UI 控制器，但 Aegis 的初次标签绑定仍等待该控制器确认，错误地返回“标签消失”。现仅对有效 Aegis 标签、Glic 关闭时的初次绑定补齐 UI 确认；没有放行页面操作或跨来源请求。

后台监控截图依赖通过两项对照用例确认：临时开启截图跳过诊断特性后，原失败的页面语义读取通过。产品修正不启用该全局特性，只对单来源、单标签、仅允许 `page.observe` 的专用监控任务接受有效语义内容；原文档、来源和结果验证仍执行。超时测试把自身等待窗口设为 45 秒，同时断言产品仍在 30–35 秒内终止，没有延长产品时限。新增无价格不生成价格证据、真实用户接管后保留标签并丢弃迟到结果两项原生测试。

06:56:16，定点 9/9 通过；06:59:13，诊断程序完整 67/67 通过，均未重试。早前 LLDB 断点未解析，不计为已获得调用栈的证据；首次失败、截图对照和最终默认特性结果分别保留。

随后将修正冻结为 44 文件候选，清单 SHA-256 为 `df50f524451c3336b6f876a69f509b2e4255c70f9e14add1d6d7a22bd7bb9269`。旧独立验收源码、完整 App 和四个测试程序均已备份并核对哈希，6 个变化文件通过补丁同步到独立 macOS 工作区。Android 活动构建仍使用原 43 文件冻结版本，未在构建中途改源码。

07:13:01，独立 macOS 完整 App 与四个测试目标增量构建成功，16 步、6 分 22 秒。07:18:21，核心 96/96、通用防护 87/87、浏览器单元 157/157、原生浏览器 67/67 全部单次通过。完整源码清单、构建参数、测试集合与程序哈希逐一校验；不是诊断对象拼接的替代证据。完整原生 browser_tests SHA-256 为 `f8a540536c88f0a9ccc7b3402f8b9769150e1b9e95af58f29b59d79f07052c25`，结果为 `14f577a35438a631b6919801da8c805d195202e1c8b864ebade1573a982b7878`。证据目录为本机 `macos-monitor-runtime-r2/` 和 `macos-monitor-runtime-tests-r2/`。

## 真实 App 复验仍失败：页面未完成加载，退出等待钥匙串

Mac 本轮已解锁。通过可见界面暂停旧专用 Profile 的 15 分钟监控，再从菜单退出旧进程；核对 PID 69380 已不存在后才复制测试资料。没有读取或复制用户日常 Profile。

本轮实际运行的是 `macos-runtime-ui-r2/GCSA Aegis.app`，初始 PID 51520，独立资料目录为同目录 `profile`。复制前后主程序和框架一致，随后仅对诊断副本做临时本地签名并通过 strict/deep 校验；签名后主程序 SHA-256 为 `863a126c77cd126b38ee598ca48e142fd2742e18a489c801d282a33c3f0987e6`，框架为 `16ef5f3f969b1ef9af443198cc99d628d1d6fce5fbd526c2b7dc8fd7bdbf2c30`。完整身份与签名前后关联保存在 `app-identity.json`。这仍不是 Developer ID 签名或发行包。

真实工具栏入口可以打开完整任务页。本地 Qwen 模型清单包含指定 35B 模型；07:22:03 从空白页输入“打开本地资料，告诉我续航和保修期”，07:22:05 可见已生成计划并开始执行，但新标签没有加载完成，07:22:35 因没有新的有效页面观察而停止。测试服务自身 GET/HEAD/404/405 自测通过、独立 curl 返回正文，但在该次 Agent 操作中没有收到网页 HTTP 请求。不能计为摘要端到端通过，也不能通过修改成功提示掩盖失败。

对同一目标的手动导航仍未显示正文。之后从菜单退出时，进程没有结束：采样明确显示主线程在 `TaskTracker::CompleteShutdown` 等待，工作线程在 `OSCrypt GetKeyTask -> KeychainPassword::GetPassword -> SecItemCopyMatching -> SecurityServer decrypt` 等待系统返回；退出前采样也有同一钥匙串等待路径。该证据确认退出阻塞点，尚未单独证明全部导航问题都由它导致。网络诊断重启脚本因旧 PID 仍存活而正确拒绝，尚未启动第二个实例或生成 NetLog；未强制结束系统安全进程。

系统 SecurityAgent 存活，但 Computer Use 明确禁止访问该安全应用，未尝试绕过。已请用户人工确认是否存在“Chromium Safe Storage / Chrome Safe Storage”授权提示及其内容；没有自动点击允许、输入系统密码、更改钥匙串访问控制、禁用加密或启用模拟钥匙串。当前不能把这个 App 当作可用新版交付，第 2、3、9、10 项仍不满足。

另一个待验证兼容性问题：复制的旧版测试资料中，已保存监控的所有者在退出前处于恢复中，而非已完成；新版恢复后显示任务到期。该旧数据库是 v5，不能把它直接等同于“新版已完成监控暂停再重启”的失败。应分别补旧监控迁移和新建监控暂停/重启的真实用例，不能直接给过期网页操作延长授权。

## 降价/到货条件计算：代码和核心测试完成，尚未接入运行闭环

canonical 与集成 overlay 新增有界的页面结果计算和条件比较：同币种价格下降才匹配；缺货到有货才匹配；同价、涨价、币种变化、重复节点、千分位/小数格式变化不误触发；原价/运费不当作当前售价；多价格、库存冲突、未知内容不猜测。英文否定语句、中文/繁体库存、不可见字符及输入大小限制均有测试。

原 96 项核心回归加 7 项新用例，两个独立诊断轮均为 103/103。第二轮增加格式比较双方必须实际解析成功的断言，并排除英文词内的错误库存匹配。最终程序 SHA-256 为 `dceb3f9ee8f445bfd433048f948b5e2cc0f638fa9566b5f34777ecc540df742f`，结果为 `dcee894d445885fa2d307ca09b4e5da00776b8ecc7042b133241c5c1dc38000f`；证据 `monitor-condition-core-r2/`。

这 3 个文件是 44 文件候选之后的待集成增量：没有更改活动 Android 源码，也不在上述真实 App 中。目前还没有接入运行中的结果处理、加密历史结果保存和重启恢复，原先“文字变化就通知”的生产逻辑尚未被替换，因此第 5 项没有关闭。待优先排除真实 App 加载/系统授权问题后，再接通持久化与通知条件，并用真实计时触发、涨价不提醒、缺货恢复及重启对照验收；不能只改按钮文案。

## 后续实现：条件判断与加密基线已接通，原生核心 106 项通过

本节更新前节“未接入”的源码状态，不代表全部运行验收完成。

- 页面检查结果改为先通过来源/文档验证，再提取版本化价格、库存或正文；只有同币种降价、缺货变有货或已测正文变化才触发相应通知。首次检查只建立基线，检查失败保留上次有效结果，不把失败恢复称为降价或到货。
- 持久监控的历史结果由 OSCrypt 加密，密文内容绑定监控、所属任务和目标摘要；恢复时同时检查这些绑定与结果摘要。错配、损坏或解密失败会禁用该监控并显示安全存储错误，不使用明文回退；会话监控仍不落盘。
- 数据库升级到 v7，仅新增密文列，提供 v5/v6 迁移路径。旧版只有哈希时不虚构历史价格或库存。新增“内容不明确”和“安全存储不可用”两类中英繁提示，去掉“目标网页未预先打开”的误导说明。
- 新增服务层重启用例覆盖正常加密恢复、暂停状态保持、其他监控/任务/目标的密文错配、哈希错配、无效密文及无效 JSON。新增浏览器用例检查生产结果真正加密，已有场景增加涨价不通知、降价通知和缺货到货断言。这两层新测试尚待编译与实际执行，不能提前计为通过。

修改共涉及此前 44 文件候选中的 11 个文件，canonical、集成 overlay、独立 macOS 已同步；Android 活动构建没有改动。备份位于本机 `monitor-conditions-candidate-r3/`，包括旧完整 App、四个测试程序和两份旧源码压缩包。R3 原生编译先因测试循环不必要复制字符串触发 `-Werror` 停止，已改用引用，未关闭编译检查；失败日志保留。

修正后的原生核心目标通过正常构建完整生成，07:59:22 执行 106/106，原有 96 项与新增 10 项清单一致，每项仅一次、没有重试。程序 SHA-256 为 `c3e9eac80659baedd3af9813593bf72c69e974d1128d52747c967e24b5456c0f`，原始结果为 `729bc440fcd196ed3ca2d13c630e14fac0c706b2da5bb07c835cb9540407b425`，证据目录 `macos-monitor-conditions-core-r4/`。这不是将新结构体与旧布局对象拼接的诊断程序。Core TypeScript 168/168、类型检查、界面状态 25/25、状态颜色 6/6、上下文错误 2/2、仓库合同与差异格式检查也通过。

R4 整包构建于 08:01:19 在 ThinLTO 链接阶段失败：`Too many open files in system: ThinLTO: Can't get a temporary file`。启动前当前句柄约 21 万、系统上限 491520，按余量保留 20 万并单并发仍未避免失败；不能以这个总量检查证明大型链接资源充足。没有修改系统限制、安全检查或优化等级，未替换默认 App。失败记录位于 `macos-monitor-conditions-r4/`。

随后修正浏览器测试的调度顺序：第三次价格检查在任务完成前登记，再验证已完成所有者的后台检查，不能要求已完成任务重新执行 `UpsertMonitor`。当前 R5 清单 SHA-256 为 `a15e841da868102abbee36e86e4752afd39216ed67977943fc7e2014b5ce0080`；只比 R4 改该测试，核心生产源码及已过的 106 项不变。服务单元目标已启动，但其实际依赖同一大型框架，不能当作绕开链接问题的替代验证；运行结果尚未取得。

Android 同一构建句柄仍存活并推进到约 2 万步，未生成新 APK；Windows 传输安全告警仍待人工核查；Mac 真实 App 的系统授权问题未获用户回复。第 5、8、9、10 项仍需生产加密恢复、真实计时和最终产物证明，其余跨平台门槛也未关闭，整体目标继续保持未完成。

## 08:27 更新：条件与加密监控完整原生回归 429 项通过

R5 服务单元目标于 08:09:34 构建成功，168/168 单次通过，包含七组实际服务重建的密文绑定验证。随后完整 App 构建成功；四套回归中，核心 106、通用 87、浏览器单元 168 通过，原生浏览器为 67 成功、1 失败、0 崩溃。唯一失败是价格用例在第三次检查前仍保留运行中的所属任务，却错误断言 Actor 总数为零。修正为中间阶段验证恰好保留该所属任务，完成第三次检查后仍必须清理到零，没有删除最终清理断言或放宽产品权限。

R6 首次重新链接 browser_tests 又遇到 `Failed to open cache file ... Too many open files in system`，随后出现连带的符号缺失；未通过删代码、改优化或补虚假实现解决。为了串行安排资源，主动向专用 Android 容器中的已核实 Siso PID 17 发送 SIGINT，等待其退出后再停止该容器；没有停止整个 Docker 或其他容器。Android 该轮用时 3 小时 7 分 45 秒，完成 21822 步、编译失败 0、剩余 46830 步，退出来自人为中断；已编译对象、日志和 43 文件冻结源码保留。

暂停 Android 后，同一 R6 源码在 R7 构建记录中于 08:24:44 成功完成链接。08:27:32，四套原生测试为 **106/106、87/87、168/168、68/68，共 429/429**，每项单次执行，清单完整且无重试。相关生产场景已覆盖首次只建基线、涨价不通知、实际降价通知、缺货到货通知、结果密文保存、无明确价格不推断以及监控标签清理。测试构建与默认 App 哈希均由脚本核对，失败记录未覆盖。

- 当前 44 文件源码清单：`monitor-conditions-candidate-r6/manifest.json`，SHA-256 `a171cb5fac2c8d620a8f9c5532cf0f3cf1eae7b3e878bf124b3ae2afa0c41e89`。
- 最新完整构建与测试证据：本机 `macos-monitor-conditions-r7/`、`macos-monitor-conditions-tests-r7/`。
- browser_tests SHA-256：`d1759fb9769e05b98aba998b28bd767d7cec304ea09602548cc216fc791ae836`。
- 原始 68 项结果 JSON SHA-256：`52deebfbd1cfb106d18007b63bd8b4b42baece9f649313e969fe74745c29f7db`。
- 集成 `pnpm quality:fast` 全部通过，包括 TypeScript、共享 Core、脚本、界面状态、中转约束、仓库合同和构建。

测试边界：Chromium 的 `InProcessBrowserTest` 在 macOS 使用测试钥匙串，服务单元测试使用 TestingBrowserProcess 的测试加密器。测试验证的是生产加密调用和结果处理，不是对真实系统钥匙串授权的验收。本轮 Computer Use 明确返回 Mac 锁屏，旧诊断 App PID 51520 仍未退出；没有自动解锁、允许系统提示或关闭系统安全进程。用户默认 App 未替换，新构建仍不可冒充可用交付。

Android 在专用容器确认为停止后同步到上述同一 44 文件候选，变更 13 个文件。旧源码压缩包经逐文件解包哈希校验可恢复，当前源码逐字节符合清单，17 个外部测试夹具删除不变。证据为本机 `android-monitor-conditions-source-r2/`；恢复构建入口为 `build-android-monitor-conditions-r2.mjs`，继续复用缓存且保持静态分析开启。新 APK、真机和 Windows 最新安装版仍待验收。

下一轮还须补齐安全存储恢复的界面交互：异步目标解密完成需要及时刷新监控状态；安全存储恢复后点击“继续”应重新尝试解密，不能在目标仍为空时只改成启用状态。此外，页面变化摘要、真实 15 分钟以上定时触发、实机 Qwen 流程和最终同版本交付仍未完成。上述 429 项通过不关闭整体十项门槛。

## 安全存储恢复增量：指定监控重试与异步界面刷新

已修复上一节指出的服务逻辑缺口。点击“继续”会为需要恢复的指定监控重新申请解密，完成前不调度网页检查；解密成功后清除安全存储错误，但不虚报一次成功的网页检查。回调通知现有 WebUI 快照订阅链路刷新，同时读取当前状态，保持用户暂停、删除和 Agent 关闭的决定；已恢复的历史结果不会被迟到的失败回调覆盖。重新启用 Agent 会重试未解密的目标，但不自动恢复用户暂停的监控。没有明文存储回退、额外授权或类数据布局变化。

新增六项服务测试：指定目标恢复且不触碰其他监控、目标/历史密文损坏仍暂停、异步刷新保持暂停、删除后不复活、Agent 关闭时忽略回调且重新启用可恢复、迟到失败不覆盖已恢复结果。原有 168 项加新增 6 项在独立原生程序中 **174/174 单次通过**，没有重试。该诊断使用已完整构建的 R6 对象布局，精确替换服务与测试两个对象，并验证新头文件只改私有回调签名和测试友元；不是把不同布局的旧对象混装成通过结果。

- 冻结候选：`monitor-recovery-candidate-r1/manifest.json`，44 文件中仅 3 文件增量，SHA-256 `ab645e11b14565a8d58cdb39d79448710c1553738fdfeb07489713c42d97081a`。
- 原生诊断：`monitor-recovery-diagnostic-r2/`，程序 SHA-256 `0d8bea30409a2ba856a429e43740c44c7d0016152dface8607a8108ea4709d01`，原始测试结果 SHA-256 `db96b59e8c0e12927db7e32deed58965692b8b40dd4d660c0c342e2b2b8c7aed`，完成时间 09:04:38。
- 旧源码压缩包已逐文件解包校验；旧完整 App 与四个原生测试程序保存在候选目录并核对摘要。canonical、集成 overlay、独立 macOS 已应用此增量；Android 在活动构建期间未改源码。
- 集成 `pnpm quality:fast` 通过，包含 Core 168 项、界面状态/颜色/错误提示、脚本与中转测试、类型/合同/构建检查。正常 App 整包随后于 09:13:15 构建成功，14 个实际步骤、93434 个跳过步骤，没有重试或资源中断；完整原生回归于 09:16:15 得到 **106/106、87/87、174/174、68/68，共 435/435 单次成功**。09:16:50 再次核验源码、程序和原始结果；不再只依赖上述独立诊断，但仍不代表真实系统授权或整体验收通过。

正常构建与原生结果分别保存在本机 `macos-monitor-recovery-r1/`、`macos-monitor-recovery-tests-r1/`。服务单元程序 SHA-256 为 `6f1a554bdead228bf2dfb9bea60bf58dc6ab33ba553b161308545f255d7f985b`，174 项原始结果为 `6349bde1b7147f9fbd7a678cf2776487e07bcd5fe18425db70b6840ff9a6582b`；browser_tests 程序为 `70c7b1369718f8a275c3beeac535710546cedb9536c4e7f6afd5b3b89ccd8103`，68 项原始结果为 `13b6318f8c0ad90f6ddc7d6cd3e6e8a688f866b7bfef6d347ad4005d0458238c`。默认 App 未替换。

资源故障单独记录：R1 脚本最初因 Docker 进程清单缺 PID 列而在只读预检停止；修正后实际链接仍报 `Too many open files in system`。冻结容器 11 秒后自动恢复未能释放句柄。随后向核实的 Android Siso PID 17 正常发送 SIGINT，29 分 07 秒这一轮留下 6588 已完成、0 编译失败、68824 剩余；不是编译错误。停止容器后仍有约 21.7 万个 Aegis 文件被虚拟机缓存占用。确认所有五个容器都已停止且重启策略均为 `no` 后，正常退出 Docker Desktop，系统文件句柄从约 22.7 万降至不足 1 万，同一源码和原优化设置下的 R2 链接及 174 项测试成功。没有修改内核限制、删除缓存/数据、关闭静态检查或启动其他项目容器。Docker 与 Android 构建将在 macOS 验证完成后恢复。

Docker Desktop 已正常恢复，其他四个项目容器保持停止。Android 在容器退出状态下同步同一 44 文件候选的 3 个增量文件；旧源码逐文件解包哈希校验通过，原有 17 个夹具删除保持不变，证据为 `android-monitor-recovery-source-r1/`。新构建入口为 `build-android-monitor-recovery-r1.mjs`，证据为 `android-monitor-recovery-r1/`。根据实测虚拟机 18 核、25159270400 字节内存，将编译并发由 2 提升到 4；仅调整构建并发，静态检查、编译器安全配置与优化均不变。没有清除缓存。Pixel 9 Pro Fold（`48311FDKD002P8`）经 ADB 确认在线，但尚无新 APK，不能把旧安装结果算作新候选通过。

Computer Use 本轮仍返回 Mac 锁屏，尚无新增真实系统授权和界面交互证据；Windows 传输安全告警仍需人工核查。第 5、8、9、10 项及其余跨平台实机门槛保持未关闭。下一步优先等待 Android 新包并进行真机验收，不在活动构建期间修改这份冻结源码；页面变化摘要和真实定时触发仍需完成，不能缩减十项标准。

## Android 真机目标检查：拒绝旧 APK 和未证实的测试 Profile

本轮没有安装、启动、停止或操作手机上的浏览器，也没有修改资料、调试设置或命令行文件。只读检查发现：Pixel 9 Pro Fold `48311FDKD002P8` 当前已亮屏解锁，`app.gcsa.aegis` 的版本号仍为 `151.0.7922.77`，实际安装 APK SHA-256 为 `326057099a0020849ed7f28762b8d87e8b6042056159db544224bbd8b04f3764`，最近更新时间为 9 月 5 日。运行进程打开的 History、Cookies、存储和 Agent 数据库位于 `/data/user/0/app.gcsa.aegis/app_chrome/Default`，不是先前准备的独立测试目录。因此不能在这份默认资料中继续执行验收任务。

设备上的 `/data/local/tmp/chrome-command-line` 与本机旧准备文件摘要也不同；仅凭那份准备文件不能证明运行参数生效，亦不能由此断言是某个具体开关失效。本轮没有读取命令行正文或浏览数据库内容，报告仅保留包、版本、摘要、Profile 根路径和设备状态。真实检查报告为本机 `.artifacts/v2-final/android-target-inspect-r1.json`，时间 09:35:37，明确记录 `targetVerified=false`、`uiTested=false`、`runtimeTested=false`。

新增 `apps/browser/scripts/verify-android-agent-target.mjs`，并将对应测试加入 `pnpm quality:fast`：

- `inspect` 只读取指定 USB 真机、包路径、APK 实际摘要、版本和运行中 Profile 文件路径；不自动安装、启动、切换用户、清理资料或解锁。
- `verify` 先检查成功构建记录、源码清单/基线和 APK 摘要，再核对手机安装的精确 APK、稳定进程、同一 Android 用户、亮屏解锁状态及唯一的指定测试 Profile。版本号相同、参数文件存在、未知或默认 Profile 均不足以通过。
- 报告不覆盖已有文件，不保存原始文件描述符清单；目标检查即使成功，也固定保留界面、Runtime 和发行资格未验证的边界。

16 项合成/真实临时文件测试全部通过；包含安装路径注入和多 APK 拒绝、目录归一化、默认/混合/未知 Profile、进程/用户变化、锁屏、构建失败/中断/源码错配、源码路径越界与证据缺失。使用真实 Pixel 检查结果调用隔离门槛，确实得到“运行中的独立 Profile 尚未证实”；当前构建没有完成记录时，完整 `verify` 也以非零退出并提示“候选构建尚未完成”，没有操作旧 App。集成快速质量检查通过，最后的缺失记录文案调整也重新执行了全部 16 项测试。

待新 APK 构建成功、安装并以全新测试 Profile 启动后，使用如下命令确认目标，再开始真实入口和任务操作；这条命令本身不完成后续 UI 场景：

```sh
node apps/browser/scripts/verify-android-agent-target.mjs verify \
  --adb /Users/lazy/Library/Android/sdk/platform-tools/adb \
  --serial 48311FDKD002P8 \
  --profile /data/user/0/app.gcsa.aegis/aegis-test-user-data-recovery-r1 \
  --source-root /Users/lazy/Projects/GCSA-aegis-chromium-linux-amd64/src \
  --manifest /Users/lazy/Projects/GCSA-aegis/.artifacts/v2-final/monitor-recovery-candidate-r1/manifest.json \
  --build-dir /Users/lazy/Projects/GCSA-aegis/.artifacts/v2-final/android-monitor-recovery-r1
```

执行位置为集成工作区。Android 本轮始终复用现有构建句柄；只增补宿主验收脚本和文档，冻结的 44 个 Chromium 文件未改动。必须继续完成入口可点击、中文输入、本地 Qwen 任务闭环、全部常用场景、真实定时与保护行为，不能用这个前置检查替代十项完成标准。

## Android 中文 UI 驱动已完成真机自测

2026-09-06 10:05，在 Pixel 9 Pro Fold `48311FDKD002P8` 安装了本次新建、此前不存在的测试工具 `app.gcsa.aegis.qa.driver`。没有替换浏览器，没有向默认浏览器发送点击、输入或资料修改命令。工具没有网络权限、启动器入口或常驻服务；通过一次性 instrumentation 的公共无障碍节点操作完成中文输入，不申请常驻无障碍授权，不关闭用户已有无障碍服务。

新增宿主脚本 `apps/browser/scripts/android-agent-ui.mjs`、原生测试夹具及 [使用说明](../../apps/browser/scripts/android-ui-driver/README.md)。宿主与设备两侧均保留候选/资料边界，拒绝锁屏、其他 App、密码框和执行前已变化的界面；只读 Profile 校验只检查已打开文件的路径，不读取数据库内容。工具不会安装浏览器、设置调试 App、解锁设备或更改浏览器资料。

- 实际真机自测 6 项通过：中文及 emoji 完整输入、真实点击和回显、过期页面拒绝、错误包拒绝、密码框拒绝、密码文本隐藏。回显为“已收到：帮我总结页面内容：电池续航18小时 🔋”，不是模型生成的结果。
- 随后直接验证设备端的只读边界：给出当前已安装浏览器的真实 APK 摘要，但要求新的独立 Profile，实际返回“拒绝默认资料或未经证实的独立 Profile”，没有返回浏览器节点或执行动作。浏览器原 PID `15968` 保持；测试结束后没有测试工具进程。
- 宿主 UI 驱动 13 项测试通过，已加入 `pnpm quality:fast`；包含错包、错资料、旧 APK、过期/未来时间、缺失/重复节点、密码、超长输入、安装路径与结果资格边界。完整快速质量检查通过，包含既有 16 项真机目标检查及 Core 168 项。
- 实际 APK 权限清单为空。签名使用的临时私钥在仓库外私有目录生成、完成后移除，没有使用产品密钥。工具 APK 摘要为 `ecfd1844ee05d6f13d3f417c48d0b4d631e3229f6ad4d8db4922cba033908049`，构建及真实设备结果保存在本机 `.artifacts/v2-final/android-ui-driver-r1/`，后者为 `device-self-test.json`。

另核查独立 Profile 参数的启动前提：设备 `debug_app=null`、`wait_for_debugger=0`；Chromium 的非 root 命令行开关默认关闭。不能只凭 `/data/local/tmp/chrome-command-line` 文件存在就宣称参数已生效，当前缓存开关值也尚未证实。已向用户请求允许在新包装好后临时指定 Aegis 为调试应用并在测试结束恢复原设置；尚未更改该设置、命令行文件或浏览器进程。

Android 原构建句柄仍存活并持续产生编译结果，未重新启动构建，也未改动冻结 44 个产品源文件。该工具自测仅关闭中文交互工具本身的技术缺口；最新 APK、真正 Agent 场景、定时触发、Windows 可见界面及十项标准仍待完成，`runtimeTested=false` 与 `releaseEligible=false` 保持不变。

## 网页变化 AI 摘要增量：核心与真实模型通过，浏览器流程未验收

本轮针对第 5 项的实际功能缺口开发：界面原本承诺“有重要更新时总结变化”，但冻结
44 文件候选只比较页面内容，没有生成 AI 变化摘要。本次实现仅位于集成工作区；
canonical、独立 macOS 和 Android 的冻结 44 文件逐字节复核未变。集成工作区相对冻结
候选有 13 个既有文件变化及 3 个新增摘要模块文件，尚未建立新的整包候选。

### 产品行为与安全边界

- 前后两次已核验的公开页面观察才生成变化输入；首次建立基线，无变化不调用模型。
  忽略空白、重复节点和顺序噪声，只发送新增/移除片段，不发送整页未变化正文。
- 删除与新增片段各分配一半预算，总计至多 16 条、12 KB 证据；输入截断时 UI 明确提示
  范围有限，不能用部分片段得出“整页没有重要变化”的结论。
- 请求使用原任务明确绑定的 provider、endpoint、model，仅提供
  `agent.summarize_monitor` 返回工具；最多两次请求，计入原模型及网络预算。
  不是让模型自行开网页、改权限或选择另一个服务。摘要引用须来自本轮变化，并与当前
  观察内容摘要绑定；引用校验不能证明语义绝不幻觉。
- 结果仍通过原系统加密观察记录保存；失败保留旧基线、旧摘要，不发送变化通知。
  暂停/删除/关闭/用户接管沿现有检查生命周期取消请求，迟到回调检查请求身份。
  系统通知不含摘要正文；WebUI 通过文本节点展示结果，不渲染模型 HTML。
- 数据库升级至 v8：新状态 14 表示摘要暂不可用；v5/v6/v7 逐步迁移，保留密文、调度、
  暂停及失败状态。最低兼容版本同为 8，旧程序不得原地读写新数据库；不自动重建空库。

### 实际完成的验证

- `monitor-summary-diagnostic-r3/`：114 项原生 Core 单次通过，包含 8 个摘要用例；
  服务源文件编译成功，但此诊断二进制不链接服务对象。
- `monitor-summary-diagnostic-r4/`：加入存储迁移后 117 项原生 Core 单次通过，精确覆盖
  旧 106 项、摘要 8 项与存储 3 项。新增测试验证 v7 迁移及再次打开、摘要失败不会使
  无关监控消失、拒绝未来数据库且不改写文件。程序 SHA-256 为
  `57bc9e3220dd01a5ed020f586399705b5553dc4a358de28631c25b9cc90c7e29`；
  原始结果摘要为 `b87afc935097cb597b9e503d6607e57d0e00ebce842e21757fb084b382597c2e`。
- `monitor-summary-qwen-r2/`：通过生产 C++ 摘要提示词、请求编码器及响应解析器实际
  调用指定本地 Qwen，两组均 HTTP 200。约 1818 ms 总结续航 18→24 小时和保修
  24→36 个月；约 998 ms 将只刷新时间的样例判为无重要变化。均为合成公开数据的
  真实模型协议诊断，不是浏览器端到端验收。
- `quality:fast` 完成；随后新增存储及浏览器测试源码后，重新执行界面状态、摘要 DOM
  6 项、仓库合同与全工作区差异格式检查，均通过。DOM 测试不替代真实界面。

### 浏览器验证的实际失败与剩余工作

新增浏览器测试覆盖噪声/无变化调用次数、两次格式失败保留基线、暂停丢弃迟到响应；
已有三类页面监控回归保留，页面变化分支改用真实 HTTP fixture 响应并断言摘要、模型
调用与网络预算。不是把页面变化场景从原测试里删除。

`monitor-summary-browser-r1/` 的浏览器测试源码编译成功，但独立链接器实际报告系统
文件句柄不足。随后只终止本轮已报错的链接进程组 96078；原始结果为 SIGTERM，原因
另记于 `stop-note.md`，没有重试或删除失败证据。未运行该轮新增浏览器测试，因此异步
取消、真实加密恢复和摘要显示还不能计为运行通过。未改系统限制、未重启 Docker，
未触碰默认 App 或旧测试程序。Android 原 session 67461 / Siso PID 732 继续推进，
截至本轮检查约 24471 步、1 小时 57 分钟，无新 APK 完成记录。

接下来应在 Android 构建完成后，统一同步增量并串行完成正常构建、Mojo/WebUI 编译、
上述浏览器回归、加密重启、指定 Qwen 整体任务与至少 15 分钟真实定时触发。最终还需
Windows 最新窗口和 Pixel 最新 APK 场景矩阵。第 5、8、9、10 项以及其余十项标准
均未因本轮诊断而关闭；完整目标保持进行中。

## 2026-09-06 07:40 UTC：摘要候选完整回归通过，真实 App 仍未通过

本节替代前节的构建中状态，不覆盖或删除历史失败。当前候选清单
`monitor-summary-candidate-r2/manifest.json` 的 SHA-256 为
`98afe2216d5e06eff41a58ba49f6ddaa15033cf273fcf14caefde6e5e76dd190`，仍以
`383d157c6f3601101038aef6ff964e61b6af7b4f` 为基线，包含 47 文件，未形成新的正式提交。

### 正常构建与单次完整回归

Android 上一构建实际已停止。核对没有活动编译及其他运行容器后，正常停止本任务容器
及 Docker Desktop，使宿主文件句柄占用从约 20.6 万回落至约 0.9 万。没有强杀 Docker、
调整系统限制或关闭安全检查。保留所有源码、缓存与失败记录。

macOS R1 在独立验收目录完成正常整包构建，包含生产服务、Mojo、TypeScript、CSS
及框架资源；并非只链接独立诊断对象。首轮完整回归为 451 成功、1 崩溃：唯一异常来自
暂停摘要测试的 HTTP 夹具。先注册的通用处理器提前处理了 `/v1/responses`，后注册的
可控处理器永远收不到请求，等待超时后触发测试辅助类的检查失败。日志与失败结果保留
在本机 `macos-monitor-summary-tests-r1/`，不将它描述为通过或已证明的产品崩溃。

修复只改变该测试的请求分派：暂停场景将请求交给可控处理器，等待前明确检查是否
收到请求，保留暂停、模型取消及迟到结果丢弃的断言。R2 正常增量构建成功，
07:31:03 UTC 全部 452 项单次通过，无重试、无漏项：

| 测试集 | 单次结果 |
|---|---|
| Agent Core | 117/117 |
| 通用防护 | 87/87 |
| 浏览器单元 | 177/177 |
| 原生浏览器 | 71/71 |

实际清单必须恰好覆盖旧 435 项与新增 17 项执行项，不能只以汇总数量判断通过。
原生浏览器覆盖正常变化摘要、噪声不通知、无变化不调用模型、格式失败最多两次并保留
旧基线、暂停丢弃迟到模型响应，以及原有价格/库存和加密保存行为。固定输入、程序
哈希、原始结果和默认 App 未变化均重新核对。证据为本机
`macos-monitor-summary-r2/`、`macos-monitor-summary-tests-r2/`；测试总报告 SHA-256 为
`b1b15380463745ae6bc544cf3e0410233eca46f17170e993ac48035e885f8300`，
`browser_tests` 为 `817d9ccd694e40d38faa63d5cd2bde6c64c0b683814e88a997d039f1cf5a2964`。
上述绿色结果不替代实际安装版、Qwen 全流程或真实计时验收。

07:42 UTC 重新运行完整 `quality:fast` 并通过，包含共享 Core 168 项、类型检查、构建与
脚本自测、界面状态及摘要 DOM、Android 目标/操作边界、模型中转和仓库合同检查。
真实界面操作后再次只读复核全部 452 项原始结果与程序/源码身份，也通过；未重跑或
覆盖这些原生测试的单次结果。

### 新空白资料的真实界面失败

从已通过的正常 App 克隆独立副本，逐文件与软链接核对后，仅对这个副本做临时本地
签名并通过严格验证。没有改写默认 App、复制日常资料、预填模型密钥或启用模拟钥匙串。
实际运行路径为本机 `macos-monitor-summary-ui-r1/GCSA Aegis.app`，新资料为同目录
`profile`，初始 PID 为 13552。签名后整包清单摘要为
`3e6806fcc992981d011cc87e84bb6bb8dcf0483eac949411e1f6154044eff49b`，完整身份在
`app-identity.json`。这仍只有内部诊断资格，不是 Developer ID 签名或发行包。

真实界面证据如下：

- 空白页可以通过固定工具栏入口打开完整 Agent；首次使用引导与模型检测可见。
- 初次逐字符输入出现模型名被截断，保存后立即核对发现；随后通过完整粘贴修正并再次
  核对保存的精确名称为 `Qwen3.6-35B-A3B-Uncensored-Heretic-MLX-4bit`。未用错误名称
  执行任务；这项输入现象保留，不凭一次修正就声称输入稳定性已通过。
- 07:32:46 UTC 从空白页输入唯一来源 `http://127.0.0.1:52861/article` 的续航与保修
  查询。07:32:48 显示模型已生成 R0 只读计划并开始操作，07:33:24 因没有新的有效页面
  观察而停止。没有生成摘要或创建监控，不能记为任务完成。
- 本地服务自身四项真实 HTTP 自测通过，但上述 App 任务期间没有收到目标网页请求。
  `state.json` 始终为修订 1，未提前切换资料去伪造监控变化。
- 本轮进程采样再次显示 `OSCrypt GetKeyTask → KeychainPassword::GetPassword →
  SecItemCopyMatching → SecurityServer decrypt` 等待系统返回。采样摘要为
  `527b78f9de80d5a4efb34ec6f1cd360b9b96ba6aecdb764e961fef919f1d8ec7`。这确认系统密钥
  读取仍有阻塞，尚不能单独证明全部导航失败均由它造成。
- 已请求用户人工查看钥匙串授权提示；随后 Mac 锁屏，网络诊断页操作被 Computer Use
  拒绝。未绕过锁屏、读取凭据、自动放行系统授权或关闭加密。

15 分钟监控尚未创建，更未发生真实定时检查。失败提示、界面可见与正常原生测试
不能替代第 2、3、5、8、9、10 项所要求的真实闭环。

### Android 新候选正在编译

上一轮 `android-monitor-recovery-r1` 的实际结束结果为退出码 1，源码未变且
`apk_sha256=null`。底层日志为 24826 步完成、61 步失败、39728 步剩余；失败原因为
对象文件时间 03:22:46 晚于虚拟机当时 03:19:55。日志记录了时间不一致；不把这个
失败改称人为中断或仍在构建，也不通过修改文件时间解决。

恢复 Docker Desktop 后再次核对所有容器：只有专用 `aegis-android-builder` 随后启动，
其他四个容器仍退出且 restart 策略均为 no。同步前已归档并逐文件验证旧 44 文件，
归档 SHA-256 为 `c752512fd448a446d26dc685ab0ab549f2d4267496da8c9f1c955289144fb370`。
新候选有 13 个既有文件变化、3 个新增文件，全部 47 文件与集成 overlay 及独立 macOS
相符；17 项原有夹具删除未动。

本机 `android-monitor-summary-r2/` 记录新构建。开工前宿主与虚拟机时钟差小于 1 秒，
新增时钟检查的六组代表性输入自测通过，过大偏差会拒绝编译，不修改系统时间。
沿用四路编译、静态分析开启和全部缓存；仅为当前编译进程建立临时闲置睡眠断言，
不保持屏幕亮起、不阻止锁屏、不修改电源设置。07:40 UTC 该构建实际仍运行，
尚无新 APK、Pixel 安装或真机任务结果；最新 Android 通过不能提前授予。

Windows 上传安全告警仍待人工核查，没有重试载荷、绕过 Defender 或放宽权限。
全体十项完成标准保持未关闭；iOS 仍按已确认范围跳过，未提交、推送或发布。

## 2026-09-06 08:32 UTC：真实 Qwen 摘要通过，重启后的首次定时检查失败

### 真实界面与正常退出后的持久化证据

本节仍使用上一节同一签名测试 App 和全新专用资料，未更换默认 App、复制日常资料或
放行系统授权。07:56:10 后实际网页请求恢复；没有根据这一变化推断用户进行了哪种
钥匙串操作。07:57:46 先关闭测试网页、回到空白页，再提交唯一来源的续航与保修查询。
07:57:55 本地 `Qwen3.6-35B-A3B-Uncensored-Heretic-MLX-4bit` 经真实计划、读取、检查
流程完成，界面答案为续航 18 小时、保修 24 个月，并给出对应测试网页来源。前次失败
没有覆盖；本案例通过也不代表全部场景矩阵完成。

07:59:13 首次创建自动化仍失败。原句明确禁止搜索、登录、填表和下载，但前端简单匹配
“下载”，选择了不含 `monitor.create` 的一次性下载模板；本地模型两次规划后停止。
只读数据库核对确认了该任务的模板工具范围，不能把根因笼统归为模型能力不足。

08:02:19 使用语义等价的限制句“权限仅限读取这个本地公开测试来源和创建上述监控，
不允许任何其他操作”进行对照；08:02:24 成功创建 15 分钟网页更新监控。随后关闭所有
目标标签、保留空白页并从菜单正常退出，再以原 App、原资料重启。签名整包身份未变，
未调整时钟、调度时间或基线；界面保留首次时间 08:17:24.886 UTC。

首次真实到点失败：下一次时间变为 08:32:24.886 UTC，但测试服务没有收到新请求。
再次从菜单正常退出（退出码 0）后，只读数据库证明：

- 两步计划 `page.observe`、`monitor.create` 的 `next_step=2`、总步骤数为 2。
- 任务从退出前的 `running` 恢复成 `recovering`；网络调用计数仍为 4，没有检查请求。
- 监控仍启用，但 `last_run=0`、检查状态为未检查、观察密文长度为 0。
- 目标密文仍为 35 字节，调度时间推进了 15 分钟；没有建立首次基线，更没有变化摘要。

证据为本机 `macos-monitor-summary-ui-r1/restart-r1/first-deadline-failure.json`，
数据库 SHA-256 为 `a1c61190db0c45041fc336a4fe2ab428b0823fe52b4dd9a382e5f06fbad097a5`。
真实 HTTP 日志最后一项仍为 08:02:19.456 UTC 的第 9 次请求，日志摘要为
`e0b56e753bbc7b646787f7a71b7b1955f80459e192c6771b02e235e89ab4586a`。
修订始终为 1，未提前修改为修订 2；该 App 现已正常退出，不能把后续时间当作仍在进行
有效的监控等待。

### 开发增量与测试边界

定位到正常完成和最终模型格式回退的两条路径都提前结束 Runtime，没有将已完成的
监控创建计划正常收尾。现已让这两条路径同样进入验证并完成任务，监控保留为独立调度；
旧网页 Actor 和一次性操作授权正常释放。未给旧 `recovering` 任务延长授权或自动重放。
界面只把已完成的监控所属任务，或刚产生监控结果的运行中任务，显示为定时等待；
真正的恢复中状态不再虚报就绪。新增真实原生浏览器用例覆盖正常模型完成、两次格式
失败后的回退、加密监控保留、旧 Actor 释放及暂停管理，尚未运行，不能记为修复验收通过。

另外补齐以下增量：

- 自动化按钮不再按禁止句中的下载、购买或书签词选择一次性模板。目标和全部禁止条款
  原样传给原生端；缺少明确来源时仍由模型路由。9 项真实按钮函数对照通过，普通任务
  的下载、购物和收藏夹入口不变。
- 原生自动化范围保留选中模板的只读能力、必要导航和监控管理，移除填表、购物结账、
  下载执行、书签修改及其他操作权限；不增加来源、标签或预算。新增两项 Core 测试，
  完整 119/119 单次通过，无重试。结果为 `automation-entry-core-tests-r1/result.json`，
  二进制摘要 `6f798e9ce5083fb451c7796ab83a4ec52a4458a5b74a8af4972f2f7c386401c1`，
  原始结果摘要 `60dd47491cf06c7c4a2999c022daa7c6ceea03d8c2134f1dec5f97e68367802a`。
- 规划回退只有在实际接受后才记录“已改用只读方案”，新增失败不误报的服务用例。
- 模型保存以“本次无错误且配置有效”为成功条件；失败保留非敏感草稿、展开设置、给出
  可读原因，异常与成功都清空密钥输入。8 项模型表单状态测试通过；真实键盘输入截断
  尚未稳定复现，不声称该现象已修复。

`quality:fast` 全部通过，含共享 Core 168 项、界面状态 27 项、颜色 6 项、上下文提示
2 项、摘要渲染 6 项、模型表单 8 项、入口对照 9 项，以及 Android 目标/操作、脚本、
模型中转、类型、合同和构建检查。以上 DOM 测试不是实际键盘或跨平台界面验收。
新入口、服务和浏览器用例已按正常 GN 配方独立编译；08:31:18 UTC 当前监控收尾版本的
6 个编译单元全部通过，证据为 `monitor-registration-compile-r1/status.json`。完整原生
浏览器执行和新 App 重建仍待完成。Core 119 项没有覆盖本次新的收尾路径，不能
将其与原先 452 项简单相加，宣称当前全部增量已通过。

开发 overlay 当前比冻结候选多 7 个既有文件修改及 2 个工作流文件修改，尚未同步到
正在构建的 Android 或独立 macOS 源码。Android 同一构建进程持续运行，缓存、四路
并发和静态检查保持不变。08:29 UTC 系统文件句柄约 20.3 万，未满足已设的大型链接
启动门槛，因此没有启动并行大型 Mac 链接、停止 Android 或修改系统上限。Windows
仍待人工核查安全告警；全体十项门槛继续保持未完成。

## 2026-09-06 08:46 UTC：收尾时间线对照与下一轮输入固化

继续复核监控收尾增量，发现 `completed + monitoring` 会被时间线当成“尚无完整结果”。
新增真实产品渲染函数的六项 DOM 对照，修正前其中正常创建、重启后创建记录、暂停后
创建记录三项失败；修正后六项全部通过。时间线现在明确显示“监控已创建”，仍有事项
未完成时优先显示“部分完成”，普通任务和真正缺少结果的提示不变。

原生 `FinishedMonitorRegistrationHasAnInertOwner` 对照也改为提供真实已观察页面的来源
URL，并分别断言正常完成消耗 3 次模型请求、两次格式失败后回退消耗 4 次。这样不能用
错误的空引用让“正常完成”实际走到格式重试，或把未收到响应的等待误作产品失败。
该用例仍未运行，只通过编译。

08:44:11 UTC `monitor-registration-ui-r1` 的原生界面资源、浏览器用例及正常生成配置下
的 WebUI TypeScript 检查全部通过，原来的正常构建目录、产物、Android 源码和默认
App 哈希未变。再次完整运行 `quality:fast` 并通过，新增时间线六项也包含在其中。

下一轮源码快照为本机 `monitor-registration-candidate-r1/source/`；49 个文件全部逐字节
核对，九个变化输入均与其最新编译或类型检查记录绑定。清单摘要为
`114d05a5139cd19c94cbf244ea3bde4924e3ee87d297f822285970e014ce24a3`。
它包含原生测试的精确名称集合：Core 119、通用防护 87、浏览器单元 178、原生浏览器 74，
共 458 项。**458 是下一轮必须运行的数量，不是通过数量。** 两个运行源码目录仍是
47 文件 R2，未改正在运行的 Android 编译，也没有替换已构建 App。

为尽早发现另一条链路的问题，在同一已签名 R2 App 下启动了新的独立空白测试资料
`macos-monitor-summary-live-r1/profile`，目的仅是不重启的真实定时与 Qwen 摘要对照。
先重新核对原有 452 项结果及 App 身份，再将前次失败时的 HTTP 日志固化为
`macos-monitor-summary-ui-r1/restart-r1/first-deadline-fixture-requests.jsonl`。
旧失败数据库未变。新 PID 为 40039，启动于 08:38:21.919 UTC；首次界面操作被锁屏
阻止，已请求用户手动解锁，没有配置模型、创建监控或绕过系统限制。

Android 同一 Siso 进程仍在真实执行编译，08:36 UTC 容器 CPU 约 324%、内存约
5.3 GiB；这些数据仅证明进程活跃，不能推算最终完成时间或授予 APK 通过资格。
完整三平台常用场景、真实定时及最终同版本验收继续保持未完成。

## 2026-09-06 09:02 UTC：新版 macOS 源码就绪，完整构建仍待资源条件

同步前再次复核 47 文件 R2 的既有 452 项结果、源码和二进制身份，全部一致。这是旧结果
的复核，不是重新执行 452 项。随后只向独立验收目录应用冻结候选的九个变化文件，
没有修改正在运行的 Android 构建输入、canonical 默认 App 或任何用户资料。

`prepare-macos-monitor-registration-r1.mjs` 的九项自测及真实同步前检查通过。它先备份
全部 49 个源文件，再逐一读取压缩包内容核对摘要，随后输出补丁，由补丁工具应用。
备份位于 `macos-monitor-registration-stage-r1/mac-source-before.tar.gz`，摘要为
`a68dabbd2973a2476cce09ef21d26fc310b16af66a5cf700e5c7627763c601a8`。
同步后的 `stage-status.json` 证明 49 文件与冻结输入一致，Android、默认 App 和链接
限制未变。旧失败 App、数据库和请求记录仍保留。

下一轮完整构建与测试入口已准备：`build-macos-monitor-registration-r1.mjs` 和
`test-macos-monitor-registration-r1.mjs`。构建器只允许完整五目标构建，保留单路并发及
原有磁盘、文件句柄守卫；只读预检显示句柄使用 223,788、上限 491,520，不满足启动前
使用量低于 30,000 的既有门槛，因此没有启动构建。没有调高系统限制或停止 Android。

测试器继续严格验证冻结清单、四个二进制身份、完整 458 项名称、单轮且无重试。
其十五项自测通过；对尚不存在的新构建结果执行验证时，明确拒绝为旧二进制授予新版
通过资格。上述共 24 项是准备脚本自测，不是产品原生测试。当前新版整包与 458 项实际
执行仍未完成。Android 同一会话仍存活并持续产生编译记录；测试 App 进程仍存活，但
再次检查仍为锁屏，真实界面与定时测试继续等待手动解锁。

## 2026-09-06 09:33 UTC：Android 下一候选切换入口准备

新增 `prepare-android-monitor-registration-r1.mjs`，输入仅来自已冻结的 49 文件源码
快照。它不停止容器、不自行改写产品文件；只有专用容器已停止、无其他运行容器，
且上一轮宿主与容器内构建记录都成功、APK 身份一致时，才允许备份并生成补丁。
同步前后还核对原有 17 项删除、V8 基线、macOS 同候选来源与所有源码摘要。

十五项脚本自测通过；真实只读预检报告容器仍运行、上一轮无完成结果，因此不可切换。
进一步实际调用准备入口，确认它在创建备份目录前拒绝操作；再次逐一核对全部 49 个
相关输入，仍与当前 Android 的旧候选一致。没有应用补丁、创建新备份或重启构建。
这些结果只证明切换保护有效，不是新 APK 或产品原生测试通过。

同一 Android 构建进程继续运行。09:33 UTC 日志仍有新的编译与生成步骤，没有取得
最终退出结果。新版 Android 源码切换、整包构建、设备真实操作与完整十项验收仍待完成。

## 2026-09-06 10:22 UTC：下一候选 Android 构建入口准备

新增 `build-android-monitor-registration-r1.mjs` 与配套容器脚本
`android-monitor-registration-r1.sh`，旧 R2 两个入口保持不变。新入口绑定冻结的
49 文件候选及同步回执；保留四路并发、静态分析、缓存、磁盘与虚拟机资源检查、
宿主与虚拟机时钟检查，并在发现本机已有已知 Siso 构建时拒绝启动。后续运行记录
同时绑定入口脚本、同步记录和构建参数摘要。

Node 与 Shell 语法检查、六项时钟参数自测通过。当前真实预检明确报告源码尚未切换，
因此不可启动；实际调用启动入口也在创建记录或操作容器之前拒绝。随后核对 49 个
Android 相关输入和旧 R2 入口均未变化，新构建记录目录没有产生。

这些结果仅证明准备入口与未同步保护有效。配套容器脚本还没有实际执行，新候选
APK、设备界面和完整原生验收均未通过。旧 47 文件 Android 构建继续使用原会话运行，
10:22 UTC 仍在生成实际编译记录，没有重启、切换输入或开始并行大型 macOS 构建。

## 2026-09-06 11:01 UTC：新候选 macOS 整包身份与启动保护

补充 `macos-monitor-registration-app.mjs` 与
`launch-macos-monitor-registration-ui-r1.mjs`。尚未执行的新候选构建入口现在会在
成功后记录整个 App 的文件、符号链接和摘要，而不仅是四个测试程序；启动入口
须重新验证同次构建、精确的 458 项单轮回归和整包清单，才可签名独立副本并创建
空白测试资料。App 符号链接不得越出包边界，已有验收目录不允许覆盖。

三个脚本语法检查通过，15 项自测通过，其中包含真实读取现有包的 333 项清单。
该读取只验证脚本，不将现有旧包当作新候选产物。另分别实际调用预检和启动入口，
二者均因新候选缺少构建结果而在复制、签名、创建资料前拒绝。随后逐一核对
macOS 新候选 49 文件及 Android 原 49 个相关输入均未变化。

本轮没有创建新 App 或用户资料，没有运行新候选的 458 项产品测试。macOS
大型构建预检仍未达到资源启动条件；独立旧测试 App 的界面检查仍返回锁屏。
Android 同一会话经 55 秒等待仍存活，11:01 UTC 日志继续产生 V8 与 Blink 编译
记录，尚无最终退出结果。三端真实验收与完整十项标准仍未完成。

## 2026-09-06 11:40 UTC：旧候选持续运行对照已进入真实定时等待

11:29 UTC 原独立测试 App 的界面恢复可操作；仍为 47 文件 R2 签名副本，
使用先前建立的全新 `macos-monitor-summary-live-r1/profile`，没有重新启动 App。
通过工具栏入口检测并保存本机无密钥 Qwen 连接，模型名称这次完整输入；
11:31:44–11:31:52 真实任务打开唯一测试来源并完成摘要，正确显示续航 18 小时、
保修 24 个月及精确来源。页面服务记录对应的真实访问。

通过自动化入口创建 15 分钟只读网页变化监控，随后关闭两个目标页面，只保留
空白页与运行中的浏览器。界面显示一个监控，下次运行时间为 11:49:10 UTC。
当前仍未建立定时检查基线，因此测试页面保持 revision 1；没有提前修改页面、
缩短频率或把创建成功当作定时执行成功。本轮是不重启对照，不代表 49 文件
注册收尾修复通过，更不替代后续新候选的重启验证。

本轮中文监控目标在 `typeText` 输入时出现丢字及冒号缺失。相同字符串在原生
地址栏对照中出现完全相同的缺失，未提交该地址栏输入；用两次 Escape 恢复空白页。
监控目标已通过粘贴恢复并逐字核对后才创建。此证据不足以把问题归因于 Agent
输入框或证明产品已修复输入问题；粘贴工具曾报观察超时，但随后真实界面内容完整。

另观察到窄侧栏截图的左右文字裁切，原因尚待确认。运行中资料库拒绝只读连接，
未尝试更改锁或停止进程。实际核对 PID 40039、专用资料路径、签名包 334 项和
旧失败数据库摘要均正确且未变。证据记录为
`macos-monitor-summary-live-r1/ui-observations-r1.json`；Android 原构建会话同时保持运行。

## 2026-09-06 11:51 UTC：首次到点访问已触发，基线结果仍待核对

持续运行对照在 11:49:10.280 UTC 产生 `/article` 的真实 HTTP 200 请求，页面
服务记录 sequence 13、revision 1，与界面安排的首次检查时间相符。测试 App
同一会话仍存活，期间没有重启或手动打开目标页。它与之前重启后仅推进时间、
没有 HTTP 请求的失败表现不同，但尚不能据此证明完整检查通过。

检查界面时 Mac 再次锁屏；已请求手动解锁，没有绕过锁屏或停止 App 来读取资料库。
暂不把网络访问等同于已保存基线，也不提前改变测试页面。状态和原始请求字段记录在
`macos-monitor-summary-live-r1/first-deadline-http-r1.json`，明确
`baseline_verified=false`。后续仍需核对页面观察、基线以及真实变化摘要。

## 2026-09-06 14:16 UTC：持续运行对照确认基线，开始真实变化测试

14:14 UTC Mac 界面恢复可操作。原 PID 40039、会话 29643 和专用资料路径保持不变，
没有重启。真实“网页更新”卡片显示“上次检查成功。 · HTTP 200”，下一次为
22:19:11 本地时间；当前没有变化摘要，浏览器只保留 about:blank。

核对 Android 构建源中的同候选 47 文件 SHA 全部匹配冻结清单；其中网页更新成功路径
要求页面观察核验通过并取得基线结果，不是仅依据 HTTP 200。结合真实界面结果，
已满足持续运行变化对照的基线前提。此项不代表已经读取并核验磁盘数据库、重启恢复、
无系统通知或新 49 文件候选。证据为
`macos-monitor-summary-live-r1/baseline-ui-verified-r1.json`。

确认基线后才把合成页面 revision 1 改为 revision 2：续航 18→24 小时、保修
24→36 个月。等待真实下一次 15 分钟检查及本地 Qwen 变化摘要，不触发手动检查，
不借旧候选结果代替新修复的完整构建和 458 项回归。Android 原构建继续运行。

## 2026-09-06 14:20 UTC：本地 Qwen 真实定时变化摘要通过持续运行对照

14:17:32 UTC 对 `/status` 的代表性读取确认 revision 2 为 24 小时续航、36 个月保修，
没有手动触发监控。14:19:11.130 UTC，原 App 的定时检查实际请求 `/article`
（sequence 33、HTTP 200、revision 2）。14:19:41 UTC 的真实界面与截图显示：

> AI 变化摘要：电池续航从 18 小时提升至 24 小时；保修期从 24 个月延长至 36 个月。

两项变化均与前后合成页面一致，卡片显示上次检查成功，下一次 22:34:12 本地时间；
连接模型仍为本地 `Qwen3.6-35B-A3B-Uncensored-Heretic-MLX-4bit`。浏览器只保留
about:blank，原 PID 40039 和专用资料路径保持不变。

再次逐项核对签名 App 全部 334 项、整包摘要、47 文件候选清单和此前 452 项原生测试
状态文件摘要，全部一致。结果记录为
`macos-monitor-summary-live-r1/change-summary-ui-verified-r1.json`。保持 revision 2
进入无变化后续检查；暂未声称系统通知送达、重启恢复、新 49 文件候选或三端整体验收通过。

## 2026-09-06 14:26 UTC：系统提醒送达尚不具备验收条件

只读打开 macOS 系统设置的“通知”页面，看到两条同名 `GCSA Aegis` 记录均显示
“关闭”。界面没有给出可确认每条记录具体包路径的依据，未猜测对应关系，也未修改
任何开关或权限。已请求用户确认并手动启用对应独立测试 App 的通知；浏览器内摘要
仍显示正确，当前不能将该摘要当作系统提醒送达证据。观察记录为
`macos-monitor-summary-live-r1/system-notification-observation-r1.json`。

## 2026-09-06 14:35 UTC：真实无变化复查及摘要保留通过持续运行对照

保持 revision 2 后，14:34:13.070 UTC 定时检查再次读取 `/article`（sequence 35，
HTTP 200）。14:34:21 UTC 的真实界面与截图显示检查成功、下一次 22:49:13 本地时间，
此前“18→24 小时、24→36 个月”的摘要完整保留，监控数量仍为 1，只保留 about:blank。
原 App 未重启，页面没有再变动。证据为
`macos-monitor-summary-live-r1/unchanged-followup-ui-verified-r1.json`。

该结果证明浏览器内的无变化复查成功及历史摘要保留；没有把相同显示文案当作模型
调用次数或系统通知次数的证据。下一步只对同一条隔离测试监控验证暂停/恢复，
不删除用户监控，不以旧 47 文件候选结果代替新 49 文件候选验收。

14:36 UTC 通过真实“暂停监控”按钮暂停这一条测试监控，界面与截图显示“已暂停”
和“恢复前不会继续运行”，原摘要保留，“恢复监控”按钮可见。此时仅确认 UI 操作，
尚未宣称定时访问已经停止；保持 App 和合成页面服务运行，计划观察至原定
14:49:13 UTC 之后再核对请求记录并恢复。起点记录为
`macos-monitor-summary-live-r1/pause-start-ui-r1.json`。

## 2026-09-06 14:53 UTC：Android 构建终止、新 macOS 构建启动及暂停截止观察

Android 原宿主会话 23869 在 14:47:47.779 UTC 真实返回退出码 255。只读检查专用
容器确认已在 14:47:39.210 UTC 以 137 退出、PID 为 0，`OOMKilled=false`，
`docker top` 也确认不在运行。容器内没有完成记录，日志尾部没有编译诊断；限定时间
的容器事件查询为空，不能据此推断具体终止原因。47 文件源码验后未变，新 APK
摘要为空；日志和缓存没有清理，证据为
`android-monitor-summary-r2/terminal-observation-r1.json`。不得再将本轮记为运行中或成功。

随后新 macOS 49 文件候选的源码、默认 App 和资源预检通过：文件句柄已用 12962、
可用 478558，磁盘可用约 61.6 GiB。14:49 UTC 启动原有独立构建脚本，宿主会话
57003；仍保持单路和原资源保护，不修改默认 App，不复用旧 452 项结果作为新
458 项通过证明。启动记录位于 `macos-monitor-registration-r1/before.json`，
构建尚待真实结束。

旧 47 文件持续运行对照在 14:50:52 UTC 越过原定 14:49:13 检查时刻：原 App 会话
仍存活，`/status` 返回 revision 2 的正确页面，暂停后没有新的 `/article` 请求
（最后仍为 sequence 35）。但随后界面工具用独立包路径定位到同名浏览器的资料
选择页，未操作或改变任何资料，也未重启、恢复原监控。已请求用户将原测试窗口
切回前台；这里只通过请求日志检查，未声称截止后的同一窗口 UI 复核或恢复运行
通过。证据为 `macos-monitor-summary-live-r1/pause-deadline-http-r1.json`。

## 2026-09-07 本机工作区收敛及 49 文件候选实测

本节日期为北京时间。8 个工作区的非忽略变更已逐项备份并核验，两组 Git bundle
均通过校验。主目录已吸收集成副本的 126 个较新文件，收敛时 860 个文件内容及权限
一致；主目录独有文件保留，Git 索引与分支没有改写。此后仅在主目录继续源码开发。
恢复记录位于 `.artifacts/v2-final/workspace-consolidation-20260906/`。
三个旧工作区的保全归档及两个旧 obj（约 17.78 GiB）的可恢复回收仍待精确清单确认，
尚未移动或删除。忽略数据、资料、模型、原型独有分支和失败证据不作为缓存处理。

默认本机 Chromium 指针改为保留的 macOS 构建区。实际调用发现顶层 `browser:run`
原先只列脚本却返回 0，修正为明确执行 run 脚本；实际入口测试又暴露系统 Bash
无参数时的空数组错误，开发/Release 两条路径均已修正。新增顶层入口的实际 dry-run
fixture 通过，完整 `quality:fast` 重跑通过（168 核心测试及全部既定脚本检查）。
真实默认启动预检会因缺少 AegisLocalDev 产物而拒绝，未回退或启动旧母库 App。

此前构建在 14:57 UTC 已正常结束。第一次原生验收失败是 browser-unit 清单漏计两个
共享源码用例，原失败证据保留；产品源码未改变，补充合同后重新单次运行四个完整套件，
于 16:06:20 UTC 通过 460 项（119/87/180/74），无重试，主目录与 macOS 49 文件
候选逐项一致。结果在 `macos-monitor-registration-tests-r2/`；不是实机或发行通过。

新独立签名副本在 `macos-monitor-registration-ui-r2/GCSA Aegis.app`，使用同目录
独立 profile，未改日常资料或系统权限。真实工具栏入口打开，连接本地
Qwen3.6-35B-A3B-Uncensored-Heretic-MLX-4bit。
00:11 的监控目标含“不要下载、整理书签、提交或购买”时，计划修正后仍失败，监控数 0；
没有把此安全停止视为功能通过。失败记录为 `negative-constraint-failure.json`。
无否定语义区分的书签目标校验和缺少监控结尾的计划恢复路径是待核查线索，
尚未获取该次底层校验错误，不能把静态线索当作唯一根因。

00:17 的同权限对照表述成功创建 1 个网页变化监控，界面计划仅为 page.observe
和 monitor.create，数据仅 public_page，来源限该本地 fixture；时间线明确“监控已创建”。
正常退出后只读数据库确认 owner 为 kCompleted（10），计划 2/2、目标密文存在，
首次观察尚空。原失败任务仍为 kFailed（11），没有被覆盖。
该实例正常退出码 0，原旧 App 未退出。00:21 重开同一包、同一 profile 后模型及监控
恢复，只有 about:blank，下次时间仍为 00:32:27；实际到点读取和基线建立尚待验证。
记录在 `restart-before.json` 与 `restart-identity.json`。另观察到窄侧栏裁切以及
重启后残留“正在制定计划”文案，均未宣称修复。

补充更正：14:50 暂停观察只证明 HTTP 日志截止范围；后续查明原持续运行实例已经退出，
不能把另一同名资料选择窗口算作同一实例仍运行，也未通过暂停恢复验收。
Android 的 47 文件编译因 Docker Desktop 退出而中断（操作发起者不明，不是已证实 OOM）；
49 文件已同步到原 Android 构建区，但新构建仍未启动。Windows Defender、Pixel 调试
及系统通知的既有授权边界不变；不以整理目录或测试通过替代完整 M0–M7 十项验收。

### 00:32 首次定时检查补证

同一 49 文件测试包、同一独立资料和 PID 50974 未再次重启或修改 fixture。
预定 16:32:27.989 UTC 后，服务端 sequence 42 在 16:32:28.030 UTC 收到
`GET /article`，返回 HTTP 200、revision 2。随后真实界面显示“上次检查成功”及
HTTP 200，下次时间为北京时间 00:47:28；前台仍仅 about:blank，模型及 1 个监控不变。
进程参数、签名后整包摘要和主目录/macOS 候选重新核对一致，460 项既有原生结果
重新核验通过（本次没有重跑原生套件）。记录为
`macos-monitor-registration-ui-r2/restart-first-deadline-verified.json`。

因此“正常重启后首次定时检查”通过。匹配源码的成功路径要求有效观察值及必要密文，
可据此推断运行时基线建立；但本次未读取截止后的数据库，不能视作基线持久化读取、
后续变化摘要或系统通知通过。否定约束失败、侧栏裁切及残留计划文案仍开放。

### 04:38 恢复界面验收并准备新版变化对照

北京时间 04:36 精确测试窗口恢复可访问。同 PID 50974、同独立资料的界面显示
最近一次检查成功、HTTP 200，下次为 04:47:29；当前仍只有 about:blank 和 1 个监控。
服务端 sequence 73 对应 20:32:29.398 UTC 的 revision 2 请求。锁屏期间请求记录
持续存在，但未将那些未观察的界面结果逐轮记为通过。签名后整包、候选及 460 项
既有原生结果重新核对一致；本次没有重跑原生测试，侧栏裁切与残留计划文案仍存在。

保存变更前基线记录后，仅将合成本地 fixture 从 revision 2 切换到已支持的 revision 1：
续航从 24 降至 18 小时，保修从 36 降至 24 个月。测试包、进程、监控及检查时钟均未改，
没有手动触发监控。真实 `/status` 自检确认新内容，14 项检查及差异格式检查通过。
起点记录为 `macos-monitor-registration-ui-r2/change-followup-start.json`。
本段只记录变化测试已准备；04:47 之后的真实定时读取、变化摘要及系统通知尚未通过。

### 07:38 新版真实变化摘要通过

同一 49 文件测试实例 PID 50974 持续运行，fixture 保持 revision 1。首次变化后
定时请求为 sequence 76、20:47:29.484 UTC，返回 HTTP 200；最近一次为
sequence 96、23:32:31.952 UTC。07:37 恢复读取真实界面后，监控显示检查成功，
摘要为“产品规格调整：电池续航从 24 小时缩短至 18 小时，保修期从 36 个月缩短至
24 个月。”方向和数值均与本轮合成变化一致，模型仍为本地 Qwen3.6-35B，前台仅
about:blank。该结果属于新 49 文件候选，不复用旧 47 文件的正向变化结果。

精确进程参数、签名后整包和候选重新核对一致，460 项既有原生结果重新核验通过，
没有重跑原生套件。记录为 `macos-monitor-registration-ui-r2/change-summary-ui-verified.json`。
本项通过定时变化摘要；不能推断锁屏期间摘要首次生成的精确时间、每一轮界面结果、
模型调用次数、系统通知送达或变化后重启持久化通过。既有否定约束和显示问题仍未修复。

07:40 通过真实界面的“暂停监控”按钮暂停唯一隔离测试监控，随后确认显示已暂停、
“恢复前不会继续运行”、恢复按钮及原准确摘要。App 和 fixture 未退出、未重启，
最后文章请求仍为 sequence 96。起点记录为同目录 `pause-start.json`；必须保持暂停
至 23:48:00 UTC 以后，越过原 23:47:31 截止，再同时核对实例存活、fixture 可用、
无新文章请求和界面仍暂停，才可认定实际停止检查通过。恢复运行尚未执行。

### 11:40 跨截止暂停通过，恢复后等待新截止

北京时间 11:39 同一测试窗口恢复可访问，精确进程 PID 50974、独立资料及签名后
整包再次核对一致。界面仍显示已暂停、恢复前不会继续运行，准确变化摘要保留。
自暂停后的日志没有新增 `/article`（最后仍 sequence 96）；本次 `/status` 自检
sequence 99 在 03:39:45.548 UTC 返回 HTTP 200 和 revision 1 的正确内容。
已超过原 23:47:31 UTC 截止，故跨截止实际暂停通过。记录为
`macos-monitor-registration-ui-r2/pause-deadline-verified.json`。

随后通过真实“恢复监控”按钮恢复同一监控，界面改为等待定时检查，下次时间
为北京时间 11:55:33，摘要保留。未重启、删除或修改 fixture。恢复起点记录为
`resume-start.json`；显示的“上次成功”是旧结果，不能当作恢复后已检查成功。
必须在新的 03:55:33 UTC 截止后结合真实请求与界面核验，恢复运行才可通过。

### 12:41 恢复后的真实定时检查通过

恢复后首个请求 sequence 100 在 03:55:33.225 UTC 返回 HTTP 200、revision 1，
最近请求 sequence 105 在 04:40:33.488 UTC 同样成功。12:40 真实界面显示检查
成功、下一次 12:55:33，原“24 小时缩短至 18 小时、36 个月缩短至 24 个月”
摘要完整保留，仍只有 1 个监控、同一本地 Qwen 模型和 about:blank。
精确进程 PID 50974、资料和签名后整包重新核对一致，暂停/恢复期间未重启。
记录为 `macos-monitor-registration-ui-r2/resume-deadline-verified.json`。
恢复定时运行及恢复后摘要保留通过；模型调用次数、系统通知和变化后重启持久化
仍不在本项通过范围内。

### 12:45 变化摘要正常重启后恢复通过

保存恢复运行证据后，通过测试实例菜单正常退出。界面读取超时不作为退出结果：
原进程 50974 已消失，原启动会话真实退出码 0，数据库无打开连接；另一旧实例
95077 仍在运行，未对其执行退出或资料操作。只读数据库完整性为 ok，读取前后
摘要一致。持久化监控为启用、上次成功 HTTP 200、失败数 0，目标密文 35 字节、
观察密文 1171 字节，owner 仍 completed；未提取或手动解密密文。

主目录/macOS 候选及 460 项既有原生结果再次核验通过后，用同一签名后测试包、
同一资料和原六个启动参数重开。新 PID 60981 的精确命令及整包内容核对一致。
真实工具栏入口打开侧栏，在自动化页看到 1 个监控、本地 Qwen 模型和原准确摘要
完整恢复；下一次仍为保存的 12:55:33，没有重新注册或重置检查时钟。
证据为 `post-change-restart-before.json` 与 `post-change-restart-verified.json`。

变化摘要及监控的正常重启恢复通过。保存的“上次成功”不算重启后新检查成功，
仍须等待 04:55:33.521 UTC 的真实检查；系统通知、否定约束及显示问题仍未通过。

### 17:41 重启后的真实检查补证及休眠边界

同一新进程 60981 在保存的首次截止 04:55:33.521 UTC 后，于 04:55:33.567 UTC
实际读取页面（sequence 106、HTTP 200、revision 1）。17:38 获取的真实界面显示
最近检查成功，原准确变化摘要保留；进程参数及整包身份再次核对一致。
记录为 `macos-monitor-registration-ui-r2/post-restart-deadline-verified.json`。
这通过“变化摘要重启后继续定时读取及保留结果”，不是只靠保存的成功标签判断。

后续请求出现较长间隔；本机只读电源日志显示合盖休眠、维护唤醒及 17:34 的开盖唤醒。
观察时卡片所列 17:26:29 已早于当前时间。当前证据不证明每次唤醒立即补检、连续清醒
时严格 15 分钟运行或整段稳定性通过；保留这些时序边界，不修改系统休眠配置。

### 17:56 否定句及重启状态的红绿回归

在主目录新增回归后，原生测试明确复现原监控目标被错误要求 `bookmark.list`，以及
被否定的检查链接/整理动作被当作必做步骤。四个新增用例中两个失败、两个只读/肯定
目标对照通过。修复必做步骤的否定子句识别后，完整核心套件 123 项单次无重试通过，
主目录与 macOS 构建区相关 C++ 源码一致，原失败与成功结果均保留于
`goal-constraints-ui-r1/`。完整原始目标、工具权限和禁止修改约束没有删除或放宽。

界面另有确定性缺陷：无活动计划一律显示规划中、重启后的空时间线显示暂无任务。
10 项新增 DOM 回归先复现 8 项失败，修改后全部通过，现共 74 项界面逻辑检查通过。
当前运行的旧签名测试包不包含这些修复；侧栏裁切、完整新 App、原句 Qwen 及跨平台
复测仍未完成。旧四个测试二进制已克隆并逐 SHA 校验保留，不用旧成功结果替代新验收。

### 18:55 新候选整包、原句 Qwen 与重启定时通过；跨平台仍在继续

本轮仅从主目录修改五个文件，冻结为 `goal-constraints-candidate-r1`，完整增量清单仍为
49 文件，SHA-256 为 `7c309786f16cfe5ebbb28fa3b7dab8581600398c78a0061730d1c4f42757358f`。
新增原生布局回归直接测真实侧栏：旧版在 240/280/320 像素时内容被撑到 327 像素，
输入框聚焦后发生横向滚动；280 像素时图标左边界曾为 -35.5 像素。修复网格和控件的
收缩边界、长内容换行及监控按钮换行后，五种宽度回归和真实监控按钮检查全部通过。
首次诊断文字不完整的失败及修正诊断后的失败都保留，没有以覆盖旧结果制造通过。

新 macOS 整包构建退出 0，465 项原生回归（123/87/180/75）全部单次通过，主目录、
macOS 构建源码、四个程序与测试精确名称重新绑定。实际运行的是新的独立签名副本，
不是旧 460 项回归对应的包。本轮快速检查也退出 0，含 168 项核心及 74 项界面逻辑检查。

使用同一个本地 Qwen 35B，原先带“不要下载、整理书签、提交或购买”的完整目标成功
创建一个 15 分钟监控，没有删减条件或要求重试。可见计划只获准该测试来源、公共页面、
`page.observe` 和 `monitor.create`。正常退出后的记录为创建任务已完成，实际使用
2 次工具、4 次模型、4 次网络。原始目标逐字核对，详情见 `original-goal-created.json`。

正常关闭并显式使用原独立资料参数重启后，PID 80699、数据库实际连接及签名后 334 个
包清单项一致；监控、本地模型和原调度恢复，界面不再残留“正在制定计划”或“暂无任务”。
首次定时请求在 18:42:04.112 发生，比保存的计划时间晚 44 毫秒，返回 HTTP 200；实际
界面显示上次成功、下一次 18:57:04，用户标签仍只有空白页。此项不推广为所有休眠/唤醒
情形，也不代表系统通知通过。

重启过程中另有验收工具隔离事件：退出后继续观察已退出的应用，误开了不带独立资料
参数的进程 80296，现已正常关闭。未继续操作其中资料、未清理或回写日常资料；不能
证明误启动时没有浏览器常规资料写入，故不把这段计作隔离通过。后续只在已明确带完整
参数启动并核对的进程上观察界面，退出后不再请求快照。事件保留于 `restart-before.json`。

Android 已验证备份并同步同一候选，保留原有 17 个删除项；专用容器中的完整构建已
启动，4 路并发、保留静态检查和缓存，不改变休眠设置。资源保护只绑定本次容器与 Siso
进程，接近余量警戒线时仅请求正常中断，不修改系统限制或停止其他程序。尚无新 APK
成功或 Pixel 安装证据。

Mac 当前锁屏，界面验证停止，等待用户手动解锁。已把合成页面改为新版规格，等待真正
的下一次变化检查；35 秒慢页面任务尚未提交。Windows、Pixel 调试、系统通知及全部
十项最终验收仍有缺项；未提交、推送或发布，也未移动仍待确认的旧目录。
完整本轮记录见 `.artifacts/v2-final/goal-constraints-ui-r1/README.zh.md`。

18:57 补充：同一 PID 80699 在 18:57:04.198 实际读取新版合成页面，sequence 124、
HTTP 200、revision 2；Mac 仍锁屏，未核验变化摘要显示或语义。该网络证据单独保存为
`change-deadline-network-only.json`，不提升为摘要或系统通知通过。Android 构建和精确
绑定的资源保护进程仍在运行，尚无新 APK 成功结果。

## 2026-09-07 19:11：Android 源码身份前置检查补齐

实际复现验收脚本的缺口：原检查只调用未暂存修改查询，清单外暂存修改、新增、改名及
删除都可能被遗漏。新增真实临时 Git 仓库回归后，原脚本在 27 项中失败 9 项；失败
报告保留为 `.artifacts/v2-final/android-source-identity-red-r1.tap`，不是产品 App 的失败。

修复仅涉及主目录的 `verify-android-agent-target.mjs`、对应测试和 Android UI 宿主
入口的参数传递。现在相对 HEAD 检查工作树和暂存区，另查未跟踪文件；禁止改名折叠，
删除必须逐项等于构建前已保存且在 `started.json` 按摘要绑定的原有删除清单。
新增 `--source-before` 参数，不接受临时新增的删除豁免；省略参数时不允许任何删除。
没有恢复用户原有删除，也没有改动已冻结的产品源码、活动构建入口或构建参数。

最终前置检查 32 项、UI 宿主交互测试 13 项，共 **45 项通过，零跳过**；报告为
`.artifacts/v2-final/android-source-identity-green-r1.tap`。`pnpm run quality:fast`
实际退出 0，包含 168 项核心、74 项界面逻辑、上述脚本与中转、仓库合同和核心构建。
源码一致性实查确认当前 Android 49 文件与清单一致、原有 17 项删除准确保留；主目录和
macOS 同样逐项一致，构建开始记录绑定的 5 项入口/参数摘要未变。

以真实当前候选调用完整 `verify`，实际以“候选构建尚未完成”退出 1，符合预期；未调用
ADB、安装或操作手机。只读源码检查明确返回 `buildVerified=false`、
`runtimeTested=false`、`releaseEligible=false`，不借由脚本通过提前关闭第 10 项。

本候选后续完整验收须在原有参数之外提供：

```text
--manifest .artifacts/v2-final/goal-constraints-candidate-r1/manifest.json
--build-dir .artifacts/v2-final/android-goal-constraints-r1
--source-before .artifacts/v2-final/android-goal-constraints-source-r1/before.json
```

相对路径以唯一主目录为基准；`android-agent-ui.mjs` 同样接收并传递 `--source-before`。
这些参数只指定验收证据，不授权安装、解锁或在默认 Profile 中操作。Android 原构建
句柄继续运行，未重开；Mac 可见界面及 Windows/Pixel 的既有等待边界不变。

## 2026-09-07 19:33：简体中文使用指引与实际控件对齐

用户指南原有“重新连接”“重新观察并继续”两处控件名称与当前候选不符，容易让新手
寻找不存在的按钮。已对照原生界面文字与按钮绑定，改为“AI 模型”中的“检测可用模型”
和“保存连接”；普通任务的“继续”仅适用于用户暂停状态，失败或目标已失效不能假定
仍可恢复。补充从空白页提供网址、检测后还须保存连接、自动化的四个模板及频率入口。

另补充“等待定时检查”表示已创建监控、应在“我的自动化”查看调度，以及“仅本次浏览器
会话”不保证重启恢复。检查成功、发现重要变化和系统通知送达分别解释，不相互替代。
指南顶部明确当前开发候选与现有安装包的区别，没有升级三端实际通过范围。

实际核对 12 处控件/状态名称与原生文字一致、两处旧名称已移除；74 项界面逻辑测试、
仓库合同及差异格式检查通过。当前指南 SHA-256 为
`b501194369aad0a504831398aec56c565e401429a106c3a41e25b4a40fcf6bb3`。
这次只更新简体中文指南与本验收记录，没有修改冻结的产品源码或活动构建入口。

Mac 系统只读查询没有返回可确认的锁屏标志，未据此猜测已经解锁，也没有调用界面工具
触发解锁或观察内容；实际可见界面验证仍待用户确认。Android 原编译进程与资源保护
持续运行，尚无新版 APK 成功记录，完整十项门槛保持未关闭。

## 2026-09-07 19:55：本地 Qwen 预检校验与修复上下文对齐

发现并修正的是验收脚本，而不是一次实际浏览器越权：原站点判断会误接收相似域名后缀，
部分计划判断未排除范围外工具，模型参数也只检查 JSON 对象而未验证请求的 schema。
新增合成回归先复现 19 项中 15 项失败；现在检查 HTTPS 与精确站点/子域边界、禁止 URL
夹带凭据或非默认端口、浏览器内部任务空目标、工具范围以及本预检 schema 子集的字段、
类型、枚举和长度。报告使用 `model-protocol-only`，明确不证明浏览器实际执行或发行。

整段 CLI 合成用例另外证明：模型两次遗漏步骤时，脚本生成的只读对照不能算作模型或
浏览器恢复成功，报告 `ok=false` 并退出 1；重复输出不会覆盖已有报告。最终 22 项回归
全部通过，最后完整 `pnpm run quality:fast` 也退出 0。失败与最终通过报告分别保留为
`local-model-preflight-validation-red-r1.tap` 和
`local-model-preflight-validation-green-r3.tap`，位于主目录的 `.artifacts/v2-final`。

11:46:32–11:46:53 UTC 使用真实本地 Qwen 35B 执行一轮：13 项中，书签“检查 URL 并给出
分类预览，不要修改”连续两次只给出 `bookmark.list → bookmark.check_urls`，遗漏
`bookmark.plan`。其余协议检查通过，但整轮正确报告失败、退出 1，原始报告为
`qwen-local-model-goal-constraints-r1.json`，未覆盖或删除。

核对当前原生规划器后确认，浏览器在这个失败分支会明确传回
`bookmark organization goal omitted required bookmark.plan step`，预检原来却只给出
笼统的顺序错误。仅将该预检修复错误与原生文字对齐，并增加源码一致性回归；未改变原
目标、允许工具、调度、请求超时、模型或最多一次修复的限制，也没有修改冻结产品源码。

11:51:36–11:51:56 UTC 对照轮通过 13 项协议和范围检查（5 路由、4 计划、4 工具参数）：
3 个计划首轮通过，书签预览仍先漏步骤，但这次一次明确修复后补齐 `bookmark.plan`，
没有使用脚本生成的恢复对照。原先带全部否定限制的 15 分钟监控目标也首轮生成仅观察
和注册的两步计划；目标与真实 App 原句逐字核对一致，只追加调度元数据。报告为
`qwen-local-model-goal-constraints-r2.json`，脚本和报告摘要见
`local-model-preflight-validation-r1.json`。

这些模型调用是真实的，但脚本提供的 tab、文档和内容证据是合成输入；没有访问上述命名
网站、操作用户收藏夹、下载或创建实际监控。13 项通过也不是摘要语义通过：合成摘要中
模型将“稳定指标 42”写成页面标题，而给定文本未证明这个标题关系；该表述作为待复核
的语义问题保留，不以函数格式正确掩盖。真实网页摘要、书签流程和界面内恢复仍待验收。

本轮再次逐项确认 49 文件在主目录、macOS、Android 三份源码一致，活动构建所绑定的
5 项入口/参数摘要未变；Android 尚无终态成功记录。没有重启构建、调用 Mac 界面或 ADB，
没有改变权限、提交或发布；完整 M0–M7 和十项标准仍未关闭。

## 21:13 更新：真实慢页面误报完成已复现，代码修复与核心回归通过

12:36 UTC重新核实同一隔离 App 的进程、完整路径和独立 Profile 后，界面工具已能
返回窗口截图，并成功切换“常用任务”。没有尝试解锁或改变系统权限。原监控实际显示
“电池续航从18小时提升至24小时；保修期从24个月延长至36个月”，与本地版本2页面
一致；最近一次可对应的定时请求为12:27:07.723 UTC。此处补齐变化摘要的界面与语义
证据，不代表系统通知已经送达。记录为
`macos-goal-constraints-ui-r1/change-visible-verified.json`。

随后从 `about:blank` 用真实本地 Qwen 提交完整原句：

> 打开 http://127.0.0.1:52861/slow 并总结页面内容。只允许读取这个本地公开测试来源；不要下载、整理书签、提交或购买。

实际测试路由是 `/slow`，固定延迟35秒返回正文。12:38:38.047 UTC收到请求，但任务
在12:38:51就显示“已完成”。技术详情只有 `tab.list` 和 `browser_metadata`，没有
`page.observe`；结果编造了“Slow Page”标题及英文描述，而实际页面是24小时续航、
36个月保修的合成资料。真实失败和页面/摘要截图已核对，记录为
`macos-goal-constraints-ui-r1/slow-page-false-completion-r1.json`，不得用“已完成”状态
或只读恢复流程将这一轮记为通过。

根因是前端关键词分类把否定清单中的“整理书签”当成工作流提示；显式网址入口直接
采用该提示，生成不含页面读取的范围。两次计划失败后，原生恢复只执行标签页列举；
结束阶段清空无页面回执的来源列表，却仍接受模型摘要。四项定向原生回归先在原实现上
全部失败，且每项只执行一次；其中还纠正了一条旧测试对纯书签计划总结当前页的错误
通过断言。原始红灯结果为 `page-evidence-core-red-r1.json`，摘要
`e2fcbb54fdd81238c93dcebfb27ee934facb7df8d9a9101860bb0b98a181ba44`。

本轮原生修复统一约束显式网址、当前页及模型路由的工作流提示，并复用原有否定语义
处理。明确的网页内容任务必须包含页面观察；只有标签元数据时不能生成恢复计划。
正常完成和最终格式恢复都必须具备实际页面读取回执，不能靠模型来源列表代替。
入口导航最多等待60秒，为35秒响应保留有界等待；没有增加模型格式修复次数。

129项核心原生测试全部单次通过，无重试，原始结果
`page-evidence-core-green-r1.json`，摘要
`d4d6b39a7211974083a30f4202fb77197a22254e65909869f17cd8362eaef4c0`。
完整 `pnpm run quality:fast` 退出0，包含168项共享 Core、74项界面状态、32项 Android
目标、13项 UI 驱动、22项模型预检及脚本、类型、合同与构建检查。上述不是新 App 的
真实 Qwen 验收。另新增两项原生浏览器回归，覆盖三种错误工作流提示和真实35秒延迟，
但它们尚未完成编译、执行。

旧 Android 候选因已知产品失败而中断：12:44:20在再次核对容器身份、启动时刻、Siso
PID、命令及工作目录后，仅向该 Siso 发送SIGINT。原构建12:44:26.714实际退出1，
`source_unchanged=true`、`apk_sha256=null`；日志为2小时33秒、23315项完成、0项
编译错误，退出原因是中断。资源守护12:44:40.291正常退出，未触发资源信号。没有停止
容器或整个 Docker，没有删除源码、日志和增量缓存。

macOS原拟仅编译三个对象，但浏览器回归对象的实际依赖图仍触发框架链接。这一轮在
56.54秒自行失败，原始错误为 `Too many open files in system`；检查时进程已退出，
没有再发送信号。服务和WebUI处理器对象已编译，不等于完整 App 或浏览器测试通过。
失败保存在 `page-evidence-macos-link-failure-r1/`，完整日志摘要
`860a25da28b2c322779b98bc687a5152914805067ee558b0b55df40789133407`。
后续不能再把这个对象目标当成避开大型链接的方式，也不能绕过既有资源门槛。

新候选清单为 `page-evidence-candidate-r1/manifest.json`，摘要
`9c41bd903e93c7cda65fa6cbb7d814bab50cda596721c29444264766eb7ee3cf`。
它绑定50个源文件、9个本轮变化和473项待完成的完整原生测试集合（129/87/180/77）。
主目录与 macOS 镜像逐文件一致；默认 App 的保护摘要未变。Android仍保留旧49文件
输入，未用部分核心通过来替代完整原生门槛，也未创建新APK成功记录。

当前需要确认不影响其他项目的 Docker 维护窗口，再正常退出并恢复 Docker Desktop，
释放虚拟机缓存句柄后完成大型链接。当前只见本任务闲置容器运行，但另有其他项目的
大量停止容器记录；未擅自关闭全局运行时。全部十项验收仍未完成，禁止发布。

## 21:47 更新：资源恢复，完整 macOS 构建已继续

用户在维护步骤确认问题后要求继续。执行前的实时检查发现 Docker 已无运行中容器，
系统文件句柄为11393，上限491520，也没有遗留 Siso 构建；本轮没有执行 Docker
全局关闭、容器停止、清缓存或系统限制修改。50文件候选在主目录、macOS 镜像及冻结
快照中逐一匹配，默认 App 摘要不变，可用磁盘约100GiB。

已启动 `macos-page-evidence-r1` 完整构建，沿用单路链接、单路本地任务和资源守护。
13:47 UTC 实际框架链接子进程仍运行，CPU约100%，没有将未结束进程记为失败或重启。
后续完整原生回归绑定473个精确测试名称；验证脚本17个自测通过，但产品测试尚未执行。
测试全通过后才允许复制、临时签名并显式使用独立资料启动新 App，再复测原始慢页面
任务。旧错误完成、链接失败、Android中断记录均保留，Windows与Pixel授权边界不变。

## 21:54 更新：修正新增测试的编译问题，R2继续完整构建

R1完整构建于13:49:41.662 UTC退出1，3分42.66秒内完成7项、1项失败、4项剩余。
框架链接已成功；失败原因是慢页面测试使用了Chromium不允许的捕获lambda回调，
不是资源守护停止。原始日志和状态保留在 `macos-page-evidence-r1/`。

已改为无捕获lambda并显式绑定测试响应对象，只修改测试文件，不改变产品逻辑、
断言或35秒真实延迟。主目录和macOS镜像同步后，冻结新的50文件R2，清单摘要为
`000cf4745ca84614e13c4493f2bb95ec953be342124115d837876caf2a8d2695`。
候选预检及验证脚本17项自测通过；473项产品回归仍须实际执行，不能沿用旧记录。
R2中该浏览器测试对象和服务单元测试对象已编译成功，最后两个测试程序链接仍在运行。

## 22:13 更新：原生构建成功，慢页面测试驱动仍在修正与复验

R2、R3完整构建均退出0，产品App清单摘要一致：
`9a4ba39cffe6875d2ebf68678d211b03f09745f68a5a6257beb3956c92a0eec5`。
每轮均实际执行473项原生回归，129项核心、87项通用、180项浏览器单元及76项浏览器
用例通过，只有 `SlowPageCannotCompleteBeforeContentArrives` 在45秒启动器门槛超时。
两轮都未重试，完整汇总为失败；没有复制、签名、启动新验收 App。

R3将35秒回调改为只发送完成信号、由主测试流程发送页面响应，但仍超时。为取得内部
等待位置，单独以120秒启动器上限诊断一次；它在约71秒失败，不计正式验收。该结果
证明测试尚未走到页面发送处，不能只把问题归因于之前的响应回调。
进一步检查发现测试等待谓词调用 `TestURLLoaderFactory::NumPending()`，该接口内部
会执行 `RunUntilIdle()`；在 `RunUntil()` 的空闲回调中调用它形成嵌套等待。

R4只将该谓词及对应断言改为无事件循环的请求列表读取，保留35秒真实延迟、禁止提前
结束/提前执行模型、读取正文后完成及来源核对。正式套件仍保持45秒启动器上限。
最新50文件清单摘要为
`5c07966dbc651fc28909eb253d5087b3f1b7fb0dcf8d5cfe0b544d573460a540`。
冻结输入、默认App保护、资源预检及验证器17项自测通过，完整构建正在运行；需待原始
测试结果确认，不因后续测试继续执行而推定某个用例通过。

## 9月10日补充：macOS提速实测及监控日志修复

最新分项证据见[当前页总结延迟与修复记录](summary-latency-2026-09-10.md)。
固定验收入口已收敛为 `/Users/lazy/Applications/GCSA Aegis Test.app`，沿用用户已确认的
个人开发签名和独立资料目录；旧包及资料副本完整保留。此处不构成发行签名或公证声明。

性能版在同一9B本地Qwen、同一cnBeta首页与同一目标下，两轮实际任务记录分别为
22.739秒和17.855秒，各3次模型、2次工具调用；此前耗时任务为349秒。
这不是缓存、页面快照和负载完全一致的受控对照。两轮同时暴露语言和列表格式不稳定，
其后已加强交付约束，但最新包的真实Qwen遵循情况仍待Mac解锁后验证。

补跑原生浏览器回归又发现监控多次变化使用重复日志编号，第二次写入会异步触发
Agent停用。先增加等待落盘的断言并在原实现上实际复现，再将日志编号绑定到每轮
检查已有的随机编号；未放宽存储错误保护、权限或通知去重。
修复后168项核心、87项通用、183项服务及74项普通浏览器测试全部单次通过，
合计512项，另有1个冷启动前置阶段。原失败记录不覆盖，测试模型与通知接收器
均为夹具，不能作为真实Qwen或系统通知显示证明。

最新固定包已包含上述修复并通过严格签名检查。本轮未变更Android或Windows安装版，
没有补出两端实机记录；十项标准仍按原范围保持未完成，未提交、推送或发布。

### 后续清单补齐与Android当前源码同步

复核原清单发现上述类名过滤漏掉15项设置与DevTools测试，已使用同一最新测试程序
按精确名称补跑，全部单次通过。当前四类原生结果为168、87、183、89项，合计527项；
512项是先前已通过子集，不再据其声称清单完整。另有冷启动前置阶段，未重复计数。

Android已同步当前54份候选源码，其中11份变化；同步前后归档、17项历史删除及
静态分析设置均保留。构建安全脚本193项自测通过，但正式启动期间其他任务连续创建
短时容器，两次被并行检查拦截，编译器尚未启动，新APK不存在。最后仅正常停止本轮
启动的空闲专用容器，没有干预其他项目。详见上述专项报告末节与
`.artifacts/v2-final/android-current-build-r20/status.json`；源码同步不等于Android实机通过。
